diff --git a/benchmark/multi-model/benchmark.py b/benchmark/multi-model/benchmark.py
index d429d5b7af94254a61fba86f4ca6ecb9f92df32b..2438f2ec4accfa7f912c8d9bd3711df43fd8ed83 100644
--- a/benchmark/multi-model/benchmark.py
+++ b/benchmark/multi-model/benchmark.py
@@ -41,6 +41,8 @@ class RequestFuncOutput:
     prefill_finish_time: float = 0.0
     finish_time: float = 0.0
     decode_timestamps: List[float] = field(default_factory=list)
+    gpu_scheduler_queue_time: float = 0.0
+    gpu_scheduler_dispatch_time: float = 0.0
 
     prompt_len: int = 0
     error: str = ""
@@ -124,6 +126,12 @@ async def send_generate_request(
                                 success = False
                                 reason = "Exceed SLO"
                             arrival_time = data["meta_info"]["arrival_timestamp"]
+                            gpu_queue_time = data["meta_info"].get(
+                                "gpu_scheduler_queue_timestamp"
+                            )
+                            gpu_dispatch_time = data["meta_info"].get(
+                                "gpu_scheduler_dispatch_timestamp"
+                            )
                             out_queue_time = data["meta_info"]["out_queue_timestamp"]
                             prefill_finish_time = data["meta_info"][
                                 "prefill_finish_timestamp"
@@ -132,6 +140,8 @@ async def send_generate_request(
                             finish_time = data["meta_info"]["finish_timestamp"]
 
                             output.arrival_time = arrival_time
+                            output.gpu_scheduler_queue_time = gpu_queue_time or 0.0
+                            output.gpu_scheduler_dispatch_time = gpu_dispatch_time or 0.0
                             output.out_queue_time = out_queue_time
                             output.prefill_finish_time = prefill_finish_time
                             output.finish_time = finish_time
@@ -822,9 +832,23 @@ async def run_tp_mode(
             output_data.append({
                 "success": output.success,
                 "latency": output.latency,
+                "latency_server": output.latency_server,
                 "ttft": output.ttft,
+                "itl": output.itl,
                 "tpot": output.tpot,
+                "wait_time": output.wait_time,
+                "arrival_time": output.arrival_time,
+                "gpu_scheduler_queue_time": output.gpu_scheduler_queue_time,
+                "gpu_scheduler_dispatch_time": output.gpu_scheduler_dispatch_time,
+                "out_queue_time": output.out_queue_time,
+                "prefill_finish_time": output.prefill_finish_time,
+                "finish_time": output.finish_time,
+                "decode_timestamps": output.decode_timestamps,
+                "prompt_len": output.prompt_len,
                 "output_len": output.output_len,
+                "slo": output.slo,
+                "slo_ttft": output.slo_ttft,
+                "slo_tpot": output.slo_tpot,
                 "model": output.model,
                 "error": output.error,
             })
diff --git a/benchmark/multi-model/trace.py b/benchmark/multi-model/trace.py
index b298f01985b9ab2091a36fa51392dcf22e8cbf85..05d7c06284617d4bcddcb3cfda29a330ce548cc1 100644
--- a/benchmark/multi-model/trace.py
+++ b/benchmark/multi-model/trace.py
@@ -237,6 +237,7 @@ class RealWorldTrace:
             with open(self.pkl_file_path, "rb") as pkl_file:
                 obj = CustomUnpickler(pkl_file).load()
             adapter_dirs, self.requests = obj[0], obj[1]
+            self._direct = bool(adapter_dirs) and adapter_dirs[0] == "__PRISM_DIRECT__"   # PAPER-FAITHFUL
         else:
             raise FileNotFoundError(f"File {self.pkl_file_path} not found")
 
@@ -416,6 +417,29 @@ class RealWorldTrace:
 
     def generate_e2e_benchmark_reqs(self, config, num_models=8, req_count=0):
         """Generate e2e benchmark requests from trace_1.py"""
+        # PAPER-FAITHFUL: direct trace -- the pickle already carries everything.
+        if getattr(self, "_direct", False):
+            out, n = [], req_count
+            for r in self.requests:
+                for k in range(config.replication):
+                    ttft = r.slo_ttft * config.ttft_slo_scale
+                    tpot = r.slo_tpot * config.tpot_slo_scale
+                    # keep the pickle's own request_id: the paired bursty and
+                    # steady traces carry the SAME id for the same payload, and
+                    # a sequential renumber (which is by arrival order) would
+                    # destroy exactly the pairing the study depends on.
+                    rid = getattr(r, "req_id", None) or str(n)
+                    rid = rid if config.replication == 1 else f"{rid}r{k}"
+                    out.append(Request(
+                        req_id=rid, prompt=r.prompt, prompt_len=r.prompt_len,
+                        output_len=r.output_len,
+                        arrival_time=r.arrival_time * config.time_scale,
+                        model=r.model, slo=ttft, slo_ttft=ttft, slo_tpot=tpot))
+                    n += 1
+            out.sort(key=lambda x: x.arrival_time)
+            span = (out[-1].arrival_time - out[0].arrival_time) if out else 0.0
+            print("[PRISM_DIRECT] %d requests, span %.1fs" % (len(out), span))
+            return out
         time_scale = config.time_scale
         replication = config.replication
         model_paths = config.model_paths
diff --git a/python/sglang/global_config.py b/python/sglang/global_config.py
index 5e7290edc51d4454f65e02320a9f404038f765ee..c40ce6dcea9dddc56290d95a493b459660146c6b 100644
--- a/python/sglang/global_config.py
+++ b/python/sglang/global_config.py
@@ -20,8 +20,8 @@ class GlobalConfig:
 
         # Runtime constants: others
         self.retract_decode_steps = 20
-        self.flashinfer_workspace_size = os.environ.get(
-            "FLASHINFER_WORKSPACE_SIZE", 384 * 1024 * 1024
+        self.flashinfer_workspace_size = int(
+            os.environ.get("FLASHINFER_WORKSPACE_SIZE", 384 * 1024 * 1024)
         )
 
         # Output tokenization configs
diff --git a/python/sglang/multi_model/model_sevice.py b/python/sglang/multi_model/model_sevice.py
index 48546889947f6955614b67e0e87e41900994fe78..422e91f9294f305cbf37ce518af0462460ce36e7 100644
--- a/python/sglang/multi_model/model_sevice.py
+++ b/python/sglang/multi_model/model_sevice.py
@@ -24,6 +24,7 @@ from sglang.srt.utils import monkey_patch_vllm_dummy_weight_loader
 
 # os.environ["TRANSFORMERS_OFFLINE"] = "1"
 import gc
+import zlib
 import os
 import time
 from concurrent.futures import ThreadPoolExecutor
@@ -205,6 +206,60 @@ def multi_process_worker(
         output_queue.put("done")
 
 
+_V4 = None
+
+
+def _v4():
+    """Lazy import so a v3 run never touches v4 code."""
+    global _V4
+    if _V4 is None:
+        import os
+        from sglang.multi_model import parallel_loading_v4 as mod
+        _V4 = (mod, os.environ.get("PRISM_V4_PAGELOCK") == "1",
+               os.environ.get("PRISM_V4_P2P_MIGRATION") == "1")
+    return _V4
+
+
+class _ModelServiceRouter:
+    """Queue facade that sends a model to a fixed worker.
+
+    Fixed, because the record of which GPU currently holds a model lives
+    inside a worker: if two loads of the same model reach different
+    workers, the migration finds no source and silently falls back to
+    host memory.
+
+    The assignment is built once from the sorted model list and travels
+    with this object when it is pickled into the spawned processes, so
+    every process agrees on it -- ``hash()`` cannot be used here, being
+    salted per process.  Sorted round-robin also spreads models evenly,
+    which a checksum modulo does not: on this six-model set crc32
+    collides onto four workers and leaves two idle.
+    """
+
+    def __init__(self, queues, model_keys=()):
+        self.queues = queues
+        self.mapping = {
+            key: i % len(queues) for i, key in enumerate(sorted(model_keys))
+        }
+
+    def _index(self, model_key):
+        key = str(model_key)
+        if key in self.mapping:
+            return self.mapping[key]
+        # Anything absent at construction still has to land somewhere
+        # deterministic.
+        return zlib.crc32(key.encode()) % len(self.queues)
+
+    def put(self, item, *args, **kwargs):
+        key = item[1] if item and item[0] in (
+            "__release__", "__kv_stash__", "__kv_fetch__",
+            "__kv_clear__") else item[0]
+        self.queues[self._index(key)].put(item, *args, **kwargs)
+
+    def __len__(self):
+        return len(self.queues)
+
+
 class ModelService:
     def __init__(
         self,
@@ -252,6 +307,34 @@ class ModelService:
         else:
             self.executor = ThreadPoolExecutor(max_workers=max_threads)
             self.streams = {i: torch.cuda.Stream(device=f"cuda:{i}") for i in gpu_ids}
+        mod, pagelock, p2p = _v4()
+        self.v4_pool = mod.StreamPool(gpu_ids, per_gpu=3)
+        self.v4_p2p = p2p
+        # model_key -> (gpu_id, state_dict) of the copy that is live on a
+        # GPU right now.  A migration reads its weights from here.
+        self.v4_resident = {}
+        # V6: model_key -> [RequestKVCapsule] left by a deactivating
+        # engine for whichever engine activates this model next.
+        self.v6_kv_stash = {}
+        self.v4_peer = mod.enable_peer_access(gpu_ids)
+        if pagelock:
+            # Only this worker's own models: the router sends a given
+            # model to a fixed worker, so locking all of them in every
+            # worker would pin the same pages N times for nothing.
+            import os as _os, zlib as _zlib
+            _n = int(_os.environ.get("PRISM_V4_NUM_SERVICE_WORKERS", "1"))
+            _own = {
+                k: i % _n for i, k in enumerate(sorted(self.model_dict))
+            } if _n > 1 else {}
+            summary = {
+                k: mod.register_host_memory(m.state_dict())
+                for k, m in self.model_dict.items()
+                if _n <= 1 or _own.get(k) == self.service_id
+            }
+            logging.info(
+                f"[PAPER-LOAD-V4] worker {self.service_id}/{_n} "
+                f"host page-lock: {summary}")
+        logging.info(f"[PAPER-LOAD-V4] peer access: {self.v4_peer} p2p={p2p}")
 
     def run(self):
         for output_queue in self.output_queue.values():
@@ -264,6 +347,61 @@ class ModelService:
                 gc.collect()
                 torch.cuda.empty_cache()
                 continue
+            if model_key == "__kv_stash__":
+                # V6: hold a migrating model's KV until its next engine
+                # asks.  Keyed by model, so source and target never have
+                # to learn each other's identity.  A second stash for the
+                # same model replaces the first: only the most recent
+                # deactivation can have live requests.
+                from sglang.multi_model import kv_migration_v6 as _kvm
+                gpu_model, source_engine_id = gpu_model
+                gpu_model = _kvm.clone_capsules_for_relay(gpu_model)
+                self.v6_kv_stash[engine_id] = gpu_model
+                self.v6_kv_queues[source_engine_id].put(
+                    ("__kv_stash_ack__", engine_id, len(gpu_model))
+                )
+                logging.info(f"[PAPER-KV-V6] relay-clone {engine_id} "
+                             f"({len(gpu_model)} requests)")
+                logging.info(f"[PAPER-KV-V6] stash {engine_id} "
+                             f"from gpu {target_gpu_id} "
+                             f"({len(gpu_model)} requests)")
+                continue
+            if model_key == "__kv_clear__":
+                stale = self.v6_kv_stash.pop(engine_id, None)
+                self.v6_kv_queues[gpu_model].put(
+                    ("__kv_clear_ack__", 0 if stale is None else len(stale))
+                )
+                logging.info(
+                    f"[PAPER-KV-V6] clear {engine_id} "
+                    f"dropped={0 if stale is None else len(stale)}"
+                )
+                continue
+            if model_key == "__kv_fetch__":
+                # None means the source stash has not reached this relay
+                # yet; [] means it arrived with no in-flight requests.
+                caps = self.v6_kv_stash.pop(engine_id, None)
+                # Answer on the dedicated queue: the engine's
+                # output_queue is the weight loader's and racing it
+                # loses the reply.
+                self.v6_kv_queues[gpu_model].put(caps)
+                logging.info(f"[PAPER-KV-V6] fetch {engine_id} "
+                             f"-> {None if caps is None else len(caps)} requests")
+                continue
+            if model_key == "__release__":
+                released_key, released_gpu = engine_id, target_gpu_id
+                held = self.v4_resident.pop(released_key, None)
+                # empty_cache() only returns this process's own
+                # allocator cache.  A CUDA IPC mapping of another
+                # process's memory is reclaimed by ipc_collect(), and
+                # without it the engine's freed weights stay resident
+                # -- 24 deactivations leaked their way to an OOM.
+                gc.collect()
+                torch.cuda.ipc_collect()
+                torch.cuda.empty_cache()
+                logging.info(
+                    f"[PAPER-LOAD-V4] release {released_key} gpu {released_gpu} "
+                    f"held={None if held is None else held[0]}")
+                continue
             logging.info(f"Model key: {model_key}, engine id: {engine_id}, target gpu id: {target_gpu_id}")
             try:
                 # gpu_model = create_empty_gpu_model(self.model_dict_serialized[model_key], target_gpu_id)
@@ -284,25 +422,42 @@ class ModelService:
                     self.output_queue[engine_id].put(t1 - t0)
                     self.output_queue[engine_id].put(self.service_id)
                 else:
-                    futures = multi_thread_copy_model_to_gpu(
+                    mod, _pagelock, _p2p = _v4()
+                    resident = self.v4_resident.get(model_key)
+                    source_sd = None
+                    if (self.v4_p2p and resident is not None
+                            and resident[0] != target_gpu_id
+                            and self.v4_peer.get(f"{resident[0]}->{target_gpu_id}")):
+                        # Target-first ordering keeps the source copy alive, so
+                        # its weights can be pulled straight across NVLink.
+                        source_sd = resident[1]
+                    self.output_queue[engine_id].put("success")
+                    mod.copy_model_to_gpu_v4(
                         self.model_dict[model_key].state_dict(),
                         gpu_model.state_dict(),
                         target_gpu_id,
                         self.executor,
-                        len(self.gpu_ids),
-                        self.max_threads,
-                        self.streams,
-                        1,
-                        0,
+                        list(self.gpu_ids),
+                        self.v4_pool,
+                        source_state_dict=source_sd,
+                        host_registered=_pagelock,
+                        tag=f"{model_key}|engine={engine_id}|src={None if source_sd is None else resident[0]}",
                     )
-                    self.output_queue[engine_id].put("success")
-                    for future in futures:
-                        future.result()
+                    if self.v4_p2p:
+                        prev = self.v4_resident.get(model_key)
+                        self.v4_resident[model_key] = (target_gpu_id, gpu_model.state_dict())
+                        if prev is not None and prev[0] != target_gpu_id:
+                            # The copy we just migrated away from is about to be
+                            # deactivated; let go of the mapping now.
+                            del prev
+                            gc.collect()
+                            torch.cuda.ipc_collect()
                     t1 = time.perf_counter()
                     logging.info(f"Time taken to copy model to GPU {target_gpu_id}, service {self.service_id}, engine {engine_id}, model {model_key}: {t1 - t0:.4f} seconds")
                     self.output_queue[engine_id].put(t1 - t0)
                     self.output_queue[engine_id].put(self.service_id)
-                del gpu_model
+                if not self.v4_p2p:
+                    del gpu_model
                 gc.collect()
                 torch.cuda.empty_cache()
             except Exception as e:
@@ -327,6 +482,7 @@ def run_model_service(
     cpu_model_dict: Dict[str, torch.nn.Module],
     input_queue: torch.multiprocessing.Queue,
     output_queues: Dict[str, torch.multiprocessing.Queue],
+    v6_kv_queues: Dict[str, torch.multiprocessing.Queue],
     max_threads: int,
     gpu_ids: List[int],
     num_shards: int,
@@ -339,8 +495,10 @@ def run_model_service(
         log_file_suffix="model_service",
     )
     model_service = ModelService(
-        cpu_model_dict, input_queue, output_queues, max_threads, gpu_ids, num_shards
+        cpu_model_dict, input_queue, output_queues, max_threads, gpu_ids,
+        num_shards, instance,
     )
+    model_service.v6_kv_queues = v6_kv_queues
     model_service.run()
 
 # def run_model_service(
diff --git a/python/sglang/multi_model/multi_model_server.py b/python/sglang/multi_model/multi_model_server.py
index d8e5235a52fcac2720ea1d8d45640511b3175874..eafd85938f2c5111f9e96b8246a57fd3d5918a02 100644
--- a/python/sglang/multi_model/multi_model_server.py
+++ b/python/sglang/multi_model/multi_model_server.py
@@ -55,6 +55,9 @@ from uvicorn.config import LOGGING_CONFIG
 from sglang.lang.backend.runtime_endpoint import RuntimeEndpoint
 from sglang.multi_model.model_sevice import ModelService
 from sglang.multi_model.multi_model_server_args import MultiModelServerArgs
+from sglang.multi_model.tp_slots import (  # PAPER-FAITHFUL-TP
+    plan_for_server_args as tp_slot_plan_for,
+)
 from sglang.multi_model.request_handler import RequestHandler
 from sglang.multi_model.request_handler_worker_pool import RequestHandlerWorkerPool
 
@@ -578,23 +581,35 @@ def launch_model_service(
         cpu_model_dict[model_path] = path_to_shared_cpu_models[(model_path, tp_size)][0]
     num_devices = torch.cuda.device_count()
     gpu_ids = list(range(num_devices))
-    input_queue = torch.multiprocessing.Queue()
     output_queues = {
         engine_id: torch.multiprocessing.Queue() for engine_id in engine_ids
     }
+    # V6: one reply queue per engine, for migrated KV only.
+    v6_kv_queues = {
+        engine_id: torch.multiprocessing.Queue() for engine_id in engine_ids
+    }
     max_loading_threads = min(4, num_devices)
     num_shards = 1
     num_model_service_workers = multi_model_server_args.num_model_service_workers
-    num_model_service_workers = 1
-    from sglang.multi_model.model_sevice import run_model_service
+    from sglang.multi_model.model_sevice import _ModelServiceRouter, run_model_service
+    # One queue per worker; the router picks a worker per model.
+    worker_queues = [
+        torch.multiprocessing.Queue() for _ in range(num_model_service_workers)
+    ]
+    input_queue = _ModelServiceRouter(worker_queues, cpu_model_dict.keys())
+    # Rides along to every engine process, so the engine can find its own
+    # reply queue without a new parameter on four call signatures.
+    input_queue.kv_replies = v6_kv_queues
+    os.environ["PRISM_V4_NUM_SERVICE_WORKERS"] = str(num_model_service_workers)
     for service_worker_id in range(num_model_service_workers):
         p = torch.multiprocessing.Process(
             target=run_model_service,
             args=(
                 multi_model_server_args,
                 cpu_model_dict,
-                input_queue,
+                worker_queues[service_worker_id],
                 output_queues,
+                v6_kv_queues,
                 max_loading_threads,
                 gpu_ids,
                 num_shards,
@@ -792,15 +807,32 @@ def launch_worker_pool_engines(
     # Prepare all engine configurations
     engine_launch_args = []
     start_port = multi_model_server_args.port
-    workers_per_gpu = multi_model_server_args.workers_per_gpu
-    num_gpus = multi_model_server_args.num_gpus
-    for gpu_id in range(num_gpus):
-        for worker_id in range(workers_per_gpu):
+    # PAPER-FAITHFUL-TP: iterate the slot plan rather than a (gpu, worker)
+    # grid.  A TP=k slot is one engine spanning k GPUs; with TP off the plan
+    # is exactly the grid and this loop is the prototype's.
+    _tp_plan = tp_slot_plan_for(multi_model_server_args)
+    logger.info("[PAPER-TP] slot plan: " + json.dumps(_tp_plan.as_dict()))
+    for _dropped_k, _dropped_combo in getattr(_tp_plan, "dropped_groups", []):
+        logger.warning(
+            "[PAPER-TP] group dropped (not silently omitted): "
+            f"tp_size={_dropped_k} gpus={_dropped_combo or 'none satisfiable'}"
+        )
+    for _slot in _tp_plan.slots:
+        if True:
+            gpu_id = _slot.owner_gpu
+            worker_id = _slot.worker_id
             engine_id = f"{gpu_id}_{worker_id}"
             server_args = ServerArgs.from_multi_model_server_args(
                 multi_model_server_args=multi_model_server_args,
                 worker_id=worker_id,
             )
+            # from_multi_model_server_args drops tp_size (it is in
+            # keys_to_remove) and the worker-pool call passes no
+            # instance_config, so tp_size silently became 1 no matter what
+            # --tensor-parallel-size said.  That is why TP=2 died in the
+            # shared-CPU lookup: the engine asked for (path, 1) while the
+            # loader had stored (path, 2).  Restore it from the slot.
+            server_args.tp_size = _slot.tp_size
             # Allocate ports for inter-process communications
             port_args = PortArgs.init_with_request_handler_ipc_name(
                 start_port, request_handler_ipc_name, schedulers_to_controller_ipc_name
@@ -810,7 +842,7 @@ def launch_worker_pool_engines(
                 (
                     server_args,
                     port_args,
-                    [gpu_id],
+                    list(_slot.gpu_ids),
                     worker_id,
                     path_to_shared_cpu_models,
                     model_names_to_model_paths,
@@ -912,12 +944,13 @@ def launch_multi_model_server(
 
         # launch model service
         if multi_model_server_args.enable_model_service:
-            engine_ids = []
-            workers_per_gpu = multi_model_server_args.workers_per_gpu
-            num_gpus = multi_model_server_args.num_gpus
-            for gpu_id in range(num_gpus):
-                for worker_id in range(workers_per_gpu):
-                    engine_ids.append(f"{gpu_id}_{worker_id}")
+            # PAPER-FAITHFUL-TP: one id per engine in the slot plan.  A TP
+            # group is named after rank0's GPU, which is where its control
+            # socket is bound.
+            engine_ids = [
+                f"{_s.owner_gpu}_{_s.worker_id}"
+                for _s in tp_slot_plan_for(multi_model_server_args).slots
+            ]
             input_queue, output_queues = launch_model_service(
                 multi_model_server_args, path_to_shared_cpu_models, engine_ids
             )
diff --git a/python/sglang/multi_model/multi_model_server_args.py b/python/sglang/multi_model/multi_model_server_args.py
index 83faf4902346942dfe5dca2544f6d05d354c27fe..0843e30fd20abca66308fb55e735369fb0ee49f5 100644
--- a/python/sglang/multi_model/multi_model_server_args.py
+++ b/python/sglang/multi_model/multi_model_server_args.py
@@ -165,6 +165,23 @@ class MultiModelServerArgs:
     enable_controller: bool = False
     enable_gpu_scheduler: bool = False
     policy: str = "simple-global"
+    # PAPER-FAITHFUL
+    enable_moore_hodgson: bool = False
+    prefill_speed_file: str = None
+    slo_base_file: str = None
+    kvpr_tau: float = 0.35
+    kvpr_rate_window: float = 30.0
+    kvpr_migration_cooldown: float = 30.0
+    kvpr_tpot_slo_scale: float = 1.0
+    parallel_model_loading: bool = False  # PAPER-FAITHFUL-V3
+    overlap_migration: bool = False       # PAPER-FAITHFUL-V3
+    # PAPER-FAITHFUL-TP.  Both default off: the released prototype is
+    # reproduced exactly unless they are asked for.
+    enable_tp_worker_pool: bool = False
+    tp_max_groups: int = 0            # 0 = every k-subset of the GPUs
+    enable_tp_anti_affinity: bool = False
+    enable_tp_anti_affinity_strict: bool = False
+    enable_kv_migration: bool = False     # PAPER-FAITHFUL-V6
     queue_id: str = str(uuid.uuid4().hex)
 
     # Async loading
@@ -909,10 +926,58 @@ class MultiModelServerArgs:
             type=str,
             choices=[
                 "simple-global",
+                "kvpr-global",   # PAPER-FAITHFUL
+                "kvpr-global-v3",
+                "kvpr-global-v4",
+                "kvpr-global-tp",
             ],
             default=MultiModelServerArgs.policy,
             help="The policy for multi-model scheduling.",
         )
+        # PAPER-FAITHFUL: paper Algorithm 1 / Algorithm 2 knobs.
+        parser.add_argument("--enable-moore-hodgson", action="store_true",
+            help="Paper Algorithm 2 (Moore-Hodgson) GPU-local request scheduling.")
+        parser.add_argument("--prefill-speed-file", type=str, default=None,
+            help="JSON of per-model chunked-prefill speed c_i in tokens/s (Algorithm 2).")
+        parser.add_argument("--slo-base-file", type=str, default=None,
+            help="JSON of per-model SLO baselines; supplies s_j to Algorithm 1.")
+        parser.add_argument("--kvpr-tau", type=float, default=MultiModelServerArgs.kvpr_tau,
+            help="Algorithm 1 migration threshold tau (relative peak-KVPR improvement).")
+        parser.add_argument("--kvpr-rate-window", type=float,
+            default=MultiModelServerArgs.kvpr_rate_window,
+            help="Sliding window in seconds for the Algorithm 1 token-rate estimate.")
+        parser.add_argument("--kvpr-migration-cooldown", type=float,
+            default=MultiModelServerArgs.kvpr_migration_cooldown,
+            help="Minimum seconds between Algorithm 1 migrations.")
+        parser.add_argument("--parallel-model-loading", action="store_true",
+            help="Load initial model placements concurrently (v3).")
+        parser.add_argument("--enable-kv-migration", action="store_true",
+                            help="Migrate a model's KV cache with the model "
+                                 "(paper 5.3). Switches the migration path to "
+                                 "preemption, since draining leaves no KV to "
+                                 "move. Off by default: the default arm "
+                                 "reproduces the released prototype.")
+        parser.add_argument("--overlap-migration", action="store_true",
+            help="Activate target before draining source during migration (v3).")
+        # PAPER-FAITHFUL-TP
+        parser.add_argument("--enable-tp-worker-pool", action="store_true",
+            help="Let worker-pool engines span several GPUs so TP>1 models can run "
+                 "under the GPU scheduler and the migration machinery.")
+        parser.add_argument("--tp-max-groups", type=int,
+            default=MultiModelServerArgs.tp_max_groups,
+            help="Cap on TP groups enumerated per tp_size (0 = all). Dropped "
+                 "groups are logged, never silently omitted.")
+        parser.add_argument("--enable-tp-anti-affinity", action="store_true",
+            help="Paper Appendix A.2.2: a TP part whose minimum-KVPR GPU already "
+                 "holds another part of the same model goes to the second-lowest "
+                 "KVPR GPU instead.")
+        parser.add_argument("--enable-tp-anti-affinity-strict", action="store_true",
+            help="Stronger than the paper: pick the lowest-KVPR GPU that holds no "
+                 "part of this model. Differs from A.2.2 only for tp_size>=3, where "
+                 "the paper's second-lowest GPU may itself collide.")
+        parser.add_argument("--kvpr-tpot-slo-scale",
+            default=MultiModelServerArgs.kvpr_tpot_slo_scale,
+            help="Scale applied to the TPOT SLO baseline used as s_j in Algorithm 1.")
         parser.add_argument(
             "--queue-id",
             type=str,
@@ -922,6 +987,9 @@ class MultiModelServerArgs:
 
     @classmethod
     def from_cli_args(cls, args: argparse.Namespace):
+        if getattr(args, "enable_kv_migration", False):
+            import os as _os
+            _os.environ["PRISM_V6_KV_MIGRATION"] = "1"
         args.tp_size = args.tensor_parallel_size
         args.dp_size = args.data_parallel_size
         attrs = [attr.name for attr in dataclasses.fields(cls)]
diff --git a/python/sglang/multi_model/request_handler_worker_pool.py b/python/sglang/multi_model/request_handler_worker_pool.py
index a875b3615072aa4c4ad4f85f832131892c2caeba..620235c62868a467ae5b21e4b7b803ffdfc46842 100644
--- a/python/sglang/multi_model/request_handler_worker_pool.py
+++ b/python/sglang/multi_model/request_handler_worker_pool.py
@@ -87,9 +87,12 @@ logger = logging.getLogger(__name__)
 class ReqState:
     """Store the state a request."""
 
+    # PAPER-OVERLAP-V6-RESPONSE-QUEUE
     out_list: List
     finished: bool
     event: asyncio.Event
+    # Events are level-triggered and lose back-to-back stream edges.
+    response_queue: Optional[asyncio.Queue] = None
 
 
 class RequestHandlerWorkerPool:
@@ -369,6 +372,12 @@ class RequestHandlerWorkerPool:
 
         assert self.is_generation, "Only generation models are supported."
 
+        # Register before publishing: a resumed/short request can reply
+        # before the caller gets another scheduling turn.
+        event = asyncio.Event()
+        state = ReqState([], False, event, asyncio.Queue())
+        self.rid_to_state[single_request_obj.rid] = state
+
         # Send request to the generation queue with model name in the key
         model = obj.model
         try:
@@ -377,15 +386,17 @@ class RequestHandlerWorkerPool:
                 obj=single_request_obj,
             )
         except asyncio.exceptions.CancelledError:
+            self.rid_to_state.pop(single_request_obj.rid, None)
             raise
         except Exception as e:
+            self.rid_to_state.pop(single_request_obj.rid, None)
             logger.error(f"Error when sending request to {model}: {e}")
             raise e
 
         # send request to the controller to update the model queue info
         if self.send_to_controller:
             self.send_to_controller.send_pyobj(single_request_obj)
-        return single_request_obj.rid, single_request_obj.input_ids
+        return single_request_obj.rid, single_request_obj.input_ids, state
 
     async def _handle_single_request(
         self,
@@ -396,7 +407,7 @@ class RequestHandlerWorkerPool:
         is_cache_for_prefill: Optional[bool] = False,
     ):
         try:
-            rid, input_ids = await self._send_single_request(
+            rid, input_ids, state = await self._send_single_request(
                 obj,
                 index,
                 input_id_index=input_id_index,
@@ -406,11 +417,7 @@ class RequestHandlerWorkerPool:
             logger.error(f"Error when handling single request for {obj.model}: {e}")
             raise e
 
-        # Recv results
-        event = asyncio.Event()
-        state = ReqState([], False, event)
-        self.rid_to_state[rid] = state
-
+        # Recv results (state was registered before Redis publish).
         if not is_cache_for_prefill:
             async for response in self._wait_for_response(state, obj, rid, request):
                 yield response
@@ -458,14 +465,10 @@ class RequestHandlerWorkerPool:
                     # Here when using parallel sampling we should consider prefill stage so the index is :  j + i * (parallel_sample_num-1) + batch_size - 1
                     index += batch_size - 1 - i
 
-                rid, _ = await self._send_single_request(
+                rid, _, state = await self._send_single_request(
                     obj, index, input_id_index=i, is_cache_for_prefill=False
                 )
 
-                event = asyncio.Event()
-                state = ReqState([], False, event)
-                self.rid_to_state[rid] = state
-
                 generators.append(
                     self._wait_for_response(
                         state,
@@ -519,7 +522,9 @@ class RequestHandlerWorkerPool:
     ):
         while True:
             try:
-                await asyncio.wait_for(state.event.wait(), timeout=4)
+                out, finished = await asyncio.wait_for(
+                    state.response_queue.get(), timeout=4
+                )
             except asyncio.TimeoutError:
                 if request is not None and await request.is_disconnected():
                     for rid in [obj.rid] if obj.is_single else obj.rid:
@@ -529,7 +534,7 @@ class RequestHandlerWorkerPool:
 
             if self.is_generation:
                 out = self.convert_logprob_style(
-                    state.out_list[-1],
+                    out,
                     obj.return_logprob if index is None else obj.return_logprob[index],
                     (
                         obj.top_logprobs_num
@@ -538,17 +543,13 @@ class RequestHandlerWorkerPool:
                     ),
                     obj.return_text_in_logprobs,
                 )
-            else:  # isinstance(obj, (EmbeddingReqInput, RewardReqInput))
-                out = state.out_list[-1]
-
             out["index"] = response_index
 
             # Log requests
-            if self.server_args.log_requests and state.finished:
+            if self.server_args.log_requests and finished:
                 logger.info(f"in={obj}, out={out}")
 
-            state.out_list = []
-            if state.finished:
+            if finished:
                 del self.rid_to_state[rid]
                 finish_obj = FinishReq(
                     rid=rid,
@@ -561,7 +562,6 @@ class RequestHandlerWorkerPool:
                 yield out
                 break
 
-            state.event.clear()
             yield out
 
     async def _wait_for_cache_prefill_response(
@@ -573,8 +573,11 @@ class RequestHandlerWorkerPool:
     ):
         while True:
             try:
-                await asyncio.wait_for(state.event.wait(), timeout=4)
-                break
+                _, finished = await asyncio.wait_for(
+                    state.response_queue.get(), timeout=4
+                )
+                if finished:
+                    break
             except asyncio.TimeoutError:
                 if request is not None and await request.is_disconnected():
                     for rid in obj.rid:
@@ -582,7 +585,6 @@ class RequestHandlerWorkerPool:
                     raise ValueError(f"Abort request {rid}")
                 continue
 
-        assert state.finished
         del self.rid_to_state[rid]
 
     def flush_cache(self, req: FlushCacheReq):
@@ -635,18 +637,30 @@ class RequestHandlerWorkerPool:
     async def _send_req_and_wait_for_response(
         self, req: Union[ActivateReqInput, DeactivateReqInput]
     ):
-        # send request to the corresponding gpu scheduler
-        self._send_req_to_gpu_scheduler(req)
-
-        # await for the response
+        # V5_2 (b): 82% of a deactivation sits between this send and the
+        # engine's own teardown.  Stamp the hops so it can be attributed.
+        import json as _json, time as _time
+        _v5_t0 = _time.time()
+        # Register the waiter before sending so a fast response cannot be lost.
         rid = req.rid
         event = asyncio.Event()
         state = ReqState([], False, event)
         self.rid_to_state[rid] = state
+        self._send_req_to_gpu_scheduler(req)
+        _v5_sent = _time.time()
 
         # wait for the response
         try:
             await state.event.wait()
+            _v5_done = _time.time()
+            logger.info("[V5-HOP] " + _json.dumps({
+                "action": type(req).__name__,
+                "model": getattr(req, "model_name", None),
+                "gpu_id": getattr(req, "gpu_id", None),
+                "register_s": _v5_sent - _v5_t0,
+                "wait_for_engine_s": _v5_done - _v5_sent,
+                "total_s": _v5_done - _v5_t0,
+            }))
             success = state.finished
             out = state.out_list[-1]
         except:
@@ -661,11 +675,15 @@ class RequestHandlerWorkerPool:
 
     async def deactivate(self, req: DeactivateReqInput):
         logger.info(f"Sending deactivate request to GPU scheduler {req.gpu_id}")
+        if getattr(self.server_args, "overlap_migration", False):
+            return await self._send_req_and_wait_for_response(req)
         self._send_req_to_gpu_scheduler(req)
         return True, None
 
     async def activate(self, req: ActivateReqInput):
         logger.info(f"Sending activate request to GPU scheduler {req.gpu_id}")
+        if getattr(self.server_args, "overlap_migration", False):
+            return await self._send_req_and_wait_for_response(req)
         self._send_req_to_gpu_scheduler(req)
         return True, None
 
@@ -772,9 +790,13 @@ class RequestHandlerWorkerPool:
                         "embedding": recv_obj.embeddings[i],
                         "meta_info": recv_obj.meta_info[i],
                     }
-                state.out_list.append(out_dict)
-                state.finished = recv_obj.finished_reason[i] is not None
-                state.event.set()
+                finished = recv_obj.finished_reason[i] is not None
+                if state.response_queue is not None:
+                    state.response_queue.put_nowait((out_dict, finished))
+                else:
+                    state.out_list.append(out_dict)
+                    state.finished = finished
+                    state.event.set()
 
     def convert_logprob_style(
         self,
diff --git a/python/sglang/multi_model/scheduling/action.py b/python/sglang/multi_model/scheduling/action.py
index 394d2620a4f0b8f708e8516ca5f85032fea5c225..f07de26d25c07a7f438f2c7a0c365a5c15d93955 100644
--- a/python/sglang/multi_model/scheduling/action.py
+++ b/python/sglang/multi_model/scheduling/action.py
@@ -1,5 +1,7 @@
 import dataclasses
+import json
 import logging
+import time
 from abc import ABC, abstractmethod
 from typing import Dict, List, Optional
 
@@ -24,7 +26,8 @@ class BaseAction(ABC):
 
     @abstractmethod
     def execute(
-        self, url: str, model_instance_state_dict: Dict[str, List[ModelInstanceState]]
+        self, url: str, model_instance_state_dict: Dict[str, List[ModelInstanceState]],
+        request_timeout: Optional[float] = None,
     ):
         pass
 
@@ -37,19 +40,26 @@ class BaseAction(ABC):
 class ActivateAction(BaseAction):
     memory_pool_size: Optional[int] = None
     gpu_id: Optional[int] = None
+    phase: str = "full"  # PAPER-FAITHFUL-V6
 
     def execute(
-        self, url: str, model_instance_state_dict: Dict[str, List[ModelInstanceState]]
+        self, url: str, model_instance_state_dict: Dict[str, List[ModelInstanceState]],
+        request_timeout: Optional[float] = None,
     ):
         try:
             logger.info(
                 f"Sending activate request to {self.model_name} (instance {self.instance_idx})"
             )
             response = requests.post(
-                f"{url}/activate", json=self.to_dict(), timeout=REQUEST_TIMEOUT
+                f"{url}/activate", json=self.to_dict(), timeout=request_timeout or REQUEST_TIMEOUT
             )
             if response.status_code == 200:
-                if response.json()["memory_usage"] is None:
+                payload = response.json()
+                # Preserve the legacy full-activation semantics, but a
+                # failed prepare/commit must fence the following phase.
+                if self.phase != "full" and not payload.get("success", True):
+                    return False
+                if payload["memory_usage"] is None:
                     memory_usage = MemoryUsage(
                         total_used_memory=0.0,
                         model_weights_memory=0.0,
@@ -61,9 +71,28 @@ class ActivateAction(BaseAction):
                     memory_usage = MemoryUsage.from_dict(
                         response.json()["memory_usage"]
                     )
-                model_instance_state_dict[self.model_name][
+                instance = model_instance_state_dict[self.model_name][
                     self.instance_idx
-                ].on_activate(memory_usage, gpu_id=self.gpu_id)
+                ]
+                if self.phase == "prepare":
+                    # Prepared targets own memory but remain invisible to
+                    # placement/routing until the commit response.
+                    instance.state = ModelState.ACTIVATING
+                    instance.memory_usage = memory_usage
+                    if self.gpu_id is not None:
+                        instance.gpu_ids = [self.gpu_id]
+                else:
+                    routing_start = time.time()
+                    instance.on_activate(memory_usage, gpu_id=self.gpu_id)
+                    routing_end = time.time()
+                    if self.phase == "commit":
+                        logger.info("[PAPER-MIGRATION-TIMELINE] " + json.dumps({
+                            "event": "routing_switch",
+                            "model": self.model_name,
+                            "gpu_id": self.gpu_id,
+                            "start": routing_start,
+                            "end": routing_end,
+                        }))
                 success = True
             else:
                 success = False
@@ -83,17 +112,21 @@ class DeactivateAction(BaseAction):
     evict_waiting_requests: bool = False
 
     def execute(
-        self, url: str, model_instance_state_dict: Dict[str, List[ModelInstanceState]]
+        self, url: str, model_instance_state_dict: Dict[str, List[ModelInstanceState]],
+        request_timeout: Optional[float] = None,
     ):
         try:
             logger.info(
                 f"Sending deactivate request to {self.model_name} (instance {self.instance_idx})"
             )
             response = requests.post(
-                f"{url}/deactivate", json=self.to_dict(), timeout=REQUEST_TIMEOUT
+                f"{url}/deactivate", json=self.to_dict(), timeout=request_timeout or REQUEST_TIMEOUT
             )
             if response.status_code == 200:
-                if response.json()["memory_usage"] is None:
+                payload = response.json()
+                if self.preempt and not payload.get("success", True):
+                    return False
+                if payload["memory_usage"] is None:
                     memory_usage = MemoryUsage(
                         total_used_memory=0.0,
                         model_weights_memory=0.0,
@@ -125,14 +158,15 @@ class ResizeAction(BaseAction):
     memory_pool_size: Optional[int] = None
 
     def execute(
-        self, url: str, model_instance_state_dict: Dict[str, List[ModelInstanceState]]
+        self, url: str, model_instance_state_dict: Dict[str, List[ModelInstanceState]],
+        request_timeout: Optional[float] = None,
     ):
         try:
             logger.info(
                 f"Sending resize request to {self.model_name} (instance {self.instance_idx})"
             )
             response = requests.post(
-                f"{url}/resize_mem_pool", json=self.to_dict(), timeout=REQUEST_TIMEOUT
+                f"{url}/resize_mem_pool", json=self.to_dict(), timeout=request_timeout or REQUEST_TIMEOUT
             )
             if response.status_code == 200:
                 success = True
diff --git a/python/sglang/multi_model/scheduling/controller_global.py b/python/sglang/multi_model/scheduling/controller_global.py
index 9f0dc7c083c53f79b8d9805ced286e82b8ad721b..45e5be0b9c53db6ac9469f8644e8a81e3b8fc9b6 100644
--- a/python/sglang/multi_model/scheduling/controller_global.py
+++ b/python/sglang/multi_model/scheduling/controller_global.py
@@ -16,8 +16,20 @@ import torch
 import zmq
 
 from sglang.multi_model.scheduling.policy.simple_global import SimpleGlobalPolicy
+from sglang.multi_model.scheduling.policy.kvpr_global_v3 import KVPRGlobalPolicyV3
+from sglang.multi_model.scheduling.policy.kvpr_global_v4 import KVPRGlobalPolicyV4
+from sglang.multi_model.scheduling.policy.kvpr_global_tp import KVPRGlobalPolicyTP
+from sglang.multi_model.tp_slots import (  # PAPER-FAITHFUL-TP
+    plan_for_server_args as tp_slot_plan_for,
+)
+# PAPER-FAITHFUL
+from sglang.multi_model.scheduling.policy.kvpr_global import KVPRGlobalPolicy
 from sglang.multi_model.multi_model_server_args import MultiModelServerArgs
-from sglang.multi_model.scheduling.action import BaseAction
+from sglang.multi_model.scheduling.action import (
+    ActivateAction,
+    BaseAction,
+    DeactivateAction,
+)
 from sglang.multi_model.scheduling.constants import (
     AIOHTTP_TIMEOUT_SECONDS,
     UPDATE_QUEUE_INTERVAL,
@@ -86,8 +98,14 @@ class GlobalController:
             models: List[ModelInstanceState] = sum(
                 self.model_instance_state_dict.values(), []
             )
-            # NOTE(ke): For TP case, only consider rank0 state
-            gpu_ids = set([mod.gpu_ids[0] for mod in models])
+            # PAPER-FAITHFUL-TP: a TP group is a multi-GPU object.  The
+            # upstream note kept only rank0, which makes every GPU past
+            # rank0 invisible to placement -- and an anti-affinity
+            # constraint over GPUs nobody can see cannot be expressed.
+            # Identical to the old line when every model is tp_size=1.
+            gpu_ids = set()
+            for mod in models:
+                gpu_ids.update(mod.gpu_ids)
             self.gpu_ids = list(gpu_ids)
         else:
             self.gpu_ids = list(range(self.server_args.num_gpus))
@@ -142,9 +160,82 @@ class GlobalController:
                 model_weights_info=self.model_weights_info_after_renamed,
                 workers_per_gpu=self.server_args.workers_per_gpu,
             )
+        elif self.server_args.policy == "kvpr-global":
+            # PAPER-FAITHFUL: paper Algorithm 1.
+            import json as _json, os as _os
+            _tpot = {}
+            _f = getattr(self.server_args, "slo_base_file", None)
+            if _f and _os.path.exists(_f):
+                _raw = _json.load(open(_f))
+                _scale = float(getattr(self.server_args, "kvpr_tpot_slo_scale", 1.0) or 1.0)
+                _p2n = {v: k for k, v in self.model_names_to_model_paths.items()}
+                for _k, _v in _raw.items():
+                    if isinstance(_v, dict):
+                        _v = _v.get("tpot") or _v.get("tpot_slo") or _v.get("tpot_p95_ms")
+                    if _v is None:
+                        continue
+                    _v = float(_v)
+                    _v = _v / 1000.0 if _v > 1.0 else _v          # ms -> s
+                    _name = _k if _k in self.model_names_to_model_paths else _p2n.get(_k)
+                    if _name:
+                        _tpot[_name] = _v * _scale
+            logger.info(f"[PAPER-ALG1] tpot_slo_s={_tpot}")
+            self.policy = KVPRGlobalPolicy(
+                num_gpus=len(self.gpu_ids),
+                gpu_mem=gpu_mem,
+                model_weights_info=self.model_weights_info_after_renamed,
+                workers_per_gpu=self.server_args.workers_per_gpu,
+                tau=float(getattr(self.server_args, "kvpr_tau", 0.35)),
+                rate_window=float(getattr(self.server_args, "kvpr_rate_window", 30.0)),
+                migration_cooldown=float(getattr(self.server_args, "kvpr_migration_cooldown", 30.0)),
+                tpot_slo_s=_tpot,
+            )
+        elif self.server_args.policy in ("kvpr-global-v3", "kvpr-global-v4",
+                                        "kvpr-global-tp"):
+            # PAPER-FAITHFUL-V3: literal Algorithm 1 line-8 absolute tau.
+            import json as _json, os as _os
+            _tpot = {}
+            _f = getattr(self.server_args, "slo_base_file", None)
+            if _f and _os.path.exists(_f):
+                _raw = _json.load(open(_f))
+                _scale = float(getattr(self.server_args, "kvpr_tpot_slo_scale", 1.0) or 1.0)
+                _p2n = {v: k for k, v in self.model_names_to_model_paths.items()}
+                for _k, _v in _raw.items():
+                    if isinstance(_v, dict):
+                        _v = _v.get("tpot") or _v.get("tpot_slo") or _v.get("tpot_p95_ms")
+                    if _v is None:
+                        continue
+                    _v = float(_v)
+                    _v = _v / 1000.0 if _v > 1.0 else _v
+                    _name = _k if _k in self.model_names_to_model_paths else _p2n.get(_k)
+                    if _name:
+                        _tpot[_name] = _v * _scale
+            _cls = (KVPRGlobalPolicyTP
+                    if self.server_args.policy == "kvpr-global-tp"
+                    else KVPRGlobalPolicyV4
+                    if self.server_args.policy == "kvpr-global-v4"
+                    else KVPRGlobalPolicyV3)
+            # Only the TP policy needs the server args (for tp_size per
+            # model and the anti-affinity flag); the others keep the
+            # keyword-only signature they already had.
+            _extra = ({"server_args": self.server_args}
+                      if self.server_args.policy == "kvpr-global-tp" else {})
+            self.policy = _cls(
+                num_gpus=len(self.gpu_ids), gpu_mem=gpu_mem,
+                model_weights_info=self.model_weights_info_after_renamed,
+                workers_per_gpu=self.server_args.workers_per_gpu,
+                tau=float(getattr(self.server_args, "kvpr_tau", 0.35)),
+                rate_window=float(getattr(self.server_args, "kvpr_rate_window", 30.0)),
+                migration_cooldown=float(getattr(self.server_args, "kvpr_migration_cooldown", 30.0)),
+                tpot_slo_s=_tpot,
+                **_extra,  # PAPER-FAITHFUL-TP
+            )
         else:
             raise ValueError(f"Unknown policy: {self.server_args.policy}")
 
+        self.policy._target_first_actions = bool(
+            getattr(self.server_args, "overlap_migration", False)
+        )
         logger.info(f"Using policy: {self.policy.__class__.__name__}")
 
         self._start_run_policy = (
@@ -302,24 +393,141 @@ class GlobalController:
                 time.sleep(5)
 
     def execute_actions(self, actions: List[BaseAction]):
-        """Execute a list of scheduling actions in parallel."""
+        """Execute actions, with a readiness barrier for overlap migration."""
         tic = time.time()
-        threads = ThreadPoolExecutor(max_workers=len(actions))
-        futures = [
-            threads.submit(
-                action.execute, self.server_args.url(), self.model_instance_state_dict
-            )
-            for action in actions
-        ]
-
-        # Wait for all actions to complete
-        for future in futures:
-            try:
-                future.result()
-            except Exception as e:
-                logger.error(f"Error executing action: {get_exception_traceback()}")
+        overlap_migration = getattr(self.server_args, "overlap_migration", False)
+        kv_migration = getattr(self.server_args, "enable_kv_migration", False)
+        from sglang.multi_model.scheduling.action_order_v6 import (
+            build_action_batches,
+        )
+        batches = build_action_batches(
+            actions, overlap_migration, kv_migration,
+            ActivateAction, DeactivateAction,
+        )
 
-        threads.shutdown()
+        failed_activation_models = set()
+        failed_deactivation_models = set()
+        deactivating_models = {
+            a.model_name for a in actions if isinstance(a, DeactivateAction)
+        }
+        migration_targets = {
+            a.model_name: a for a in actions
+            if isinstance(a, ActivateAction)
+            and a.model_name in deactivating_models
+        }
+        for batch, max_workers in batches:
+            if overlap_migration and isinstance(batch[0], DeactivateAction):
+                safe_batch = []
+                for action in batch:
+                    if action.model_name not in failed_activation_models:
+                        safe_batch.append(action)
+                        continue
+                    active_copies = sum(
+                        instance.state == ModelState.ACTIVE
+                        for instance in self.model_instance_state_dict[action.model_name]
+                    )
+                    if active_copies > 1:
+                        safe_batch.append(action)
+                    else:
+                        logger.error(
+                            "Skipping source deactivation for %s after target "
+                            "activation failed",
+                            action.model_name,
+                        )
+                batch = safe_batch
+                if not batch:
+                    continue
+
+            if (overlap_migration and isinstance(batch[0], ActivateAction)
+                    and getattr(batch[0], "phase", "full") == "commit"):
+                batch = [a for a in batch
+                         if a.model_name not in failed_activation_models]
+                if not batch:
+                    continue
+
+            with ThreadPoolExecutor(max_workers=max_workers) as threads:
+                def _timed_execute(action):
+                    _t0 = time.time()
+                    _tracker = self.model_queues.get(action.model_name)
+                    _finish_before = (
+                        _tracker.get_last_finished_time()
+                        if _tracker is not None else float("-inf")
+                    )
+                    _ok = False
+                    try:
+                        _result = action.execute(
+                            self.server_args.url(),
+                            self.model_instance_state_dict,
+                            600.0 if overlap_migration else None,
+                        )
+                        _ok = _result is not False
+                        return _result
+                    finally:
+                        _t1 = time.time()
+                        _finish_after = (
+                            _tracker.get_last_finished_time()
+                            if _tracker is not None else float("-inf")
+                        )
+                        _phase = getattr(action, "phase", "full")
+                        logger.info("[PAPER-ACTION-V4] " + json.dumps({
+                            "action": type(action).__name__,
+                            "model": getattr(action, "model_name", None),
+                            "gpu_id": getattr(action, "gpu_id", None),
+                            "instance_idx": getattr(action, "instance_idx", None),
+                            "phase": _phase,
+                            "start": _t0, "end": _t1, "duration_s": _t1 - _t0,
+                            "last_finish_before": _finish_before,
+                            "last_finish_after": _finish_after,
+                            "source_completed_during_prepare": (
+                                _phase == "prepare" and _finish_after >= _t0),
+                            "success": _ok,
+                        }, default=str))
+
+                future_to_action = {
+                    threads.submit(_timed_execute, action): action
+                    for action in batch
+                }
+                for future, action in future_to_action.items():
+                    try:
+                        result = future.result()
+                        if result is False:
+                            if isinstance(action, ActivateAction):
+                                failed_activation_models.add(action.model_name)
+                            elif isinstance(action, DeactivateAction):
+                                failed_deactivation_models.add(action.model_name)
+                    except Exception:
+                        if isinstance(action, ActivateAction):
+                            failed_activation_models.add(action.model_name)
+                        elif isinstance(action, DeactivateAction):
+                            failed_deactivation_models.add(action.model_name)
+                        logger.error(f"Error executing action: {get_exception_traceback()}")
+
+            if (kv_migration and isinstance(batch[0], DeactivateAction)
+                    and failed_deactivation_models):
+                # A prepared target must not leak if source quiesce fails.
+                # Roll it back before fencing the matching commit.
+                for model_name in sorted(failed_deactivation_models):
+                    target = migration_targets.get(model_name)
+                    if target is None:
+                        continue
+                    failed_activation_models.add(model_name)
+                    rollback = DeactivateAction(
+                        model_name=model_name,
+                        instance_idx=target.instance_idx,
+                        gpu_id=target.gpu_id,
+                        preempt=False, evict_waiting_requests=False,
+                    )
+                    try:
+                        rollback.execute(
+                            self.server_args.url(),
+                            self.model_instance_state_dict, 600.0)
+                        logger.warning(
+                            "[PAPER-OVERLAP-V6] rolled back prepared target "
+                            f"for {model_name} after source deactivation failure")
+                    except Exception:
+                        logger.error(
+                            "[PAPER-OVERLAP-V6] target rollback failed: "
+                            + get_exception_traceback())
         toc = time.time()
         logger.info(
             f"Executed {len(actions)} actions in {toc - tic:.2f} seconds. Actions: {actions}"
diff --git a/python/sglang/multi_model/scheduling/gpu/gpu_scheduler.py b/python/sglang/multi_model/scheduling/gpu/gpu_scheduler.py
index 681fc35d1609b286a03a21274c57483ffd101b3b..7f8861e2f0b019022bbbffa76acba7d97de445dd 100644
--- a/python/sglang/multi_model/scheduling/gpu/gpu_scheduler.py
+++ b/python/sglang/multi_model/scheduling/gpu/gpu_scheduler.py
@@ -1,10 +1,11 @@
 import atexit
+from concurrent.futures import ThreadPoolExecutor
 import logging
 import os
 import signal
 import time
 from dataclasses import dataclass
-from threading import Event, Thread
+from threading import Event, Lock, Thread
 from typing import Dict, List, Optional, Union
 
 import multiprocessing as mp
@@ -15,13 +16,19 @@ from sglang.multi_model.multi_model_server_args import MultiModelServerArgs
 from sglang.multi_model.scheduling.gpu.request_queue import RequestQueue
 from sglang.multi_model.scheduling.gpu.resource_manager import ResourceManager
 from sglang.multi_model.scheduling.gpu.worker_pool import WorkerPool
+from sglang.multi_model.tp_slots import (  # PAPER-FAITHFUL-TP
+    plan_for_server_args as tp_slot_plan_for,
+)
 from sglang.multi_model.utils.get_memory_pool_size import get_model_path_to_cell_size
 from sglang.srt.managers.io_struct import (
     ActivateReqInput,
     ActivateReqOutput,
+    BackendAdmitReq,
+    BatchRunReq,
     DeactivateReqInput,
     DeactivateReqOutput,
     GenerateReqInput,
+    PrefillCompleteReq,
 )
 from sglang.srt.redis_utils import RedisClient
 from sglang.srt.utils import configure_logger, kill_parent_process
@@ -58,8 +65,42 @@ class GPUScheduler:
         self._init_model_name_to_paths(model_names_to_model_paths)
         model_name_to_cell_size = self._get_model_name_to_cell_size()
         self.queue = RequestQueue(model_name_to_cell_size)
+        # PAPER-FAITHFUL: paper Algorithm 2 configuration.
+        if getattr(multi_model_server_args, "enable_moore_hodgson", False):
+            import json as _json, os as _os
+            _speed = {}
+            _f = getattr(multi_model_server_args, "prefill_speed_file", None)
+            if _f and _os.path.exists(_f):
+                _raw = _json.load(open(_f))
+                _raw = _raw.get("model_prefill_speed", _raw)
+                _p2n = {v: k for k, v in model_names_to_model_paths.items()}
+                for _k, _v in _raw.items():
+                    _v = _v.get("c_i") if isinstance(_v, dict) else _v
+                    if _k in model_names_to_model_paths:
+                        _speed[_k] = float(_v)
+                    elif _k in _p2n:
+                        _speed[_p2n[_k]] = float(_v)
+            _log = getattr(multi_model_server_args, "log_file", None)
+            _log = f"{_log}.alg2_gpu{gpu_id}.jsonl" if _log else None
+            self.queue.configure_moore_hodgson(True, _speed, _log)
 
         self.gpu_id = gpu_id
+        self._mh_runtime_gate = bool(
+            getattr(multi_model_server_args, "enable_moore_hodgson", False)
+        )
+        self._mh_gate_lock = Lock()
+        # All pipelined requests remain here from dispatch through final prefill
+        # completion, including Redis-queued and engine-staged work.
+        self._mh_outstanding_prefills = {}
+        self._mh_dispatch_seq = 0
+        self._mh_next_backend_admit_seq = 1
+        self._mh_next_prefill_start_seq = 1
+        self._mh_admission_seq_key = (
+            f"{multi_model_server_args.engine_to_gpu_scheduler_key_prefix}:"
+            f"alg2_next_admission:{gpu_id}"
+        )
+        if self._mh_runtime_gate:
+            self.redis_client.set_int(self._mh_admission_seq_key, 1)
 
         # Create ZMQ context
         num_io_threads = (
@@ -88,6 +129,8 @@ class GPUScheduler:
             multi_model_server_args.enable_worker_pool,
             model_names_to_model_paths,
             num_workers=multi_model_server_args.workers_per_gpu,
+            slot_plan=getattr(self, "_tp_slot_plan", None),        # PAPER-FAITHFUL-TP
+            model_tp_sizes=getattr(self, "_tp_model_sizes", None),  # PAPER-FAITHFUL-TP
         )
         self._shutdown_event = Event()
         self._receiver_thread = None
@@ -96,14 +139,16 @@ class GPUScheduler:
 
     def _init_models(self, init_model_names: List[str]):
         """Initialize specified models."""
-        for model_name in init_model_names:
+        def _activate(model_name):
             self._set_model_state(model_name, "activating")
-            activate_req = ActivateReqInput(
-                model_name=model_name,
-                instance_idx=self.gpu_id,
-                gpu_id=self.gpu_id,
-            )
-            self.worker_pool.handle_activate_model(activate_req)
+            return self.worker_pool.handle_activate_model(ActivateReqInput(
+                model_name=model_name, instance_idx=self.gpu_id, gpu_id=self.gpu_id))
+        if getattr(self.server_args, "parallel_model_loading", False) and len(init_model_names) > 1:
+            with ThreadPoolExecutor(max_workers=len(init_model_names)) as pool:
+                list(pool.map(_activate, init_model_names))
+        else:
+            for model_name in init_model_names:
+                _activate(model_name)
 
         while True:
             self._recv_from_engine()
@@ -119,8 +164,17 @@ class GPUScheduler:
     def _maybe_init_worker_pool(self):
         """Initialize worker pool if enabled."""
         if self.server_args.enable_worker_pool:
+            self._tp_slot_plan = tp_slot_plan_for(self.server_args)
+            self._tp_model_sizes = {
+                mc.model_name: int(getattr(mc, "tp_size", 1) or 1)
+                for mc in (getattr(self.server_args, "model_configs", None) or [])
+            }
             self.worker_pool = WorkerPool(
-                self.server_args.workers_per_gpu, self.gpu_id, self.context
+                self.server_args.workers_per_gpu,
+                self.gpu_id,
+                self.context,
+                slot_plan=self._tp_slot_plan,
+                model_tp_sizes=self._tp_model_sizes,
             )
         else:
             self.worker_pool = None
@@ -136,11 +190,20 @@ class GPUScheduler:
         self._receiver_thread = Thread(target=self._recv_requests_loop, daemon=True)
         self._receiver_thread.start()
 
+        # V5_2 (a): time the loop, since Algorithm 2 costs goodput while
+        # deferring nothing -- the loop period is the other thing that
+        # differs between the arms.
+        import json as _json
+        _v5 = {"iters": 0, "t_mem": 0.0, "t_redis": 0.0, "t_admit": 0.0,
+               "t_send": 0.0, "t_iter": 0.0, "admitted": 0, "iter_max": 0.0}
+        _v5_last = time.time()
         try:
             while not self._shutdown_event.is_set():
+                _t0 = time.perf_counter()
                 available_kv_cache_memory = (
                     self.resource_manager.get_available_kv_cache_memory()
                 )
+                _t1 = time.perf_counter()
 
                 # Get backend queue lengths for each active model
                 active_models = self._get_active_or_activating_model_names()
@@ -150,18 +213,93 @@ class GPUScheduler:
                     )
                     for model_name in active_models
                 }
+                _t2 = time.perf_counter()
+
+                # Pop requests with admission control.  In Algorithm-2 mode,
+                # preserve the selector's global order through actual engine
+                # admission.  A bounded window may be dispatched without
+                # waiting for per-request start ACKs; the shared backend token
+                # orders actual admission.  Every staged or started prefill
+                # remains visible as outstanding p_i/c_i work until completion.
+                if self._mh_runtime_gate:
+                    # launch_request_handler flushes Redis after GPU schedulers
+                    # start. Recreate the sequence key exactly once if that
+                    # startup flush removed it.
+                    self.redis_client.set_int_if_absent(
+                        self._mh_admission_seq_key, 1
+                    )
+                    with self._mh_gate_lock:
+                        outstanding_work_s = sum(
+                            item["predicted_exec_s"]
+                            for item in self._mh_outstanding_prefills.values()
+                        )
+                        outstanding_count = len(self._mh_outstanding_prefills)
+                    # One staged slot per independent active backend consumer.
+                    # This is a structural bound, not a policy heuristic.
+                    pipeline_window = max(1, len(active_models))
+                    dispatch_budget = max(0, pipeline_window - outstanding_count)
+                    if dispatch_budget == 0:
+                        reqs_can_be_admitted = []
+                    else:
+                        reqs_can_be_admitted = self.queue.admission_control(
+                            available_resources=available_kv_cache_memory,
+                            model_backend_queue_lens=model_backend_queue_lens,
+                            model_states=self._model_states,
+                            allow_sending_when_activating=True,
+                            mh_outstanding_work_s=outstanding_work_s,
+                            mh_dispatch_budget=dispatch_budget,
+                        )
+                        if reqs_can_be_admitted:
+                            with self._mh_gate_lock:
+                                for req in reqs_can_be_admitted:
+                                    self._mh_dispatch_seq += 1
+                                    req.alg2_seq = self._mh_dispatch_seq
+                                    record = {
+                                        "seq": req.alg2_seq,
+                                        "rid": req.rid,
+                                        "model": req.model,
+                                        "dispatch_time": time.time(),
+                                        "backend_admit_time": None,
+                                        "start_time": None,
+                                        "predicted_exec_s": self.queue.mh_exec_time(req),
+                                        "backend_admitted": False,
+                                        "started": False,
+                                    }
+                                    self._mh_outstanding_prefills[req.rid] = record
+                else:
+                    reqs_can_be_admitted = self.queue.admission_control(
+                        available_resources=available_kv_cache_memory,
+                        model_backend_queue_lens=model_backend_queue_lens,
+                        model_states=self._model_states,
+                        allow_sending_when_activating=True,
+                    )
 
-                # Pop requests from priority queue with admission control
-                reqs_can_be_admitted = self.queue.admission_control(
-                    available_resources=available_kv_cache_memory,
-                    model_backend_queue_lens=model_backend_queue_lens,
-                    model_states=self._model_states,
-                    allow_sending_when_activating=True,
-                )
-
-                for model_name, reqs in reqs_can_be_admitted.items():
-                    logger.info(f"Admitted {len(reqs)} requests for {model_name}")
+                _t3 = time.perf_counter()
+                if self._mh_runtime_gate:
+                    for req in reqs_can_be_admitted:
+                        logger.info(f"Admitted 1 request for {req.model}")
+                else:
+                    for model_name, reqs in reqs_can_be_admitted.items():
+                        logger.info(f"Admitted {len(reqs)} requests for {model_name}")
                 self._send_to_backend_queue(reqs_can_be_admitted)
+                _t4 = time.perf_counter()
+                _v5["iters"] += 1
+                _v5["t_mem"] += _t1 - _t0
+                _v5["t_redis"] += _t2 - _t1
+                _v5["t_admit"] += _t3 - _t2
+                _v5["t_send"] += _t4 - _t3
+                _v5["t_iter"] += _t4 - _t0
+                _v5["iter_max"] = max(_v5["iter_max"], _t4 - _t0)
+                _v5["admitted"] += (
+                    len(reqs_can_be_admitted)
+                    if self._mh_runtime_gate
+                    else sum(len(r) for r in reqs_can_be_admitted.values())
+                )
+                if time.time() - _v5_last >= 5.0:
+                    logger.info("[V5-LOOP] " + _json.dumps(
+                        dict(_v5, gpu_id=self.gpu_id, window_s=time.time() - _v5_last)))
+                    _v5 = {k: (0.0 if isinstance(v, float) else 0) for k, v in _v5.items()}
+                    _v5_last = time.time()
 
                 time.sleep(0.01)
         except Exception as e:
@@ -203,6 +341,9 @@ class GPUScheduler:
             reqs = self.redis_client.pop_all(
                 key=f"{self.server_args.frontend_generate_request_key_prefix}:{model_name}",
             )
+            queue_time = time.time()
+            for req in reqs:
+                req.gpu_scheduler_queue_time = queue_time
             recv_reqs.extend(reqs)
         return recv_reqs
 
@@ -222,7 +363,11 @@ class GPUScheduler:
                     f"{'succeeded' if success else 'failed'} on GPU {self.gpu_id}"
                 )
                 if success:
-                    self._set_model_state(model_name, "activated")
+                    phase = getattr(message, "phase", "full")
+                    self._set_model_state(
+                        model_name,
+                        "prepared" if phase == "prepare" else "activated",
+                    )
                 # Can record additional activation completion status here, such as updating model state dictionaries
 
             elif isinstance(message, DeactivateReqOutput):
@@ -236,11 +381,119 @@ class GPUScheduler:
                 if success:
                     self._set_model_state(model_name, "deactivated")
                 # Can record additional deactivation completion status here, such as cleaning up resources
+            elif isinstance(message, BatchRunReq) and self._mh_runtime_gate:
+                self._handle_mh_prefill_start(message)
+            elif isinstance(message, BackendAdmitReq) and self._mh_runtime_gate:
+                self._handle_mh_backend_admit(message)
+            elif isinstance(message, PrefillCompleteReq) and self._mh_runtime_gate:
+                self._handle_mh_prefill_complete(message)
             else:
                 logger.info(f"Received unsupported message type: {type(message)}")
 
         return messages
 
+    def _mh_runtime_log(self, event, message, record, order_ok=True):
+        import json
+
+        logger.info("[PAPER-ALG2-RUNTIME] " + json.dumps({
+            "event": event,
+            "gpu_id": self.gpu_id,
+            "alg2_seq": record["seq"] if record else None,
+            "expected_rid": record["rid"] if record else None,
+            "expected_model": record["model"] if record else None,
+            "actual_rids": list(message.rids),
+            "actual_alg2_seqs": list(getattr(message, "alg2_seqs", []) or []),
+            "actual_model": message.model,
+            "event_time": getattr(message, "admit_time", None)
+            or getattr(message, "run_time", None)
+            or getattr(message, "complete_time", None),
+            "order_ok": bool(order_ok),
+        }, sort_keys=True))
+
+    def _handle_mh_backend_admit(self, message):
+        with self._mh_gate_lock:
+            if len(message.rids) != 1 or len(message.alg2_seqs) != 1:
+                records = []
+            else:
+                records = [self._mh_outstanding_prefills.get(message.rids[0])]
+            record = records[0] if records else None
+            order_ok = bool(
+                record
+                and not record["backend_admitted"]
+                and record["model"] == message.model
+                and record["seq"] == message.alg2_seqs[0]
+                and record["seq"] == self._mh_next_backend_admit_seq
+            )
+            self._mh_runtime_log("backend_admit", message, record, order_ok)
+            if not order_ok:
+                self._shutdown_event.set()
+                raise RuntimeError(
+                    "Algorithm-2 backend admission order violation: "
+                    f"expected_seq={self._mh_next_backend_admit_seq}, "
+                    f"record={record}, actual={message}"
+                )
+            record["backend_admitted"] = True
+            record["backend_admit_time"] = message.admit_time
+            self._mh_next_backend_admit_seq += 1
+
+    def _handle_mh_prefill_start(self, message):
+        with self._mh_gate_lock:
+            records = [self._mh_outstanding_prefills.get(rid) for rid in message.rids]
+            newly_started = [rec for rec in records if rec and not rec["started"]]
+            order_ok = bool(
+                all(rec is None or rec["model"] == message.model for rec in records)
+                and (
+                    not newly_started
+                    or (
+                        len(newly_started) == 1
+                        and newly_started[0]["backend_admitted"]
+                        and newly_started[0]["seq"]
+                        == self._mh_next_prefill_start_seq
+                    )
+                )
+            )
+            record = newly_started[0] if newly_started else next(
+                (rec for rec in records if rec), None
+            )
+            event = "prefill_start" if newly_started else "prefill_chunk_start"
+            self._mh_runtime_log(event, message, record, order_ok)
+            if not order_ok:
+                self._shutdown_event.set()
+                raise RuntimeError(
+                    "Algorithm-2 runtime prefill order violation: "
+                    f"expected_seq={self._mh_next_prefill_start_seq}, "
+                    f"records={records}, actual={message}"
+                )
+            if newly_started:
+                record["started"] = True
+                record["start_time"] = getattr(message, "run_time", time.time())
+                self._mh_next_prefill_start_seq += 1
+
+    def _handle_mh_prefill_complete(self, message):
+        with self._mh_gate_lock:
+            records = [self._mh_outstanding_prefills.get(rid) for rid in message.rids]
+            order_ok = bool(
+                records
+                and all(
+                    rec
+                    and rec["started"]
+                    and rec["model"] == message.model
+                    for rec in records
+                )
+            )
+            self._mh_runtime_log(
+                "prefill_complete", message,
+                records[0] if records else None, order_ok,
+            )
+            if not order_ok:
+                self._shutdown_event.set()
+                raise RuntimeError(
+                    "Algorithm-2 runtime prefill completion violation: "
+                    f"outstanding={self._mh_outstanding_prefills}, actual={message}"
+                )
+            for rid in message.rids:
+                del self._mh_outstanding_prefills[rid]
+
     def _recv_from_request_handler(self):
         """Receive activate/deactivate requests from request handler."""
         try:
@@ -285,8 +538,13 @@ class GPUScheduler:
                     logger.info(
                         f"Received activate request for model {message.model_name}"
                     )
-                    # No special handling needed here, only take action when tokenizer sends activation result
-                    self._set_model_state(message.model_name, "activating")
+                    phase = getattr(message, "phase", "full")
+                    self._set_model_state(
+                        message.model_name,
+                        "preparing" if phase == "prepare"
+                        else "committing" if phase == "commit"
+                        else "activating",
+                    )
                     logger.info(
                         f"Received activate request for {message.model_name}, "
                         f"model_states: {self._model_states}"
@@ -308,7 +566,10 @@ class GPUScheduler:
         old_state = self._model_states.get(model_name, "deactivated")
         self._model_states[model_name] = new_state
 
-        states_need_resource = ("activating", "activated", "deactivating")
+        states_need_resource = (
+            "preparing", "prepared", "committing",
+            "activating", "activated", "deactivating",
+        )
         old_need = old_state in states_need_resource
         new_need = new_state in states_need_resource
 
@@ -338,14 +599,34 @@ class GPUScheduler:
                 f"Error sending preempted requests: {get_exception_traceback()}"
             )
 
-    def _send_to_backend_queue(self, reqs: Dict[str, List[GenerateReqInput]]):
+    def _send_to_backend_queue(self, reqs):
         """Send requests to backend queue."""
-        for model_name, reqs in reqs.items():
-            for req in reqs:
-                self.redis_client.send_pyobj(
-                    key=f"{self.server_args.backend_generate_request_key_prefix}:{model_name}",
-                    obj=req,
+        if self._mh_runtime_gate:
+            ordered_reqs = reqs
+        else:
+            ordered_reqs = [req for model_reqs in reqs.values() for req in model_reqs]
+        for req in ordered_reqs:
+            model_name = req.model
+            req.gpu_scheduler_dispatch_time = time.time()
+            if self._mh_runtime_gate:
+                with self._mh_gate_lock:
+                    record = dict(self._mh_outstanding_prefills[req.rid])
+                self._mh_runtime_log(
+                    "dispatch",
+                    BatchRunReq(
+                        rids=[req.rid], model=model_name,
+                        run_time=req.gpu_scheduler_dispatch_time,
+                        gpu_id=self.gpu_id,
+                    ),
+                    record,
                 )
+            self.redis_client.send_pyobj(
+                key=(
+                    f"{self.server_args.backend_generate_request_key_prefix}:"
+                    f"{model_name}"
+                ),
+                obj=req,
+            )
 
     def _init_model_states(self, engine_info_dict: Dict[str, List]):
         """
diff --git a/python/sglang/multi_model/scheduling/gpu/request_queue.py b/python/sglang/multi_model/scheduling/gpu/request_queue.py
index 511874f2632869146747d67caa9384792b6be83c..b4272b14f709f2d01b0ef11b68ffda124a959cc5 100644
--- a/python/sglang/multi_model/scheduling/gpu/request_queue.py
+++ b/python/sglang/multi_model/scheduling/gpu/request_queue.py
@@ -9,6 +9,10 @@ from sglang.srt.managers.io_struct import GenerateReqInput
 
 logger = logging.getLogger(__name__)
 
+# PAPER-FAITHFUL: paper Algorithm 2 lives in request_queue_mh.py and is inert
+# unless configure_moore_hodgson(enabled=True, ...) has been called.
+from sglang.multi_model.scheduling.gpu.request_queue_mh import MooreHodgsonMixin
+
 
 class RequestWrapper:
     """Request wrapper for encapsulating requests and calculating priorities."""
@@ -39,7 +43,7 @@ class RequestWrapper:
         return self.__str__()
 
 
-class RequestQueue:
+class RequestQueue(MooreHodgsonMixin):
     """
     Request queue manager using priority queue to manage requests for different models.
     Supports admission control and resource management.
@@ -52,6 +56,7 @@ class RequestQueue:
         self._lock = threading.Lock()
         self._model_name_to_cell_size = model_name_to_cell_size
         self.last_log_time = 0
+        self._mh_enabled = False   # PAPER-FAITHFUL
 
         # Record resources used by requests that have been admitted but are still in "activating" state
         # Used to subtract this portion from available resources in subsequent admission_control calls
@@ -118,6 +123,8 @@ class RequestQueue:
         model_backend_queue_lens: Dict[str, int],
         model_states: Dict[str, str],
         allow_sending_when_activating: bool = False,
+        mh_outstanding_work_s: float = 0.0,
+        mh_dispatch_budget: int = 1,
     ) -> Dict[str, List[GenerateReqInput]]:
         """
         Request admission control.
@@ -129,6 +136,12 @@ class RequestQueue:
 
         This ensures subsequent calls won't double-count already consumed resources.
         """
+        # PAPER-FAITHFUL: route to paper Algorithm 2 when enabled.
+        if getattr(self, "_mh_enabled", False):
+            return self.admission_control_mh(
+                available_resources, model_backend_queue_lens,
+                model_states, allow_sending_when_activating,
+                mh_outstanding_work_s, mh_dispatch_budget)
         admitted = defaultdict(list)
 
         # Calculate total resources consumed by activating state requests
diff --git a/python/sglang/multi_model/scheduling/gpu/resource_manager.py b/python/sglang/multi_model/scheduling/gpu/resource_manager.py
index 3b15fa73fdae77f8c57a31d7dca7065fc72a455d..5114b6a03acf0be41697372b131a5b9ff7dfe8b2 100644
--- a/python/sglang/multi_model/scheduling/gpu/resource_manager.py
+++ b/python/sglang/multi_model/scheduling/gpu/resource_manager.py
@@ -27,6 +27,8 @@ class ResourceManager:
         enable_worker_pool: bool,
         model_names_to_model_paths: Dict[str, str],
         num_workers: Optional[int] = None,
+        slot_plan=None,          # PAPER-FAITHFUL-TP
+        model_tp_sizes=None,     # PAPER-FAITHFUL-TP
     ):
         self.gpu_id = gpu_id
         self.mem_frac = mem_frac
@@ -34,6 +36,15 @@ class ResourceManager:
         self.enable_worker_pool = enable_worker_pool
         self.model_names_to_model_paths = model_names_to_model_paths
         self.num_workers = num_workers
+        # PAPER-FAITHFUL-TP: a GPU's slot ids are a sparse subset once TP
+        # groups exist, because a group's id has to be free on every GPU it
+        # spans and is therefore allocated globally.  range(num_workers)
+        # would name segments that do not exist on this GPU.
+        if slot_plan is None:
+            self._worker_ids = list(range(num_workers or 0))
+        else:
+            self._worker_ids = slot_plan.worker_ids_on(gpu_id)
+        self._model_tp_sizes = dict(model_tp_sizes or {})
         
         self._init_model_names_mappings(engine_info_dict)
         self._init_shared_memory_readers()
@@ -44,7 +55,7 @@ class ResourceManager:
         self.username = getpass.getuser()
         if self.enable_worker_pool:
             self._worker_to_mem_reader = {}
-            for worker_id in range(self.num_workers):
+            for worker_id in self._worker_ids:  # PAPER-FAITHFUL-TP sparse (readers)
                 self._worker_to_mem_reader[worker_id] = MemoryUsageReader(
                     f"ipc_{self.gpu_id}_{worker_id}_{self.username}"
                 )
@@ -97,7 +108,7 @@ class ResourceManager:
         """Get total used KV cache memory in bytes."""
         if self.enable_worker_pool:
             used_sum = 0
-            for worker_id in range(self.num_workers):
+            for worker_id in self._worker_ids:  # PAPER-FAITHFUL-TP sparse (usage)
                 used_sum += self._worker_to_mem_reader[
                     worker_id
                 ].get_memory_usage_in_bytes()
@@ -154,9 +165,11 @@ class ResourceManager:
             }
             model_paths = list(set(self.model_names_to_model_paths.values()))
             model_path_to_model_size = get_model_path_to_model_size(model_paths)
+            # PAPER-FAITHFUL-TP: weights are sharded, so this GPU holds
+            # model_size / tp_size, not model_size.
             self._model_names_to_weights_memory = {
                 model_name: model_path_to_model_size[
                     self.model_names_to_model_paths[model_name]
-                ]
+                ] / max(1, self._model_tp_sizes.get(model_name, 1))
                 for model_name in self.model_names_to_model_paths.keys()
             }
\ No newline at end of file
diff --git a/python/sglang/multi_model/scheduling/gpu/worker_pool.py b/python/sglang/multi_model/scheduling/gpu/worker_pool.py
index 77bccbed05860906a99e06fd106c9022d5b016ec..bab009a049030026647ddd40cab050201d4ec6a3 100644
--- a/python/sglang/multi_model/scheduling/gpu/worker_pool.py
+++ b/python/sglang/multi_model/scheduling/gpu/worker_pool.py
@@ -11,6 +11,7 @@ from sglang.srt.managers.io_struct import (
     DeactivateReqInput,
     DeactivateReqOutput,
 )
+from sglang.multi_model.tp_slots import TPSlot  # PAPER-FAITHFUL-TP
 from sglang.utils import cleanup_zmq_ipc
 
 logger = logging.getLogger(__name__)
@@ -33,7 +34,14 @@ class WorkerPool:
     Manages worker allocation, release, and model activation/deactivation operations.
     """
 
-    def __init__(self, num_workers: int, gpu_id: int, zmq_context: zmq.Context):
+    def __init__(
+        self,
+        num_workers: int,
+        gpu_id: int,
+        zmq_context: zmq.Context,
+        slot_plan=None,            # PAPER-FAITHFUL-TP
+        model_tp_sizes=None,       # PAPER-FAITHFUL-TP
+    ):
         """
         Initialize worker pool.
         
@@ -48,26 +56,66 @@ class WorkerPool:
         self._free_workers: Deque[int] = deque()
         self._worker_to_ipc_name: Dict[int, zmq.Socket] = {}
         self.zmq_context = zmq_context
+        # PAPER-FAITHFUL-TP.  Slots are typed by tp_size, because an
+        # engine's tp_size is fixed at server start while the model that
+        # occupies the slot is chosen here at runtime.
+        #
+        # A group's non-rank0 GPUs hold the same slot id as a *shadow*:
+        # reserved permanently, never assignable.  That reservation is what
+        # keeps a second engine off the group's accounting segment
+        # (ipc_{gpu}_{worker}_{user}), and because it is static there is no
+        # partially-acquired group to roll back -- activation touches only
+        # rank0's pool.
+        if slot_plan is None:
+            self._slots = {
+                i: TPSlot(worker_id=i, tp_size=1, gpu_ids=(gpu_id,))
+                for i in range(num_workers)
+            }
+        else:
+            self._slots = {s.worker_id: s for s in slot_plan.slots_on(gpu_id)}
+        self._owned_ids = sorted(
+            wid for wid, s in self._slots.items()
+            if s.role_on(gpu_id) == "owner"
+        )
+        self._shadow_ids = sorted(
+            wid for wid, s in self._slots.items()
+            if s.role_on(gpu_id) == "shadow"
+        )
+        self._model_tp_sizes = dict(model_tp_sizes or {})
         self._init_workers()
+        logger.info(
+            f"[PAPER-TP] WorkerPool gpu={gpu_id} owned={self._owned_ids} "
+            f"shadow={self._shadow_ids} "
+            + str({wid: s.tp_size for wid, s in sorted(self._slots.items())})
+        )
 
     def _init_workers(self):
-        """Initialize all workers."""
-        for i in range(self.num_workers):
+        """Initialize the slots this GPU owns.
+
+        Shadow slots get neither a free-list entry nor a socket: nothing
+        binds ``gpu_scheduler_{this gpu}_to_worker_{id}`` for them, because
+        only rank0 binds a group's control socket (scheduler.py:157-168).
+        Connecting anyway would queue activations into a socket no process
+        ever reads.
+        """
+        for i in self._owned_ids:
             worker = Worker(i, self.gpu_id)
             self._free_workers.append(worker.worker_id)
             self._worker_to_ipc_name[i] = self.zmq_context.socket(zmq.PUSH)
             self._worker_to_ipc_name[i].connect(f"ipc://{worker.ipc_name}")
 
-    def get_idle_worker(self):
-        """
-        Get an idle worker.
-        
-        Returns:
-            worker_id: Idle worker ID, or None if no idle worker available
+    def get_idle_worker(self, tp_size: int = 1):
+        """An idle slot whose tp_size matches, or None.
+
+        A TP=1 model must not take a TP=k engine and vice versa: the engine
+        already holds its rank layout and its shard of the CPU weights, so
+        the two cannot meet.
         """
-        if len(self._free_workers) == 0:
-            return None
-        return self._free_workers[0]
+        for worker_id in self._free_workers:
+            slot = self._slots.get(worker_id)
+            if slot is not None and slot.tp_size == tp_size:
+                return worker_id
+        return None
 
     def assign_worker(self, worker_id: int, model_name: str):
         """
@@ -115,15 +163,41 @@ class WorkerPool:
                 req.gpu_id == self.gpu_id
             ), f"GPU ID mismatch: {req.gpu_id} != {self.gpu_id}"
 
+        phase = getattr(req, "phase", "full")
+        if phase == "commit":
+            worker_id = self._model_to_worker.get(model_name)
+            if worker_id is None:
+                logger.error(f"Cannot commit unprepared model {model_name}")
+                return False
+            self._worker_to_ipc_name[worker_id].send_pyobj(req)
+            logger.info(f"[PAPER-OVERLAP-V6] commit dispatched "
+                        f"model={model_name} gpu={self.gpu_id} "
+                        f"worker={worker_id}")
+            return True
+
         # Check if the model is already served by a worker
         if model_name in self._model_to_worker:
             logger.info(f"Model {model_name} is already served by a worker")
             return False
 
-        worker_id = self.get_idle_worker()
+        # PAPER-FAITHFUL-TP: match the model's TP size to a slot of that type.
+        tp_size = self._model_tp_sizes.get(model_name, 1)
+        worker_id = self.get_idle_worker(tp_size)
         if worker_id is None:
-            logger.error(f"No idle worker found for GPU {self.gpu_id}")
+            logger.error(
+                f"No idle tp_size={tp_size} worker for GPU {self.gpu_id}; "
+                f"free={list(self._free_workers)} "
+                + str({w: self._slots[w].tp_size for w in self._free_workers
+                       if w in self._slots})
+            )
             return False
+        _slot = self._slots[worker_id]
+        if _slot.tp_size > 1:
+            logger.info(
+                "[PAPER-TP] activating "
+                f"{model_name} tp_size={_slot.tp_size} on gpus={list(_slot.gpu_ids)} "
+                f"slot={worker_id} (owner gpu={_slot.owner_gpu})"
+            )
             
         self.assign_worker(worker_id, model_name)
         logger.info(
diff --git a/python/sglang/multi_model/scheduling/policy/simple_global.py b/python/sglang/multi_model/scheduling/policy/simple_global.py
index 316cdf824ecabbc1925bed8daf365e62838b9125..57d455deb3065c2927d15cfa1e37252670adcc48 100644
--- a/python/sglang/multi_model/scheduling/policy/simple_global.py
+++ b/python/sglang/multi_model/scheduling/policy/simple_global.py
@@ -708,7 +708,8 @@ class SimpleGlobalPolicy(GlobalPolicy):
         gpu_available_memory: Dict[int, float],
         sorted_clusters: List[List[int]],
         model_queues: Dict[str, ModelQueueTracker],
-        model_instance_state_dict: Dict[str, List[ModelInstanceState]]
+        model_instance_state_dict: Dict[str, List[ModelInstanceState]],
+        gpu_planned_active_count: Optional[Dict[int, int]] = None,
     ) -> Optional[int]:
         """Place an inactive model with requests.
         Uses cluster-based approach to find suitable GPU.
@@ -717,6 +718,11 @@ class SimpleGlobalPolicy(GlobalPolicy):
         for cluster in sorted_clusters:
             gpu_time_budgets = []
             for gpu_id in cluster:
+                # Do not reserve more models than the worker pool can serve.
+                if (gpu_planned_active_count is not None and
+                        gpu_planned_active_count[gpu_id] >= self.workers_per_gpu):
+                    continue
+
                 # Check if the GPU has enough memory
                 if gpu_available_memory[gpu_id] < model_memory_required:
                     continue
@@ -875,15 +881,25 @@ class SimpleGlobalPolicy(GlobalPolicy):
         sorted_clusters = self._prepare_gpu_clusters(gpu_available_memory)
         
         activation_plan = {}
+        # Include target slots reserved earlier in this cycle. Source slots
+        # remain occupied until target-first activation has completed.
+        gpu_planned_active_count = {
+            gpu_id: len(gpu_to_active_instances[gpu_id])
+            for gpu_id in range(self.num_gpus)
+        }
+        for _, _, target_gpu_id in migrations:
+            gpu_planned_active_count[target_gpu_id] += 1
         for model_name in sorted_inactive_models:
             model_memory_required = self.model_weights_info[model_name]["model_size"]
             target_gpu = self._place_inactive_model(
                 model_name, 0, model_memory_required,  # instance_idx doesn't matter here
-                gpu_available_memory, sorted_clusters, model_queues, model_instance_state_dict
+                gpu_available_memory, sorted_clusters, model_queues,
+                model_instance_state_dict, gpu_planned_active_count,
             )
             
             if target_gpu is not None:
                 activation_plan[(model_name, target_gpu)] = target_gpu
+                gpu_planned_active_count[target_gpu] += 1
             else:
                 logger.warning(f"No suitable GPU found for inactive model {model_name}")
 
@@ -899,12 +915,19 @@ class SimpleGlobalPolicy(GlobalPolicy):
         for model_name, source_gpu_id, target_gpu_id in migrations:
             # Deactivate the instance on source GPU
             source_instance_key = (model_name, source_gpu_id)
+            # V6: the paper's KV migration presupposes preemption --
+            # retract the in-flight requests, move their KV, resume on
+            # the target.  preempt=False drains instead, which leaves
+            # nothing to move.  Only the migration path flips; idle
+            # eviction has no target and keeps draining.
+            import os as _os
+            _v6_kv = _os.environ.get("PRISM_V6_KV_MIGRATION") == "1"
             if source_instance_key not in idle_instance_keys:
                 model_instance_to_action_dict.setdefault(source_instance_key, []).append(
                     DeactivateAction(
                         model_name=model_name,
                         instance_idx=source_gpu_id,
-                        preempt=False,
+                        preempt=_v6_kv,
                         preempt_mode="RECOMPUTE",
                         evict_waiting_requests=True,
                         gpu_id=source_gpu_id,
@@ -942,9 +965,17 @@ class SimpleGlobalPolicy(GlobalPolicy):
         logger.info(f"Total time for global scheduling: {end_total - start_total:.4f}s")
 
         all_actions = []
+        _target_first = bool(getattr(self, "_target_first_actions", False))
 
         for actions in model_instance_to_action_dict.values():
-            sorted_actions = sorted(actions, key=lambda action: 0 if isinstance(action, DeactivateAction) else 1)
+            sorted_actions = sorted(actions, key=lambda action: (
+                1 if isinstance(action, DeactivateAction) else 0) if _target_first else (
+                0 if isinstance(action, DeactivateAction) else 1))
             all_actions.extend(sorted_actions)
 
+        # Migration actions live under separate (model, instance) keys. A
+        # per-key sort alone cannot guarantee target-first ordering globally.
+        if _target_first:
+            all_actions.sort(key=lambda action: 1 if isinstance(action, DeactivateAction) else 0)
+
         return all_actions
\ No newline at end of file
diff --git a/python/sglang/multi_model/utils/model_info.json b/python/sglang/multi_model/utils/model_info.json
index 4a8f82c1ec0c97c675792cbf09d1081e3dacc32b..76a561092d7538fcaa93d5c8883874e75aef9d3b 100644
--- a/python/sglang/multi_model/utils/model_info.json
+++ b/python/sglang/multi_model/utils/model_info.json
@@ -1,98 +1,118 @@
 {
-    "meta-llama/Llama-2-13b-chat-hf": {
-        "model_size": 24.376953125,
-        "cell_size": 819200
+    "Qwen/Qwen2.5-0.5B-Instruct": {
+        "cell_size": 12288,
+        "model_size": 1.048828125
     },
-    "meta-llama/Llama-2-13b-hf": {
-        "model_size": 24.376953125,
-        "cell_size": 819200
+    "Qwen/Qwen2.5-1.5B-Instruct": {
+        "cell_size": 28672,
+        "model_size": 3.0078125
     },
-    "microsoft/Phi-3-mini-4k-instruct": {
-        "model_size": 7.12109375,
-        "cell_size": 393216
+    "Qwen/Qwen2.5-32B": {
+        "cell_size": 8192,
+        "model_size": 8
     },
-    "microsoft/Phi-3-mini-128k-instruct": {
-        "model_size": 7.19140625,
-        "cell_size": 393216
+    "Qwen/Qwen2.5-3B-Instruct": {
+        "cell_size": 36864,
+        "model_size": 5.8359375
     },
-    "microsoft/Phi-3.5-mini-instruct": {
-        "model_size": 7.169921875,
-        "cell_size": 393216
+    "Qwen/Qwen2.5-7B-Instruct": {
+        "cell_size": 57344,
+        "model_size": 14.283203125
     },
-    "microsoft/phi-2": {
-        "model_size": 5.212890625,
-        "cell_size": 327680
+    "allenai/OLMo-1B-hf": {
+        "cell_size": 131072,
+        "model_size": 2.1875
+    },
+    "bigcode/starcoder2-3b": {
+        "cell_size": 30720,
+        "model_size": 5.65234375
+    },
+    "google/gemma-2-9b-it": {
+        "cell_size": 344064,
+        "model_size": 17.21484375
+    },
+    "google/gemma-2b": {
+        "cell_size": 18432,
+        "model_size": 4.6640625
+    },
+    "google/gemma-2b-it": {
+        "cell_size": 18432,
+        "model_size": 4.6640625
     },
     "ibm/PowerLM-3b": {
-        "model_size": 6.73046875,
-        "cell_size": 368640
+        "cell_size": 368640,
+        "model_size": 6.73046875
     },
-    "microsoft/phi-1_5": {
-        "model_size": 2.650390625,
-        "cell_size": 196608
+    "meta-llama/Llama-2-13b-chat-hf": {
+        "cell_size": 819200,
+        "model_size": 24.376953125
     },
-    "allenai/OLMo-1B-hf": {
-        "model_size": 2.1875,
-        "cell_size": 131072
+    "meta-llama/Llama-2-13b-hf": {
+        "cell_size": 819200,
+        "model_size": 24.376953125
     },
-    "bigcode/starcoder2-3b": {
-        "model_size": 5.65234375,
-        "cell_size": 30720
+    "meta-llama/Llama-2-7b-hf": {
+        "cell_size": 524288.0,
+        "model_size": 12.55
+    },
+    "meta-llama/Llama-3.1-8B": {
+        "cell_size": 131072,
+        "model_size": 15.080078125
     },
     "meta-llama/Llama-3.2-1B": {
-        "model_size": 2.279296875,
-        "cell_size": 32768
+        "cell_size": 32768,
+        "model_size": 2.279296875
     },
     "meta-llama/Llama-3.2-1B-Instruct": {
-        "model_size": 2.263671875,
-        "cell_size": 32768
+        "cell_size": 32768,
+        "model_size": 2.263671875
     },
     "meta-llama/Llama-3.2-3B": {
-        "model_size": 6.0,
-        "cell_size": 114688
+        "cell_size": 114688,
+        "model_size": 6.0
     },
     "meta-llama/Llama-3.2-3B-Instruct": {
-        "model_size": 5.94921875,
-        "cell_size": 114688
+        "cell_size": 114688,
+        "model_size": 5.94921875
     },
-    "google/gemma-2b": {
-        "model_size": 4.6640625,
-        "cell_size": 18432
+    "meta-llama/Llama-3.3-70B-Instruct": {
+        "cell_size": 10240,
+        "model_size": 17.5
     },
-    "google/gemma-2b-it": {
-        "model_size": 4.6640625,
-        "cell_size": 18432
+    "meta-llama/Meta-Llama-3.1-8B": {
+        "cell_size": 131072,
+        "model_size": 15.005859375
     },
-    "mistralai/Mistral-7B-Instruct-v0.2": {
-        "model_size": 13.490234375,
-        "cell_size": 131072
+    "microsoft/Phi-3-mini-128k-instruct": {
+        "cell_size": 393216,
+        "model_size": 7.19140625
     },
-    "meta-llama/Meta-Llama-3.1-8B": {
-        "model_size": 15.005859375,
-        "cell_size": 131072
+    "microsoft/Phi-3-mini-4k-instruct": {
+        "cell_size": 393216,
+        "model_size": 7.12109375
     },
-    "meta-llama/Llama-3.1-8B": {
-        "model_size": 15.080078125,
-        "cell_size": 131072
+    "microsoft/Phi-3.5-mini-instruct": {
+        "cell_size": 393216,
+        "model_size": 7.169921875
     },
-    "google/gemma-2-9b-it": {
-        "model_size": 17.21484375,
-        "cell_size": 344064
+    "microsoft/phi-1_5": {
+        "cell_size": 196608,
+        "model_size": 2.650390625
     },
-    "mistralai/Mistral-Nemo-Instruct-2407": {
-        "model_size": 22.849609375,
-        "cell_size": 163840
+    "microsoft/phi-2": {
+        "cell_size": 327680,
+        "model_size": 5.212890625
     },
-    "meta-llama/Llama-2-7b-hf": {
-        "model_size": 12.550,
-        "cell_size": 524288.00
+    "mistralai/Mistral-7B-Instruct-v0.2": {
+        "cell_size": 131072,
+        "model_size": 13.490234375
     },
-    "meta-llama/Llama-3.3-70B-Instruct": {
-        "model_size": 17.5,
-        "cell_size": 10240
+    "mistralai/Mistral-Nemo-Instruct-2407": {
+        "cell_size": 163840,
+        "model_size": 22.849609375
     },
-    "Qwen/Qwen2.5-32B": {
-        "model_size": 8,
-        "cell_size": 8192
+    "meta-llama/Llama-3.1-70B": {
+        "cell_size": 327680,
+        "model_size": 131.416598216
     }
 }
\ No newline at end of file
diff --git a/python/sglang/srt/managers/io_struct.py b/python/sglang/srt/managers/io_struct.py
index 48bf59267a4bee2190b6902134ac0d5675f7e0b6..83c7be1a1a7f57c86882e7a32c19400e1d70177a 100644
--- a/python/sglang/srt/managers/io_struct.py
+++ b/python/sglang/srt/managers/io_struct.py
@@ -62,6 +62,13 @@ class GenerateReqInput:
     model: Optional[str] = None
     slo: Optional[float] = None
     arrival_time: Optional[float] = None
+    # Diagnostic timestamps set by the per-GPU scheduler.  They are inert
+    # metadata and do not participate in admission or engine scheduling.
+    gpu_scheduler_queue_time: Optional[float] = None
+    gpu_scheduler_dispatch_time: Optional[float] = None
+    # Monotonic per-GPU order assigned by paper Algorithm 2.  None keeps the
+    # released-prototype path unchanged.
+    alg2_seq: Optional[int] = None
     is_warmup: bool = False
     # For controller to know the input and output length
     prompt_len: Optional[int] = None
@@ -201,6 +208,9 @@ class TokenizedGenerateReqInput:
 
     arrival_time: Optional[float] = None
     slo: Optional[float] = None
+    gpu_scheduler_queue_time: Optional[float] = None
+    gpu_scheduler_dispatch_time: Optional[float] = None
+    alg2_seq: Optional[int] = None
 
 
 @dataclass
@@ -454,10 +464,13 @@ class ActivateReqInput:
     instance_idx: Optional[int] = 0
     rid: Optional[str] = None
     memory_pool_size: Optional[float] = None
+    phase: str = "full"  # PAPER-FAITHFUL-V6: full|prepare|commit
 
     def __post_init__(self):
         if self.rid is None:
             self.rid = uuid.uuid4().hex
+        if self.phase not in ("full", "prepare", "commit"):
+            raise ValueError(f"Invalid activation phase: {self.phase}")
 
 
 @dataclass
@@ -470,6 +483,7 @@ class ActivateReqOutput:
     model_name: str
     instance_idx: Optional[int] = 0
     gpu_id: Optional[int] = None
+    phase: str = "full"  # PAPER-FAITHFUL-V6
 
 
 @dataclass
@@ -495,6 +509,28 @@ class BatchRunReq:
     model: str
     run_time: float
     gpu_id: Optional[int] = None
+    alg2_seqs: Optional[List[Optional[int]]] = None
+
+
+@dataclass
+class BackendAdmitReq:
+    """Engine-side admission of an ordered Algorithm-2 request."""
+
+    rids: List[str]
+    model: str
+    alg2_seqs: List[int]
+    admit_time: float
+    gpu_id: Optional[int] = None
+
+
+@dataclass
+class PrefillCompleteReq:
+    """Worker-pool acknowledgement for the per-GPU Algorithm-2 gate."""
+
+    rids: List[str]
+    model: str
+    complete_time: float
+    gpu_id: Optional[int] = None
 
 
 @dataclass
diff --git a/python/sglang/srt/managers/schedule_batch.py b/python/sglang/srt/managers/schedule_batch.py
index 9f0b7000d13ff4ad079a1d50856758c6c56a35dd..18858268b8eabf4aeb31dc92ad9cec83556e77dd 100644
--- a/python/sglang/srt/managers/schedule_batch.py
+++ b/python/sglang/srt/managers/schedule_batch.py
@@ -181,6 +181,9 @@ class Req:
         lora_path: Optional[str] = None,
         arrival_time: Optional[float] = None,
         slo: Optional[float] = None,
+        gpu_scheduler_queue_time: Optional[float] = None,
+        gpu_scheduler_dispatch_time: Optional[float] = None,
+        alg2_seq: Optional[int] = None,
     ):
         # Input and output info
         self.rid = rid
@@ -191,6 +194,11 @@ class Req:
         self.fill_ids = None  # fill_ids = origin_input_ids + output_ids
         self.arrival_time = arrival_time
         self.slo = slo
+        self.gpu_scheduler_queue_time = gpu_scheduler_queue_time
+        self.gpu_scheduler_dispatch_time = gpu_scheduler_dispatch_time
+        self.alg2_seq = alg2_seq
+        self.alg2_backend_admitted = False
+        self.alg2_started = False
         self.out_queue_timestamp = None
         self.prefill_finish_timestamp = None
         self.decode_timestamps = (
diff --git a/python/sglang/srt/managers/scheduler.py b/python/sglang/srt/managers/scheduler.py
index 3cf0db7067d1383ad7d5b8d13e917a25ba7e29f1..dff2e3ceca9e904484ee9cd6760ef04c30480a78 100644
--- a/python/sglang/srt/managers/scheduler.py
+++ b/python/sglang/srt/managers/scheduler.py
@@ -47,6 +47,7 @@ from sglang.srt.managers.io_struct import (
     ActivateReqInput,
     ActivateReqOutput,
     BatchEmbeddingOut,
+    BackendAdmitReq,
     BatchRetractDecodeReq,
     BatchRunReq,
     BatchTokenIDOut,
@@ -58,6 +59,7 @@ from sglang.srt.managers.io_struct import (
     GetMemoryUsageReqOutput,
     GetMemPoolSizeReq,
     GetMemPoolSizeReqOutput,
+    PrefillCompleteReq,
     PreemptMode,
     ProfileReq,
     ResizeMemPoolReqInput,
@@ -130,6 +132,12 @@ class Scheduler:
         self.server_args = server_args
         self.gpu_id = gpu_id
         self.tp_rank = tp_rank
+        self._v6_captured = []            # V6: retracted KV awaiting stash
+        self._v6_capture_failures = 0
+        self._v6_prepared_model = None   # V6: private until commit
+        self._v6_committed_model = None
+        self._v6_first_request_logged = False
+        self._v6_first_decode_logged = False
         self.tp_size = server_args.tp_size
         self.schedule_policy = server_args.schedule_policy
         self.disable_regex_jump_forward = server_args.disable_regex_jump_forward
@@ -338,6 +346,14 @@ class Scheduler:
         self.last_tput_update_time = time.time()
         self.model_name = server_args.model_name
         self.instance_idx = getattr(server_args, "instance_idx", 0)
+        self._alg2_runtime_gate = bool(
+            self.enable_worker_pool and server_args.enable_moore_hodgson
+        )
+        self._alg2_staged_generation_reqs = []
+        self._alg2_admission_seq_key = (
+            f"{server_args.engine_to_gpu_scheduler_key_prefix}:"
+            f"alg2_next_admission:{self.gpu_id}"
+        )
 
         self.tput_update_thread = threading.Thread(
             target=self._send_token_tput_updates, daemon=True
@@ -605,12 +621,27 @@ class Scheduler:
             max_prefill_count = (
                 int(approx_remain_prefill_tokens // self.avg_input_len) + 1
             )
-            max_fetch_count = max_prefill_count - len(self.waiting_queue)
+            max_fetch_count = (
+                max_prefill_count
+                - len(self.waiting_queue)
+                - len(self._alg2_staged_generation_reqs)
+            )
             if max_fetch_count > 0:
-                recv_reqs = self.redis_client.recv_pyobj_non_block(
+                fetched_reqs = self.redis_client.recv_pyobj_non_block(
                     key=f"{self.server_args.backend_generate_request_key_prefix}:{self.model_name}",
                     count=max_fetch_count,
                 )
+                if self._alg2_runtime_gate:
+                    self._alg2_staged_generation_reqs.extend(fetched_reqs)
+                else:
+                    recv_reqs = fetched_reqs
+            if self._alg2_runtime_gate and self._alg2_staged_generation_reqs:
+                next_seq = self.redis_client.get_int(self._alg2_admission_seq_key)
+                if next_seq is not None:
+                    for index, req in enumerate(self._alg2_staged_generation_reqs):
+                        if req.alg2_seq == next_seq:
+                            recv_reqs = [self._alg2_staged_generation_reqs.pop(index)]
+                            break
             # if max_fetch_count > 0:
             #     recv_reqs = self.redis_client.recv_pyobj_non_block(
             #         key=f"{self.server_args.backend_generate_request_key_prefix}:{self.model_name}",
@@ -668,6 +699,24 @@ class Scheduler:
             assert isinstance(recv_req, GenerateReqInput)
             tokenized_req = self._tokenize(recv_req)
             self.handle_generate_request(tokenized_req)
+            if self._alg2_runtime_gate:
+                req = self.waiting_queue[-1]
+                req.alg2_backend_admitted = True
+                admit_time = time.time()
+                if self.tp_rank == 0:
+                    self.redis_client.send_pyobj(
+                        key=(
+                            f"{self.server_args.engine_to_gpu_scheduler_key_prefix}:"
+                            f"{self.gpu_id}"
+                        ),
+                        obj=BackendAdmitReq(
+                            rids=[req.rid],
+                            model=self.model_name,
+                            alg2_seqs=[req.alg2_seq],
+                            admit_time=admit_time,
+                            gpu_id=self.gpu_id,
+                        ),
+                    )
 
     def process_input_other_requests(self, recv_reqs: List):
         recv_deactivate_reqs = []
@@ -778,6 +827,9 @@ class Scheduler:
             obj.lora_path,
             obj.arrival_time,
             obj.slo,
+            obj.gpu_scheduler_queue_time,
+            obj.gpu_scheduler_dispatch_time,
+            obj.alg2_seq,
         )
         return tokenized_obj
 
@@ -807,6 +859,9 @@ class Scheduler:
             lora_path=recv_req.lora_path,
             arrival_time=recv_req.arrival_time,
             slo=recv_req.slo,
+            gpu_scheduler_queue_time=recv_req.gpu_scheduler_queue_time,
+            gpu_scheduler_dispatch_time=recv_req.gpu_scheduler_dispatch_time,
+            alg2_seq=recv_req.alg2_seq,
         )
         req.tokenizer = self.tokenizer
 
@@ -923,14 +978,62 @@ class Scheduler:
         # Prefill first
         new_batch = self.get_new_batch_prefill()
         if new_batch is not None:
-            self.send_to_controller.send_pyobj(
-                BatchRunReq(
-                    rids=[req.rid for req in new_batch.reqs],
-                    model=self.model_config.name,
-                    run_time=time.time(),
-                    gpu_id=self.gpu_id,
+            if self._v6_committed_model and not self._v6_first_request_logged:
+                self._v6_first_request_logged = True
+                logger.info("[PAPER-MIGRATION-TIMELINE] " + json.dumps({
+                    "event": "first_request_on_target",
+                    "model": self._v6_committed_model,
+                    "gpu_id": self.gpu_id,
+                    "time": time.time(),
+                    "rids": [req.rid for req in new_batch.reqs],
+                }))
+            newly_started_alg2 = [
+                req for req in new_batch.reqs
+                if req.alg2_seq is not None
+                and req.alg2_backend_admitted
+                and not req.alg2_started
+            ]
+            if self._alg2_runtime_gate and len(newly_started_alg2) > 1:
+                raise RuntimeError(
+                    "Algorithm-2 backend barrier admitted multiple new prefills"
                 )
+            for req in newly_started_alg2:
+                req.alg2_started = True
+            batch_run_req = BatchRunReq(
+                rids=[req.rid for req in new_batch.reqs],
+                model=self.model_config.name,
+                run_time=time.time(),
+                gpu_id=self.gpu_id,
+                alg2_seqs=[req.alg2_seq for req in new_batch.reqs],
             )
+            self.send_to_controller.send_pyobj(batch_run_req)
+            # Algorithm 2 models one global prefill stream per GPU.  In worker
+            # pool mode, report the actual engine-side admission event so the
+            # per-GPU gate can verify that independent model engines did not
+            # reorder the selected schedule.
+            if (
+                self.tp_rank == 0
+                and self.enable_worker_pool
+                and self.server_args.enable_moore_hodgson
+            ):
+                self.redis_client.send_pyobj(
+                    key=(
+                        f"{self.server_args.engine_to_gpu_scheduler_key_prefix}:"
+                        f"{self.gpu_id}"
+                    ),
+                    obj=batch_run_req,
+                )
+                if newly_started_alg2:
+                    seq = newly_started_alg2[0].alg2_seq
+                    if not self.redis_client.compare_and_advance_int(
+                        self._alg2_admission_seq_key, seq
+                    ):
+                        raise RuntimeError(
+                            "Algorithm-2 admission token advance failed: "
+                            f"model={self.model_name} rid={newly_started_alg2[0].rid} "
+                            f"seq={seq} current="
+                            f"{self.redis_client.get_int(self._alg2_admission_seq_key)}"
+                        )
             return new_batch
 
         # Check memory
@@ -1201,6 +1304,15 @@ class Scheduler:
 
     def process_batch_result(self, batch: ScheduleBatch, result):
         if batch.forward_mode.is_decode():
+            if self._v6_committed_model and not self._v6_first_decode_logged:
+                self._v6_first_decode_logged = True
+                logger.info("[PAPER-MIGRATION-TIMELINE] " + json.dumps({
+                    "event": "first_decode_on_target",
+                    "model": self._v6_committed_model,
+                    "gpu_id": self.gpu_id,
+                    "time": time.time(),
+                    "rids": [req.rid for req in batch.reqs],
+                }))
             self.process_batch_result_decode(batch, result)
             if batch.is_empty():
                 self.running_batch = None
@@ -1208,6 +1320,7 @@ class Scheduler:
             self.process_batch_result_prefill(batch, result)
 
     def process_batch_result_prefill(self, batch: ScheduleBatch, result):
+        completed_prefill_rids = []
         if self.is_generation:
             logits_output, next_token_ids, bid = result
 
@@ -1241,6 +1354,7 @@ class Scheduler:
                     req.is_inflight_req -= 1
                 else:
                     # Inflight reqs' prefill is not finished
+                    completed_prefill_rids.append(req.rid)
                     req.completion_tokens_wo_jump_forward += 1
                     req.output_ids.append(next_token_ids[i])
                     req.check_finished()
@@ -1278,16 +1392,37 @@ class Scheduler:
                     req.is_inflight_req -= 1
                 else:
                     # Inflight reqs' prefill is not finished
+                    completed_prefill_rids.append(req.rid)
                     # dummy output token for embedding models
                     req.output_ids.append(0)
                     req.check_finished()
-
                 if req.finished():
                     self.tree_cache.cache_finished_req(req)
                     req.set_finish_time()
                 else:
                     self.tree_cache.cache_unfinished_req(req)
 
+        # Remove this work from Algorithm-2 outstanding accounting only after
+        # the engine has actually completed the request's final prefill chunk.
+        if (
+            completed_prefill_rids
+            and self.tp_rank == 0
+            and self.enable_worker_pool
+            and self.server_args.enable_moore_hodgson
+        ):
+            self.redis_client.send_pyobj(
+                key=(
+                    f"{self.server_args.engine_to_gpu_scheduler_key_prefix}:"
+                    f"{self.gpu_id}"
+                ),
+                obj=PrefillCompleteReq(
+                    rids=completed_prefill_rids,
+                    model=self.model_config.name,
+                    complete_time=time.time(),
+                    gpu_id=self.gpu_id,
+                ),
+            )
+
         self.stream_output(batch.reqs)
 
     def process_batch_result_decode(self, batch: ScheduleBatch, result):
@@ -1472,6 +1607,8 @@ class Scheduler:
                             else None
                         ),
                         "arrival_timestamp": req.arrival_time,
+                        "gpu_scheduler_queue_timestamp": req.gpu_scheduler_queue_time,
+                        "gpu_scheduler_dispatch_timestamp": req.gpu_scheduler_dispatch_time,
                         "out_queue_timestamp": req.out_queue_timestamp,
                         "prefill_finish_timestamp": req.prefill_finish_timestamp,
                         "finish_timestamp": req.finish_timestamp,
@@ -1596,9 +1733,49 @@ class Scheduler:
         else:
             gpu_id = recv_req.gpu_id
 
-        if self._activated:
+        phase = getattr(recv_req, "phase", "full")
+        if phase == "commit":
+            success = self._v6_prepared_model == recv_req.model_name
+            if success:
+                commit_start = time.time()
+                self._restore_waiting_requests()
+                inject_start = time.time()
+                self._v6_inject_migrated_kv()
+                inject_end = time.time()
+                logger.info("[PAPER-MIGRATION-TIMELINE] " + json.dumps({
+                    "event": "target_inject",
+                    "model": self.model_name,
+                    "gpu_id": self.gpu_id,
+                    "start": inject_start,
+                    "end": inject_end,
+                }))
+                self._activated = True
+                self._v6_prepared_model = None
+                self._v6_committed_model = self.model_name
+                self._v6_first_request_logged = False
+                self._v6_first_decode_logged = False
+                logger.info("[PAPER-OVERLAP-V6] " + json.dumps({
+                    "event": "target_commit", "model": self.model_name,
+                    "gpu": self.gpu_id, "rid": recv_req.rid,
+                    "start": commit_start, "ready": time.time()}))
+            else:
+                logger.error("[PAPER-OVERLAP-V6] commit without matching prepare "
+                             f"model={recv_req.model_name}")
+            if self.tp_rank == 0:
+                activate_req_output = ActivateReqOutput(
+                    rid=recv_req.rid, gpu_id=gpu_id,
+                    model_name=recv_req.model_name,
+                    instance_idx=recv_req.instance_idx, success=success,
+                    memory_usage=self.get_memory_usage(), phase=phase)
+                self.send_to_detokenizer.send_pyobj(activate_req_output)
+                self.redis_client.send_pyobj(
+                    key=f"{self.server_args.engine_to_gpu_scheduler_key_prefix}:{self.gpu_id}",
+                    obj=activate_req_output)
+            return
+
+        if self._activated or self._v6_prepared_model is not None:
             if self.tp_rank == 0:
-                logger.warning("Scheduler is already activated")
+                logger.warning("Scheduler is already activated or prepared")
                 activate_req_output = ActivateReqOutput(
                     rid=recv_req.rid,
                     gpu_id=gpu_id,
@@ -1606,6 +1783,7 @@ class Scheduler:
                     instance_idx=recv_req.instance_idx,
                     success=False,
                     memory_usage=self.get_memory_usage(),
+                    phase=phase,
                 )
                 self.send_to_detokenizer.send_pyobj(
                     activate_req_output
@@ -1619,6 +1797,26 @@ class Scheduler:
         assert self.last_batch is None, "Last batch should be None"
         assert self.running_batch is None, "Running batch should be None"
 
+        if phase == "prepare" and not self._v6_clear_stale_kv(
+            self.model_names_to_model_paths[recv_req.model_name]
+        ):
+            logger.error(
+                "[PAPER-OVERLAP-V6] stale KV clear failed; refusing prepare "
+                f"model={recv_req.model_name}"
+            )
+            if self.tp_rank == 0:
+                activate_req_output = ActivateReqOutput(
+                    rid=recv_req.rid, gpu_id=gpu_id,
+                    model_name=recv_req.model_name,
+                    instance_idx=recv_req.instance_idx, success=False,
+                    memory_usage=self.get_memory_usage(), phase=phase)
+                self.send_to_detokenizer.send_pyobj(activate_req_output)
+                self.redis_client.send_pyobj(
+                    key=f"{self.server_args.engine_to_gpu_scheduler_key_prefix}:{self.gpu_id}",
+                    obj=activate_req_output)
+            return
+
+        prepare_wall_start = time.time()
         start_time = time.perf_counter()
 
         self.tp_worker.activate_model_runner(
@@ -1651,16 +1849,22 @@ class Scheduler:
         self.update_memory_pool()
         logger.info("Memory pool updated")
         # restore waiting queue
-        self._restore_waiting_requests()
-        logger.info("Waiting requests restored")
-
-        if not self._activated:
+        if phase == "prepare":
+            self._v6_prepared_model = recv_req.model_name
+            logger.info("[PAPER-OVERLAP-V6] " + json.dumps({
+                "event": "target_ready", "model": self.model_name,
+                "gpu": self.gpu_id, "rid": recv_req.rid,
+                "start": prepare_wall_start,
+                "ready": time.time(),
+                "prepare_duration_s": time.perf_counter() - start_time}))
+        else:
+            self._restore_waiting_requests()
+            self._v6_inject_migrated_kv()
             self._activated = True
+            logger.info("Waiting requests restored")
             logger.info(
                 f"Scheduler activated. Activation takes {time.perf_counter() - start_time:.2f} s"
             )
-        else:
-            logger.warning("Scheduler is already activated")
 
         if self.tp_size > 1:
             self.tp_worker.model_runner.tp_group.barrier()
@@ -1674,6 +1878,7 @@ class Scheduler:
                 instance_idx=recv_req.instance_idx,
                 success=True,
                 memory_usage=memory_usage,
+                phase=phase,
             )
             self.send_to_detokenizer.send_pyobj(activate_req_output)
 
@@ -1690,8 +1895,15 @@ class Scheduler:
         else:
             gpu_id = recv_req.gpu_id
 
-        if self._activated:
+        if self._activated or self._v6_prepared_model is not None:
+            quiesce = time.time()
+            was_serving = self._activated
             self._activated = False
+            self._v6_prepared_model = None
+            logger.info("[PAPER-OVERLAP-V6] " + json.dumps({
+                "event": "source_quiesce", "model": self.model_name,
+                "gpu": self.gpu_id, "rid": recv_req.rid,
+                "time": quiesce, "was_serving": was_serving}))
             logger.info(f"Scheduler receives the deactivate requests with rid: {recv_req.rid}")
         else:
             if self.tp_rank == 0:
@@ -1715,6 +1927,7 @@ class Scheduler:
             return
 
         start_time = time.perf_counter()
+        drain_start = time.time()
         logger.info(
             f"In handle deactivate request, waiting queue size: {len(self.waiting_queue)}. Running batch size: {0 if self.running_batch is None else len(self.running_batch.reqs)}, last batch size: {0 if self.last_batch is None else len(self.last_batch.reqs)}"
         )
@@ -1748,12 +1961,49 @@ class Scheduler:
                     f"Preempt mode {preempt_mode} is not supported"
                 )
         t1 = time.perf_counter()
+        drain_end = time.time()
+        logger.info("[PAPER-MIGRATION-TIMELINE] " + json.dumps({
+            "event": "request_drain",
+            "model": self.model_name,
+            "gpu_id": self.gpu_id,
+            "start": drain_start,
+            "end": drain_end,
+            "preempt": preempt,
+        }))
         time_taken = t1 - start_time
         logger.info(
             f"Process ongoing requests with preempt ({preempt}) takes {time_taken:.4f} s"
         )
 
+        stash_ok = self._v6_stash_captured()
+        if not stash_ok:
+            self._activated = True
+            logger.error(
+                "[PAPER-KV-V6] relay did not acknowledge stash; "
+                f"keeping source active model={self.model_name}"
+            )
+            if self.tp_rank == 0:
+                deactivate_req_output = DeactivateReqOutput(
+                    rid=recv_req.rid, gpu_id=gpu_id,
+                    model_name=recv_req.model_name,
+                    instance_idx=recv_req.instance_idx, success=False,
+                    memory_usage=self.get_memory_usage())
+                self.send_to_detokenizer.send_pyobj(deactivate_req_output)
+                self.redis_client.send_pyobj(
+                    key=f"{self.server_args.engine_to_gpu_scheduler_key_prefix}:{self.gpu_id}",
+                    obj=deactivate_req_output)
+            return
+        if self._v6_kv_enabled():
+            # Captured requests were removed after a successful stash;
+            # any leftovers failed capture/stash and must recompute.
+            self._evict_all_waiting_requests()
         self.tp_worker.deactivate_model_runner()
+        logger.info("[PAPER-MIGRATION-TIMELINE] " + json.dumps({
+            "event": "source_release",
+            "model": recv_req.model_name,
+            "gpu_id": self.gpu_id,
+            "time": time.time(),
+        }))
 
         if self.tp_size > 1:
             self.tp_worker.model_runner.tp_group.barrier()
@@ -1816,6 +2066,8 @@ class Scheduler:
             lora_path=req.lora_path,
             arrival_time=req.arrival_time,
             slo=req.slo,
+            gpu_scheduler_queue_time=getattr(req, "gpu_scheduler_queue_time", None),
+            gpu_scheduler_dispatch_time=getattr(req, "gpu_scheduler_dispatch_time", None),
             image_data=None,
             model=self.model_name,
             prompt_len=len(req.origin_input_ids),
@@ -1839,6 +2091,148 @@ class Scheduler:
             logger.error(f"Error evicting requests to Redis: {e}")
         self.waiting_queue.clear()
 
+    def _v6_inject_migrated_kv(self):
+        """Pull this model's migrated KV, if any, and rebuild its requests.
+
+        Runs after update_memory_pool(), so the target's own pools and
+        allocator already exist.  Slots come from the target allocator and
+        bear no relation to the source's numbering -- token order is what
+        keeps the KV correct.
+
+        The capsule is also the request body.  If KV placement fails or
+        exceeds the transfer cap, rebuild it with an empty prefix so it
+        recomputes instead of disappearing.
+        """
+        if not self._v6_kv_enabled():
+            return
+        try:
+            import json as _json
+            from sglang.multi_model import kv_migration_v6 as _kvm
+            mr = self.tp_worker.model_runner
+            q = getattr(mr, "input_queue", None)
+            # A queue nothing else reads, so the weight loader
+            # cannot take this reply out from under us.
+            oq = getattr(q, "kv_replies", {}).get(mr.engine_id)
+            if q is None or oq is None:
+                return
+            # Queue puts from different engine processes have no global
+            # ordering guarantee. Retry a not-yet-present (None) stash; an
+            # empty list is an acknowledged source quiesce with no KV.
+            deadline = time.monotonic() + 5
+            caps = None
+            while caps is None:
+                q.put(("__kv_fetch__", mr.model_path, None, mr.engine_id))
+                try:
+                    caps = oq.get(timeout=max(0.01, deadline - time.monotonic()))
+                except Exception:
+                    logger.warning("[PAPER-KV-V6] fetch timed out; "
+                                   "falling back to recompute")
+                    return
+                if caps is None:
+                    if time.monotonic() >= deadline:
+                        logger.warning("[PAPER-KV-V6] stash readiness timed out")
+                        return
+                    time.sleep(0.01)
+            if not caps:
+                return
+            moved, skipped, rec = _kvm.migrate_request_kv(
+                caps, self.gpu_id, tag=f"{self.model_name}|gpu={self.gpu_id}")
+            pool = self.token_to_kv_pool
+            dev = f"cuda:{self.gpu_id}"
+            injected = recomputed = rebuild_failures = 0
+
+            def _build(cap, slots=None):
+                if slots is None:
+                    req = _kvm.build_recomputed_request(
+                        Req, cap, self.tokenizer)
+                else:
+                    req = _kvm.build_resumed_request(
+                        Req, cap, slots, self.tokenizer)
+                if (req.sampling_params.json_schema is not None or
+                        req.sampling_params.regex is not None):
+                    if req.sampling_params.json_schema is not None:
+                        req.regex_fsm, regex = self.regex_fsm_cache.query(
+                            ("json", req.sampling_params.json_schema))
+                    else:
+                        req.regex_fsm, regex = self.regex_fsm_cache.query(
+                            ("regex", req.sampling_params.regex))
+                    if not self.disable_regex_jump_forward:
+                        req.jump_forward_map = self.jump_forward_cache.query(regex)
+                self.waiting_queue.append(req)
+                return req
+
+            for cap in moved:
+                slots = None
+                try:
+                    slots = pool.alloc(cap.num_tokens)
+                    if slots is None:
+                        raise RuntimeError("target KV slots unavailable")
+                    _kvm.scatter_request_kv(pool.k_buffer, pool.v_buffer,
+                                            slots, cap, dev)
+                    req = _build(cap, slots)
+                    injected += 1
+                    logger.info("[PAPER-KV-V6] " + _json.dumps({
+                        "event": "resume", "rid": req.rid,
+                        "tokens": len(slots), "gpu": self.gpu_id,
+                        "prompt_tokens": len(req.origin_input_ids),
+                        "output_tokens": len(req.output_ids),
+                        "resume_extend_len": req.extend_input_len}))
+                except Exception as e:
+                    if slots is not None:
+                        pool.free(slots)
+                    try:
+                        req = _build(cap)
+                        recomputed += 1
+                        logger.warning("[PAPER-KV-V6] " + _json.dumps({
+                            "event": "recompute", "rid": req.rid,
+                            "reason": str(e), "gpu": self.gpu_id}))
+                    except Exception as rebuild_error:
+                        rebuild_failures += 1
+                        logger.warning(
+                            f"[PAPER-KV-V6] rebuild failed for {cap.rid}: "
+                            f"{rebuild_error}")
+            for cap in skipped:
+                try:
+                    req = _build(cap)
+                    recomputed += 1
+                    logger.info("[PAPER-KV-V6] " + _json.dumps({
+                        "event": "recompute", "rid": req.rid,
+                        "reason": "over-cap", "gpu": self.gpu_id}))
+                except Exception as e:
+                    rebuild_failures += 1
+                    logger.warning(
+                        f"[PAPER-KV-V6] rebuild failed for {cap.rid}: {e}")
+            logger.info("[PAPER-KV-V6] " + _json.dumps(dict(
+                rec, event="inject", requests_injected=injected,
+                requests_recomputed=recomputed,
+                request_rebuild_failures=rebuild_failures,
+                model=self.model_name)))
+        except Exception as e:
+            logger.warning(f"[PAPER-KV-V6] inject stage failed: {e}")
+
+    def _v6_clear_stale_kv(self, model_key):
+        """ACK a stale-stash clear before source quiesce begins."""
+        if not self._v6_kv_enabled():
+            return True
+        try:
+            q = self.tp_worker.model_runner.input_queue
+            engine_id = self.tp_worker.model_runner.engine_id
+            oq = getattr(q, "kv_replies", {}).get(engine_id)
+            if q is None or oq is None:
+                return False
+            q.put(("__kv_clear__", model_key, None, engine_id))
+            ack = oq.get(timeout=5)
+            ok = isinstance(ack, tuple) and ack[0] == "__kv_clear_ack__"
+            if ok:
+                logger.info(
+                    "[PAPER-KV-V6] stale-clear acknowledged "
+                    f"model={model_key} dropped={ack[1]}"
+                )
+            return ok
+        except Exception as e:
+            logger.warning(f"[PAPER-KV-V6] stale-clear failed: {e}")
+            return False
+
     def _restore_waiting_requests(self):
         self.waiting_queue.extend(self.waiting_queue_stash)
         self.waiting_queue_stash = []
@@ -1898,6 +2292,96 @@ class Scheduler:
 
         self._retract_running_batch_normal()
 
+    def _v6_kv_enabled(self):
+        import os
+        return os.environ.get("PRISM_V6_KV_MIGRATION") == "1"
+
+    def _v6_capture_kv(self, req):
+        """Stash one retracted request's KV before the slots are freed.
+
+        Silent on any failure: a migration that cannot carry its KV must
+        degrade to the prototype's recompute, never take the engine down.
+        The counter below is what makes that visible instead of silent.
+        """
+        if not self._v6_kv_enabled():
+            return
+        try:
+            from sglang.multi_model import kv_migration_v6 as _kvm
+            pool = self.token_to_kv_pool
+            if getattr(pool, "k_buffer", None) is None:
+                return
+            n = len(req.origin_input_ids) + len(req.output_ids) - 1
+            if n <= 0:
+                return
+            slots = self.req_to_token_pool.req_to_token[req.req_pool_idx, :n]
+            dev = f"cuda:{self.gpu_id}"
+            k, v = _kvm.gather_request_kv(pool.k_buffer, pool.v_buffer, slots, dev)
+            cap = _kvm.RequestKVCapsule(
+                rid=req.rid, model_name=self.model_name,
+                origin_input_ids=list(req.origin_input_ids),
+                output_ids=list(req.output_ids),
+                sampling_params=req.sampling_params,
+                arrival_time=getattr(req, "arrival_time", None),
+                slo=getattr(req, "slo", None),
+                k=k, v=v, source_gpu=self.gpu_id,
+                request_state={name: getattr(req, name, None) for name in (
+                    "origin_input_text", "lora_path", "return_logprob",
+                    "top_logprobs_num", "stream", "logprob_start_len",
+                    "vid", "decoded_text", "surr_offset", "read_offset",
+                    "completion_tokens_wo_jump_forward",
+                    "normalized_prompt_logprob", "input_token_logprobs",
+                    "input_top_logprobs", "output_token_logprobs",
+                    "output_top_logprobs", "out_queue_timestamp",
+                    "prefill_finish_timestamp", "decode_timestamps",
+                    "regex_fsm_state")})
+            self._v6_captured.append(cap)
+        except Exception as e:
+            self._v6_capture_failures += 1
+            logger.warning(f"[PAPER-KV-V6] capture failed for {req.rid}: {e}")
+
+    def _v6_stash_captured(self):
+        """Publish the latest KV and wait until the relay owns its clone."""
+        if not self._v6_kv_enabled():
+            return True
+        stash_start = time.time()
+        try:
+            import json as _json
+            mr = self.tp_worker.model_runner
+            q = mr.input_queue
+            engine_id = mr.engine_id
+            oq = getattr(q, "kv_replies", {}).get(engine_id)
+            if q is None or oq is None:
+                return False
+            key = mr.model_path
+            n_tok = sum(c.num_tokens for c in self._v6_captured)
+            nbytes = sum(c.nbytes for c in self._v6_captured)
+            q.put(("__kv_stash__", key, self.gpu_id,
+                   (self._v6_captured, engine_id)))
+            ack = oq.get(timeout=30)
+            if not (isinstance(ack, tuple) and len(ack) == 3
+                    and ack[0] == "__kv_stash_ack__"
+                    and ack[1] == key
+                    and ack[2] == len(self._v6_captured)):
+                raise RuntimeError(f"invalid stash acknowledgement: {ack!r}")
+            captured_rids = {c.rid for c in self._v6_captured}
+            self.waiting_queue[:] = [
+                req for req in self.waiting_queue if req.rid not in captured_rids
+            ]
+            logger.info("[PAPER-KV-V6] " + _json.dumps({
+                "event": "stash", "model": key, "source_gpu": self.gpu_id,
+                "logical_model": self.model_name,
+                "start": stash_start, "end": time.time(),
+                "requests": len(self._v6_captured), "tokens": n_tok,
+                "kv_bytes": nbytes,
+                "capture_failures": self._v6_capture_failures}))
+            return True
+        except Exception as e:
+            logger.warning(f"[PAPER-KV-V6] stash failed: {e}")
+            return False
+        finally:
+            self._v6_captured = []
+            self._v6_capture_failures = 0
+
     def _retract_running_batch_normal(self):
         # merge the prefill batch into the running batch
         if (
@@ -1926,6 +2410,7 @@ class Scheduler:
         len_output_ids = []
         if self.running_batch is not None:
             for req in self.running_batch.reqs:
+                self._v6_capture_kv(req)
                 req.prefix_indices = []
                 req.last_node = None
                 req.extend_input_len = 0
diff --git a/python/sglang/srt/model_executor/model_runner.py b/python/sglang/srt/model_executor/model_runner.py
index 5bddef92edbc406111207129f3dfe3035132a111..41f767b113efbeaddb03b84e58bc09d6ec98021c 100644
--- a/python/sglang/srt/model_executor/model_runner.py
+++ b/python/sglang/srt/model_executor/model_runner.py
@@ -136,6 +136,13 @@ class BaseModelRunner:
                 "Tensor parallelism is enabled, model service will not be used."
             )
             self.use_model_service = False
+        # PAPER-FAITHFUL-TP evidence line.  Every rank prints where it
+        # actually is; the shared log prefix cannot, since it carries
+        # rank0's gpu_id for the whole group.
+        logger.info(
+            f"[PAPER-TP] engine rank: tp_rank={tp_rank} gpu_id={gpu_id} "
+            f"tp_size={tp_size} model_service={self.use_model_service}"
+        )
         self._init_shared_memory()
 
         # Global vars
@@ -253,6 +260,17 @@ class BaseModelRunner:
 
     def delete_gpu_model(self):
         if hasattr(self, "model") and self.model is not None:
+            # Tell the model service to drop its reference first. It may hold
+            # this model's weights as a migration source, and a reference held
+            # across process boundaries would keep the memory alive after the
+            # engine has released it.
+            import os as _os
+            if _os.environ.get("PRISM_V4_P2P_MIGRATION") == "1" and self.input_queue is not None:
+                try:
+                    self.input_queue.put(
+                        ("__release__", self.model_path, self.gpu_id, None))
+                except Exception:
+                    pass
             del self.model
             self.model = None
 
diff --git a/python/sglang/srt/redis_utils.py b/python/sglang/srt/redis_utils.py
index ddd7bd50973334fbd638c73303f5132157c58719..650c1c247d871773473acd53fa85e210898eb2db 100644
--- a/python/sglang/srt/redis_utils.py
+++ b/python/sglang/srt/redis_utils.py
@@ -78,6 +78,29 @@ class RedisClient:
     def get_queue_length(self, key):
         return self.client.llen(key)
 
+    def set_int(self, key, value):
+        self.client.set(key, int(value))
+
+    def set_int_if_absent(self, key, value):
+        return bool(self.client.set(key, int(value), nx=True))
+
+    def get_int(self, key):
+        value = self.client.get(key)
+        return int(value) if value is not None else None
+
+    def compare_and_advance_int(self, key, expected):
+        """Atomically advance an integer sequence iff it equals `expected`."""
+        script = """
+        local current = tonumber(redis.call('GET', KEYS[1]))
+        local expected = tonumber(ARGV[1])
+        if current == expected then
+            redis.call('SET', KEYS[1], expected + 1)
+            return 1
+        end
+        return 0
+        """
+        return bool(self.client.eval(script, 1, key, int(expected)))
+
     def recv_pyobj_block(self, key):
         _, obj_bytes = self.client.blpop(key)
         return pickle.loads(obj_bytes)
diff --git a/python/sglang/srt/server_args.py b/python/sglang/srt/server_args.py
index 5c710da131051fbb70f254cad562221b3cc33ac1..8c811a39f5d49a82283a9b45a2f1d7b3f6e8d078 100644
--- a/python/sglang/srt/server_args.py
+++ b/python/sglang/srt/server_args.py
@@ -154,6 +154,9 @@ class ServerArgs:
     frontend_generate_request_key_prefix: Optional[str] = None
     backend_generate_request_key_prefix: Optional[str] = None
     engine_to_gpu_scheduler_key_prefix: Optional[str] = None
+    # Internal worker-pool runtime gate for paper Algorithm 2.  This is copied
+    # from MultiModelServerArgs and is intentionally not a standalone policy.
+    enable_moore_hodgson: bool = False
 
     def __post_init__(self):
         # Set missing default values
@@ -269,6 +272,19 @@ class ServerArgs:
             "policy",
             "enable_model_service",
             "num_model_service_workers",
+            "prefill_speed_file",
+            "slo_base_file",
+            "kvpr_tau",
+            "kvpr_rate_window",
+            "kvpr_migration_cooldown",
+            "kvpr_tpot_slo_scale",
+            "parallel_model_loading",
+            "enable_tp_worker_pool",   # PAPER-FAITHFUL-TP
+            "tp_max_groups",
+            "enable_tp_anti_affinity",
+            "enable_tp_anti_affinity_strict",
+            "overlap_migration",
+            "enable_kv_migration",   # PAPER-FAITHFUL-V6
         }
         # remove keys to remove in multi_model_server_args
         multi_model_server_args = vars(multi_model_server_args).copy()
diff --git a/python/sglang/multi_model/kv_migration_v6.py b/python/sglang/multi_model/kv_migration_v6.py
new file mode 100644
index 0000000..58d86a0
--- /dev/null
+++ b/python/sglang/multi_model/kv_migration_v6.py
@@ -0,0 +1,351 @@
+"""KV-cache migration (paper §5.3) -- transport and request capsule.
+
+The paper migrates a model's KV cache with the model.  The released prototype
+does not, and the reason is not a missing transfer: it is that there is nothing
+to transfer at the moment it tears a model down.
+
+Every migration path emits ``DeactivateAction(preempt=False, ...)``
+(``simple_global.py:822,918``; the paper-faithful policies subclass that class
+and override only ``_find_optimal_migrations``, so they inherit it).  With
+``preempt=False`` the scheduler runs ``_run_to_completion_normal`` and drains
+the in-flight batch before teardown, so the KV is consumed rather than dropped.
+That is a coherent design -- it avoids needing KV migration by paying a longer
+teardown -- but it is a *different* design from the paper's, which retracts
+in-flight requests and moves their KV so the target can resume decoding.
+
+Turning that on means switching the migration path to
+``preempt=True, preempt_mode=RECOMPUTE`` so ``_retract_running_batch_normal``
+runs, and then moving what it would otherwise free.  Both live behind
+``--enable-kv-migration``; with the flag off this module is never entered and
+the default arm keeps reproducing the released prototype.
+
+Layout, and why a gather is not an optimisation but a requirement
+----------------------------------------------------------------
+kvcached hands SGLang one virtually-contiguous ``FTensor`` per layer;
+``k_buffer[l]`` and ``v_buffer[l]`` are its two halves, each shaped
+``[num_tokens, head_num, head_dim]`` (``kvcached/ops.py:81-107``).  A request's
+tokens occupy *scattered* slot indices -- ``req_to_token[req_pool_idx, :n]`` --
+and slot ``i`` sits on page ``i * cell_size // 2MiB``
+(``slab_allocator.py:483``), a page it generally shares with other requests.
+So a request's KV is never a contiguous range and must be gathered whichever
+way it is then moved.
+
+That gather is also what makes the transfer legal.  ``csrc/page.cpp`` maps each
+2 MiB page with ``cuMemSetAccess`` for the owning device only, so a peer read of
+a source ``FTensor`` should fault.  It does not, because ``k_buffer[l][slots]``
+is advanced indexing with the index tensor on the source device: the gather
+executes *there* and materialises a temporary in the ordinary caching
+allocator, and only that temporary crosses the link.  Measured on this box in
+``exp/results/paper-faithful-v6/kv-probe/``.  No kvcached change is needed.
+
+What the target cannot inherit
+------------------------------
+``req_pool_idx``, ``prefix_indices`` and ``last_node`` are indices into the
+*source* engine's pools and are meaningless on the target; the target
+re-allocates its own and rebuilds ``req_to_token`` from them.  The KV bytes are
+positional, so they survive the renumbering as long as token order is kept --
+which is why the capsule stores the gathered tensors in token order rather than
+storing slot ids.
+
+``output_ids`` is the only record of decode progress, and the prototype's
+existing serialiser drops it: ``_convert_req_to_frontend_reqs``
+(``scheduler.py:1772-1824``) rebuilds a ``GenerateReqInput`` from
+``origin_input_ids`` alone and hardcodes ``output_len=512``.  Reusing it would
+silently restart every migrated request from its prompt, which is exactly the
+recomputation this mechanism exists to avoid, so the capsule carries its own
+fields.
+
+Every migration appends one ``[PAPER-KV-V6]`` JSON record, so ``kv_bytes`` in
+the migration CSVs comes from measurement rather than staying a hardcoded 0.
+"""
+
+import json
+import logging
+import os
+import threading
+import time
+
+import torch
+
+logger = logging.getLogger(__name__)
+
+TRACE_PATH = os.environ.get("PRISM_V6_KV_TRACE", "")
+ENABLED = os.environ.get("PRISM_V6_KV_MIGRATION", "0") == "1"
+# Cap what one migration may carry.  A retracted batch can be large, and the
+# point of the mechanism is to save recomputation, not to move an unbounded
+# amount of memory while both GPUs wait.  Requests past the cap fall back to
+# the prototype's behaviour (recompute), and the record says how many did.
+MAX_TOKENS = int(os.environ.get("PRISM_V6_KV_MAX_TOKENS", "65536"))
+
+_trace_lock = threading.Lock()
+
+
+def _emit(record):
+    logger.info("[PAPER-KV-V6] " + json.dumps(record, default=str))
+    if TRACE_PATH:
+        try:
+            with _trace_lock, open(TRACE_PATH, "a") as fh:
+                fh.write(json.dumps(record, default=str) + "\n")
+        except OSError:
+            pass
+
+
+class RequestKVCapsule:
+    """One retracted request: its scheduler state and its KV, in token order.
+
+    Deliberately not a ``Req``.  A ``Req`` carries tokenizer handles, radix-tree
+    nodes and source-pool indices that cannot cross a process boundary or would
+    be wrong on the target; this carries only what the target needs to rebuild
+    one.
+    """
+
+    __slots__ = ("rid", "model_name", "origin_input_ids", "output_ids",
+                 "sampling_params", "arrival_time", "slo", "k", "v",
+                 "num_tokens", "source_gpu", "request_state")
+
+    def __init__(self, rid, model_name, origin_input_ids, output_ids,
+                 sampling_params, arrival_time, slo, k, v, source_gpu,
+                 request_state=None):
+        self.rid = rid
+        self.model_name = model_name
+        self.origin_input_ids = list(origin_input_ids)
+        self.output_ids = list(output_ids)
+        self.sampling_params = sampling_params
+        self.arrival_time = arrival_time
+        self.slo = slo
+        self.k = k                      # list[layer] -> [n, head_num, head_dim]
+        self.v = v
+        self.num_tokens = k[0].shape[0] if k else 0
+        self.source_gpu = source_gpu
+        self.request_state = dict(request_state or {})
+
+    @property
+    def nbytes(self):
+        return sum(t.numel() * t.element_size() for t in self.k) + \
+               sum(t.numel() * t.element_size() for t in self.v)
+
+    def __repr__(self):
+        return (f"RequestKVCapsule(rid={self.rid}, tokens={self.num_tokens}, "
+                f"layers={len(self.k)}, bytes={self.nbytes})")
+
+
+def gather_request_kv(k_buffer, v_buffer, slots, device):
+    """Pull one request's KV off the source GPU, in token order.
+
+    ``slots`` is ``req_to_token[req_pool_idx, :n]`` -- the ordered slot index per
+    token.  Runs on the source device so the VMM pages are only ever read
+    locally (see the module docstring).
+    """
+    if len(slots) == 0:
+        return [], []
+    idx = torch.as_tensor(slots, dtype=torch.long, device=device)
+    # .clone() so the result owns its storage: the caller is about to free the
+    # slots these were read from.
+    k = [k_buffer[i][idx].clone() for i in range(len(k_buffer))]
+    v = [v_buffer[i][idx].clone() for i in range(len(v_buffer))]
+    return k, v
+
+
+def clone_capsules_for_relay(capsules):
+    """Make a relay process the owner of received CUDA tensor storage.
+
+    PyTorch intentionally refuses to send a CUDA tensor that the sending
+    process itself received over multiprocessing IPC.  The model service is a
+    relay (source engine -> service -> target engine), so it must first clone
+    the received tensors into storage it owns.  Mutating the capsules also
+    drops the service's references to the source-owned IPC mappings.
+    """
+    devices = set()
+    for capsule in capsules:
+        capsule.k = [tensor.clone() for tensor in capsule.k]
+        capsule.v = [tensor.clone() for tensor in capsule.v]
+        devices.update(tensor.device.index for tensor in capsule.k + capsule.v)
+    for device in devices:
+        torch.cuda.synchronize(device)
+    return capsules
+
+
+def transfer_capsule(capsule, target_gpu, stream=None):
+    """Move a capsule's tensors to ``target_gpu``.
+
+    Peer-to-peer when the pair allows it, otherwise through the host -- the
+    same fallback shape as v4's weight path, so a PCIe-only box degrades
+    instead of failing.
+    """
+    if capsule.num_tokens == 0:
+        return capsule, "empty"
+    src = capsule.source_gpu
+    peer = bool(torch.cuda.can_device_access_peer(src, target_gpu)) \
+        if src != target_gpu else True
+    path = "gpu-to-gpu-p2p" if peer else "via-host"
+
+    def _move(t):
+        if peer:
+            return t.to(f"cuda:{target_gpu}", non_blocking=True)
+        return t.cpu().to(f"cuda:{target_gpu}", non_blocking=True)
+
+    ctx = torch.cuda.stream(stream) if stream is not None else _NullCtx()
+    with ctx:
+        capsule.k = [_move(t) for t in capsule.k]
+        capsule.v = [_move(t) for t in capsule.v]
+    if stream is not None:
+        stream.synchronize()
+    else:
+        torch.cuda.synchronize(target_gpu)
+    capsule.source_gpu = target_gpu
+    return capsule, path
+
+
+def scatter_request_kv(k_buffer, v_buffer, slots, capsule, device):
+    """Write a capsule's KV into freshly allocated slots on the target.
+
+    ``slots`` come from the *target's* allocator and bear no relation to the
+    source's numbering; token order is what preserves correctness.
+    """
+    if capsule.num_tokens == 0:
+        return
+    if len(slots) != capsule.num_tokens:
+        raise ValueError(
+            f"slot count {len(slots)} != capsule tokens {capsule.num_tokens}")
+    idx = torch.as_tensor(slots, dtype=torch.long, device=device)
+    for i in range(len(k_buffer)):
+        k_buffer[i][idx] = capsule.k[i]
+        v_buffer[i][idx] = capsule.v[i]
+
+
+def _restore_request_state(req, capsule):
+    for name, value in capsule.request_state.items():
+        setattr(req, name, value)
+
+
+def build_resumed_request(req_cls, capsule, slots, tokenizer):
+    """Rebuild a request on the target and attach its migrated KV slots.
+
+    ``BatchRetractDecodeReq`` carries queue-accounting metadata only; it does
+    not requeue a ``GenerateReqInput``.  Therefore the capsule, rather than a
+    future frontend request, is the authoritative request body.  The target
+    computes one final token while treating all migrated tokens as its cached
+    prefix.
+    """
+    if len(slots) != capsule.num_tokens:
+        raise ValueError(
+            f"slot count {len(slots)} != capsule tokens {capsule.num_tokens}")
+
+    origin_input_ids = list(capsule.origin_input_ids)
+    output_ids = list(capsule.output_ids)
+    fill_ids = origin_input_ids + output_ids
+    expected_tokens = len(fill_ids) - 1
+    if expected_tokens <= 0 or capsule.num_tokens != expected_tokens:
+        raise ValueError(
+            f"capsule tokens {capsule.num_tokens} != resumable prefix "
+            f"{expected_tokens} for {capsule.rid!r}")
+
+    state = capsule.request_state
+    req = req_cls(
+        capsule.rid,
+        state.get("origin_input_text", ""),
+        origin_input_ids,
+        capsule.sampling_params,
+        lora_path=state.get("lora_path"),
+        arrival_time=capsule.arrival_time,
+        slo=capsule.slo,
+    )
+    req.tokenizer = tokenizer
+    _restore_request_state(req, capsule)
+    req.origin_input_ids = origin_input_ids
+    req.origin_input_ids_unpadded = list(origin_input_ids)
+    req.output_ids = output_ids
+    req.fill_ids = fill_ids
+    req.sampling_params = capsule.sampling_params
+    req.arrival_time = capsule.arrival_time
+    req.slo = capsule.slo
+
+    # These slots belong to the target engine.  Source-pool indices and radix
+    # nodes are deliberately never transferred.
+    req.req_pool_idx = None
+    req.prefix_indices = slots
+    req.extend_input_len = len(fill_ids) - len(slots)
+    req.last_node = None
+    req.cached_tokens = 0
+    if req.extend_input_len != 1:
+        raise ValueError(
+            f"resume extend length {req.extend_input_len} != 1 for {capsule.rid!r}")
+    return req
+
+
+def build_recomputed_request(req_cls, capsule, tokenizer):
+    """Rebuild a migrated request without KV when transfer cannot be used."""
+    origin_input_ids = list(capsule.origin_input_ids)
+    state = capsule.request_state
+    req = req_cls(
+        capsule.rid,
+        state.get("origin_input_text", ""),
+        origin_input_ids,
+        capsule.sampling_params,
+        lora_path=state.get("lora_path"),
+        arrival_time=capsule.arrival_time,
+        slo=capsule.slo,
+    )
+    req.tokenizer = tokenizer
+    _restore_request_state(req, capsule)
+    req.origin_input_ids = origin_input_ids
+    req.origin_input_ids_unpadded = list(origin_input_ids)
+    req.output_ids = list(capsule.output_ids)
+    req.fill_ids = req.origin_input_ids + req.output_ids
+    req.req_pool_idx = None
+    req.prefix_indices = []
+    req.extend_input_len = len(req.fill_ids)
+    req.last_node = None
+    req.cached_tokens = 0
+    return req
+
+
+def migrate_request_kv(capsules, target_gpu, tag=""):
+    """Transfer a batch of capsules and account for it.
+
+    Returns ``(moved, skipped, record)``.  The cap is enforced here rather than
+    at the call site so the record always reports what was left behind -- a
+    silent truncation would read as "everything migrated".
+    """
+    t0 = time.time()
+    moved, skipped, budget = [], [], MAX_TOKENS
+    for c in capsules:
+        if c.num_tokens <= budget:
+            moved.append(c)
+            budget -= c.num_tokens
+        else:
+            skipped.append(c)
+
+    paths, nbytes = set(), 0
+    for c in moved:
+        nbytes += c.nbytes
+        _, p = transfer_capsule(c, target_gpu)
+        paths.add(p)
+    t1 = time.time()
+    seconds = t1 - t0
+
+    record = {
+        "tag": tag,
+        "start_time": t0,
+        "end_time": t1,
+        "target_gpu": target_gpu,
+        "requests_moved": len(moved),
+        "requests_skipped_over_cap": len(skipped),
+        "tokens_moved": sum(c.num_tokens for c in moved),
+        "tokens_skipped": sum(c.num_tokens for c in skipped),
+        "kv_bytes": nbytes,
+        "seconds": round(seconds, 6),
+        "kv_gbps": round(nbytes / seconds / 1e9, 3) if seconds > 0 else None,
+        "transfer_path": "+".join(sorted(paths)) if paths else "none",
+        "max_tokens_cap": MAX_TOKENS,
+    }
+    _emit(record)
+    return moved, skipped, record
+
+
+class _NullCtx:
+    def __enter__(self):
+        return None
+
+    def __exit__(self, *a):
+        return False
diff --git a/python/sglang/multi_model/parallel_loading_v4.py b/python/sglang/multi_model/parallel_loading_v4.py
new file mode 100644
index 0000000..c46e342
--- /dev/null
+++ b/python/sglang/multi_model/parallel_loading_v4.py
@@ -0,0 +1,319 @@
+"""Instrumented parallel model weight loading (paper §5.3).
+
+The released prototype already contains the paper's mechanism, in
+``model_sevice.multi_thread_copy_model_to_gpu``: every weight tensor is cut
+into ``max_threads`` slices and slice ``b`` is driven by *broker* GPU
+``(b + target + 1) % num_gpus``.  A slice whose broker is the target is a plain
+host->device copy; a slice whose broker is another GPU is staged into that
+helper GPU and then pulled across the GPU-GPU link.  Two host->device engines
+feed the target instead of one.
+
+Measuring it exposed where the prototype leaves throughput behind.  Both legs
+of the helper path -- host->helper, then helper->target -- are issued on the
+*same* CUDA stream, so the NVLink hop cannot start until the whole PCIe leg has
+landed.  The helper's two links run one after the other, and the second
+host->device engine buys 1.36x rather than the ~2x its bandwidth implies.
+
+Measuring it a second way exposed the larger loss.  The prototype holds its
+master weights in ``share_memory()`` host pages, which are *pageable*: CUDA
+cannot DMA out of them, so every host->device copy is staged synchronously
+through the driver's own bounce buffer and ``non_blocking=True`` is silently
+ignored.  Page-locking that same shared mapping in place with
+``cudaHostRegister`` -- no copy, no extra host RAM, paid once at startup --
+lifts a single link from 11.7 to 26.1 GB/s on this box, and is what makes the
+overlap above possible at all.
+
+Policies:
+
+``direct``  every slice is a host->device copy to the target.  One PCIe link,
+            no helper GPU, no GPU-GPU hop: the sequential baseline.
+``paper``   the released prototype's formula, unchanged, serialised legs and
+            all.  This is what exp/paper-faithful-v3 actually ran.
+``v4``      identical byte split and identical broker assignment, but each
+            helper slice is cut into ``pipeline_depth`` sub-chunks issued
+            across two helper streams joined by events, so the NVLink hop for
+            sub-chunk k overlaps the PCIe leg of sub-chunk k+1.
+
+Overlap only pays once the host pages are locked, so the two mechanisms are
+reported both together and separately -- see ``register_host_memory``.
+
+Every transfer appends one ``[PAPER-LOAD-V4]`` JSON record with per-path byte
+counts, wall time and bandwidth.
+"""
+
+import json
+import logging
+import os
+import threading
+import time
+
+import torch
+
+logger = logging.getLogger(__name__)
+
+BROKER_POLICY = os.environ.get("PRISM_V4_BROKER_POLICY", "paper")
+LOADING_SPLIT = int(os.environ.get("PRISM_V4_LOADING_SPLIT", "0"))
+PIPELINE_DEPTH = int(os.environ.get("PRISM_V4_PIPELINE_DEPTH", "4"))
+TRACE_PATH = os.environ.get("PRISM_V4_LOAD_TRACE", "")
+
+_trace_lock = threading.Lock()
+
+
+def _emit(record):
+    logger.info("[PAPER-LOAD-V4] " + json.dumps(record, default=str))
+    if TRACE_PATH:
+        try:
+            with _trace_lock, open(TRACE_PATH, "a") as fh:
+                fh.write(json.dumps(record, default=str) + "\n")
+        except OSError:
+            pass
+
+
+def enable_peer_access(gpu_ids):
+    """Report, per ordered pair, whether direct peer access exists.
+
+    A cross-device ``copy_`` still succeeds without it, but the driver stages
+    it through a host bounce buffer -- the very thing the design avoids.  The
+    report states the measured path rather than assuming NVLink.
+    """
+    matrix = {}
+    for src in gpu_ids:
+        for dst in gpu_ids:
+            if src == dst:
+                continue
+            try:
+                matrix[f"{src}->{dst}"] = bool(torch.cuda.can_device_access_peer(src, dst))
+            except Exception:
+                matrix[f"{src}->{dst}"] = False
+    return matrix
+
+
+_registered = set()
+_register_lock = threading.Lock()
+
+
+def register_host_memory(state_dict):
+    """Page-lock an existing shared host mapping in place.
+
+    ``pin_memory()`` would allocate a second copy of every weight; the master
+    copy is shared across engine processes and duplicating it is not an option.
+    ``cudaHostRegister`` locks the pages that are already there, so the cost is
+    one pass at startup and no additional host memory.
+
+    Returns a summary rather than raising: a tensor that cannot be registered
+    still transfers correctly, only slower, and the report should say how many
+    were locked instead of the run dying.
+    """
+    cudart = torch.cuda.cudart()
+    locked = failed = already = 0
+    t0 = time.perf_counter()
+    for tensor in state_dict.values():
+        ptr = tensor.data_ptr()
+        nbytes = tensor.numel() * tensor.element_size()
+        if not nbytes:
+            continue
+        with _register_lock:
+            if ptr in _registered:
+                already += 1
+                continue
+            try:
+                rc = int(cudart.cudaHostRegister(ptr, nbytes, 0))
+            except Exception:
+                rc = -1
+            if rc == 0:
+                _registered.add(ptr)
+                locked += 1
+            else:
+                failed += 1
+    return {
+        "locked": locked,
+        "already_locked": already,
+        "failed": failed,
+        "seconds": time.perf_counter() - t0,
+    }
+
+
+class StreamPool:
+    """Per-GPU streams.  The helper path needs two to overlap its legs."""
+
+    def __init__(self, gpu_ids, per_gpu=3):
+        self.streams = {
+            g: [torch.cuda.Stream(device=f"cuda:{g}") for _ in range(per_gpu)]
+            for g in gpu_ids
+        }
+
+    def get(self, gpu, idx=0):
+        pool = self.streams[gpu]
+        return pool[idx % len(pool)]
+
+    def synchronize(self):
+        for pool in self.streams.values():
+            for stream in pool:
+                stream.synchronize()
+
+
+def _slice_bounds(size, parts):
+    """Split ``size`` rows into ``parts`` contiguous ranges.
+
+    The prototype asserts ``local_size % max_threads == 0``; that assert is why
+    the split cannot be raised past 2.  Spreading the remainder over the first
+    slices keeps every slice non-empty and covers the tensor exactly.
+    """
+    if not size:
+        return [(0, 0)]
+    parts = max(1, min(parts, size))
+    base, extra = divmod(size, parts)
+    bounds, start = [], 0
+    for i in range(parts):
+        end = start + base + (1 if i < extra else 0)
+        if end > start:
+            bounds.append((start, end))
+        start = end
+    return bounds
+
+
+def _broker_for(slice_idx, target_gpu_id, gpu_ids, policy):
+    if policy == "direct" or len(gpu_ids) < 2:
+        return target_gpu_id
+    return gpu_ids[(slice_idx + gpu_ids.index(target_gpu_id) + 1) % len(gpu_ids)]
+
+
+def _copy_direct(dst, src, stream, non_blocking=True):
+    with torch.cuda.stream(stream):
+        dst.copy_(src, non_blocking=non_blocking)
+
+
+def _copy_via_helper_serial(dst, src, helper, stream):
+    """The prototype's helper path: both legs on one stream, in order."""
+    with torch.cuda.stream(stream):
+        staged = src.to(f"cuda:{helper}", non_blocking=True)
+        dst.copy_(staged, non_blocking=True)
+
+
+def _copy_via_helper_pipelined(dst, src, helper, pool, depth):
+    """Overlap the PCIe leg and the NVLink leg across sub-chunks."""
+    s_h2d = pool.get(helper, 0)
+    s_p2p = pool.get(helper, 1)
+    rows = src.shape[0] if src.dim() else 0
+    chunks = _slice_bounds(rows, depth) if rows else [(0, 0)]
+    for start, end in chunks:
+        sub_src = src[start:end] if rows else src
+        sub_dst = dst[start:end] if rows else dst
+        with torch.cuda.stream(s_h2d):
+            staged = sub_src.to(f"cuda:{helper}", non_blocking=True)
+            event = torch.cuda.Event()
+            event.record(s_h2d)
+        with torch.cuda.stream(s_p2p):
+            s_p2p.wait_event(event)
+            sub_dst.copy_(staged, non_blocking=True)
+            # The staging buffer is freed by the allocator only after the
+            # consuming stream is done with it.
+            staged.record_stream(s_p2p)
+
+
+def copy_model_to_gpu_v4(
+    cpu_state_dict,
+    gpu_state_dict,
+    target_gpu_id,
+    executor,
+    gpu_ids,
+    pool,
+    policy=None,
+    split=None,
+    pipeline_depth=None,
+    source_state_dict=None,
+    host_registered=None,
+    tag="",
+):
+    """Fill ``gpu_state_dict`` on ``target_gpu_id`` and record how it got there.
+
+    ``source_state_dict`` -- a live copy of the same model resident on another
+    GPU -- makes this a pure device-to-device transfer with the host out of the
+    loop entirely.  That is the migration path; ``cpu_state_dict`` is the
+    cold-start path.
+    """
+    policy = policy or BROKER_POLICY
+    split = split or LOADING_SPLIT or min(4, len(gpu_ids))
+    depth = pipeline_depth or PIPELINE_DEPTH
+    wall_start = time.time()
+    t0 = time.perf_counter()
+
+    counts = {"h2d_direct": 0, "h2d_helper": 0, "p2p": 0}
+    slices = 0
+    futures = []
+
+    def _plan(dst_t, src_t, broker, device_src):
+        if device_src:
+            # Source already on a GPU: one hop over the GPU-GPU link.
+            _copy_direct(dst_t, src_t, pool.get(target_gpu_id, 0))
+        elif broker == target_gpu_id:
+            _copy_direct(dst_t, src_t, pool.get(target_gpu_id, 0))
+        elif policy == "v4":
+            _copy_via_helper_pipelined(dst_t, src_t, broker, pool, depth)
+        else:
+            _copy_via_helper_serial(dst_t, src_t, broker, pool.get(broker, 0))
+
+    for key, gpu_tensor in gpu_state_dict.items():
+        device_src = source_state_dict is not None
+        src_full = source_state_dict[key] if device_src else cpu_state_dict[key]
+        rows = gpu_tensor.shape[0] if gpu_tensor.dim() else 0
+        itemsize = gpu_tensor.element_size()
+        per_row = (gpu_tensor.numel() // rows * itemsize) if rows else gpu_tensor.numel() * itemsize
+
+        if not rows:
+            futures.append(executor.submit(_plan, gpu_tensor, src_full, target_gpu_id, device_src))
+            counts["p2p" if device_src else "h2d_direct"] += gpu_tensor.numel() * itemsize
+            slices += 1
+            continue
+
+        for idx, (start, end) in enumerate(_slice_bounds(rows, split)):
+            broker = _broker_for(idx, target_gpu_id, gpu_ids, policy)
+            nbytes = (end - start) * per_row
+            futures.append(
+                executor.submit(_plan, gpu_tensor[start:end], src_full[start:end], broker, device_src)
+            )
+            if device_src:
+                counts["p2p"] += nbytes
+            elif broker == target_gpu_id:
+                counts["h2d_direct"] += nbytes
+            else:
+                # Crosses PCIe once and the GPU-GPU link once.
+                counts["h2d_helper"] += nbytes
+                counts["p2p"] += nbytes
+            slices += 1
+
+    for future in futures:
+        future.result()
+    pool.synchronize()
+    torch.cuda.synchronize(target_gpu_id)
+    elapsed = time.perf_counter() - t0
+    wall_end = time.time()
+
+    payload = counts["p2p"] if source_state_dict is not None else (
+        counts["h2d_direct"] + counts["h2d_helper"])
+    record = {
+        "tag": tag,
+        "start_time": wall_start,
+        "end_time": wall_end,
+        "policy": policy,
+        "split": split,
+        "pipeline_depth": depth if policy == "v4" else None,
+        "target_gpu": target_gpu_id,
+        "gpu_ids": list(gpu_ids),
+        "source": "gpu" if source_state_dict is not None else "cpu",
+        "host_registered": host_registered,
+        "slices": slices,
+        "payload_bytes": payload,
+        "bytes_h2d_direct": counts["h2d_direct"],
+        "bytes_h2d_helper": counts["h2d_helper"],
+        "bytes_p2p": counts["p2p"],
+        "seconds": elapsed,
+        "payload_gbps": (payload / elapsed / 1e9) if elapsed > 0 else 0.0,
+        "transfer_path": (
+            "gpu-to-gpu-p2p" if source_state_dict is not None
+            else "host-to-device-only" if counts["h2d_helper"] == 0
+            else "host-to-device + helper-gpu-p2p"
+        ),
+    }
+    _emit(record)
+    return record
diff --git a/python/sglang/multi_model/scheduling/action_order_v6.py b/python/sglang/multi_model/scheduling/action_order_v6.py
new file mode 100644
index 0000000..3101e94
--- /dev/null
+++ b/python/sglang/multi_model/scheduling/action_order_v6.py
@@ -0,0 +1,65 @@
+"""Action batching for two-phase overlap and KV migration.
+
+The target is prepared while the source is still serving.  Only after that
+readiness barrier does the source quiesce and stash its latest KV; a final
+commit fetches/injects the capsules and exposes the target to routing.
+
+This helper is independent of SGLang so the ordering contract can be tested
+without importing the controller and its runtime dependencies.
+"""
+
+import copy
+
+
+def _with_phase(action, phase):
+    clone = copy.copy(action)
+    clone.phase = phase
+    return clone
+
+
+def build_action_batches(
+    actions,
+    overlap_migration,
+    kv_migration,
+    activate_type,
+    deactivate_type,
+):
+    """Return ``(batch, worker_count)`` pairs in dependency-safe order."""
+    if not overlap_migration:
+        return [(actions, len(actions))]
+
+    activations = [a for a in actions if isinstance(a, activate_type)]
+    middle = [
+        a for a in actions
+        if not isinstance(a, (activate_type, deactivate_type))
+    ]
+    deactivations = [a for a in actions if isinstance(a, deactivate_type)]
+
+    # Only an activation paired with a deactivation of the same model is a
+    # migration.  Demand-loading an inactive model has no source KV to fetch
+    # and must keep the legacy one-shot activation path.
+    deactivating_models = {a.model_name for a in deactivations}
+    migrating = [a for a in activations if a.model_name in deactivating_models]
+    ordinary = [a for a in activations if a.model_name not in deactivating_models]
+
+    # The HTTP control path cannot safely process two concurrent activations
+    # for the same GPU, so activation stages remain serialized.  prepare and
+    # commit are shallow copies: the policy's original action stays reusable
+    # for diagnostics and the phase is explicit in every action trace.
+    if kv_migration and migrating:
+        prepares = [_with_phase(a, "prepare") for a in migrating]
+        commits = [_with_phase(a, "commit") for a in migrating]
+        ordered = (
+            (ordinary, 1),
+            (prepares, 1),
+            (middle, len(middle)),
+            (deactivations, len(deactivations)),
+            (commits, 1),
+        )
+    else:
+        ordered = (
+            (activations, 1),
+            (middle, len(middle)),
+            (deactivations, len(deactivations)),
+        )
+    return [(batch, workers) for batch, workers in ordered if batch]
diff --git a/python/sglang/multi_model/scheduling/gpu/moore_hodgson.py b/python/sglang/multi_model/scheduling/gpu/moore_hodgson.py
new file mode 100644
index 0000000..6dd1adb
--- /dev/null
+++ b/python/sglang/multi_model/scheduling/gpu/moore_hodgson.py
@@ -0,0 +1,85 @@
+"""Paper Algorithm 2 -- GPU-Local Request Scheduling (Moore-Hodgson).
+
+Transcribed from *Prism: Cost-Efficient Multi-LLM Serving via GPU Memory
+Ballooning*, Algorithm 2, verbatim:
+
+    1: sort R ascending by deadline d_i = a_i + s_i
+    2: S <- {}, current_time <- Timer.time()
+    3: for k = 1..n:
+    4:     r <- r_k, e_r <- p_r / c_r
+    5:     append r to S
+    6:     current_time <- current_time + e_r
+    7:     if current_time > a_r + s_r:
+    9:         r_max <- argmax_{r' in S} p_r' / c_r'
+   10:         remove r_max from S
+   11:         current_time <- current_time - p_rmax / c_rmax
+   12: return S
+
+Notes on fidelity, kept here because they matter for interpreting results:
+
+* Line 2 initialises the clock to *wall time*, and line 6 accumulates
+  ``e_r`` for every accepted request.  This is the classic single-machine
+  ``1||sum U_j`` model: one job at a time.  The paper justifies it in its
+  analysis paragraph ("prefill completion time ... d_ri = a_ri + sum p/c"),
+  which is exact only if the engine runs one chunked-prefill stream whose
+  aggregate token throughput is ``c``.  We keep it literal.  ``c_i`` must
+  therefore be the model's *aggregate chunked-prefill throughput*, not a
+  per-request latency reciprocal -- see exp/scripts/profile_prefill_speed_v2.py.
+* The paper does not say what to do with the requests Moore-Hodgson excludes.
+  This module returns them; the policy decision lives in request_queue.py.
+"""
+
+import dataclasses
+from typing import Iterable, List, Sequence, Tuple
+
+
+@dataclasses.dataclass
+class MHJob:
+    """One request as Algorithm 2 sees it.
+
+    key       opaque identifier handed back to the caller
+    deadline  absolute d_i = a_i + s_i (seconds, same clock as `now`)
+    exec_time e_i = p_i / c_i (seconds)
+    payload   caller's object, untouched
+    """
+
+    key: object
+    deadline: float
+    exec_time: float
+    payload: object = None
+
+
+def is_feasible(jobs: Sequence[MHJob], now: float) -> bool:
+    """True if every job in `jobs` meets its deadline when run back-to-back
+    in ascending-deadline order starting at `now`."""
+    clock = now
+    for j in sorted(jobs, key=lambda j: j.deadline):
+        clock += j.exec_time
+        if clock > j.deadline:
+            return False
+    return True
+
+
+def select(jobs: Iterable[MHJob], now: float) -> Tuple[List[MHJob], List[MHJob]]:
+    """Algorithm 2.
+
+    Returns ``(selected, deferred)``.  ``selected`` is in dispatch order
+    (ascending deadline) and is guaranteed feasible from ``now``.
+    ``deferred`` holds everything Moore-Hodgson shed, in the order it was shed.
+    """
+    ordered = sorted(jobs, key=lambda j: j.deadline)
+    selected: List[MHJob] = []
+    deferred: List[MHJob] = []
+    clock = float(now)
+
+    for job in ordered:
+        selected.append(job)                    # line 5
+        clock += job.exec_time                  # line 6
+        if clock > job.deadline:                # line 7
+            # line 9: pop the request with the longest execution time
+            worst = max(range(len(selected)), key=lambda i: selected[i].exec_time)
+            dropped = selected.pop(worst)       # line 10
+            clock -= dropped.exec_time          # line 11
+            deferred.append(dropped)
+
+    return selected, deferred
diff --git a/python/sglang/multi_model/scheduling/gpu/request_queue_mh.py b/python/sglang/multi_model/scheduling/gpu/request_queue_mh.py
new file mode 100644
index 0000000..c9315e7
--- /dev/null
+++ b/python/sglang/multi_model/scheduling/gpu/request_queue_mh.py
@@ -0,0 +1,276 @@
+"""Moore-Hodgson (paper Algorithm 2) branch for RequestQueue.
+
+Injected into
+``sglang/multi_model/scheduling/gpu/request_queue.py`` by apply_patches.py.
+The prototype's own ``admission_control`` is untouched and still runs whenever
+``--enable-moore-hodgson`` is absent, so the released-prototype arm is bit-for-bit
+the upstream path.
+
+--------------------------------------------------------------------------
+ASSUMPTION / OUR IMPLEMENTATION DECISION
+--------------------------------------------------------------------------
+B1. The paper does not say what happens to the requests Algorithm 2 sheds.
+    Moore-Hodgson minimises the NUMBER of late jobs on the premise that every
+    job still runs -- late ones simply run after the on-time ones.  So:
+      * deferred with d_i >  now  -> requeued (real backpressure, retried next
+        round with its original a_i / d_i)
+      * deferred with d_i <= now  -> dispatched after S at lowest priority.
+    Holding the second class back is a livelock: the verdict is stable across
+    rounds, so they are shed forever (observed in the v1 sweep: 328k rounds
+    with the client blocked).  Regression-tested by test_moore_hodgson.py #9.
+B2. c_i is the model's aggregate chunked-prefill token throughput, read from
+    --prefill-speed-file.  The paper defines it as "a chunked-prefill speed c_i
+    determined by the model that serves it" and its optimality argument assumes
+    a single chunked-prefill stream of that throughput, so c_i is an ENGINE
+    CAPACITY, not the reciprocal of a single request's measured TTFT.
+B3. Every round is scored from scratch over the whole queue, matching
+    Algorithm 2's `Require: a set of n requests R`.
+"""
+
+import json
+import logging
+import os
+import time
+from collections import defaultdict
+from typing import Dict, List
+
+from sglang.multi_model.scheduling.gpu.moore_hodgson import MHJob, select
+
+logger = logging.getLogger(__name__)
+
+DEFAULT_PREFILL_SPEED = 2048.0   # released prototype's implicit constant
+
+
+class MooreHodgsonMixin:
+    # ------------------------------------------------------------ configuration
+    def configure_moore_hodgson(self, enabled: bool, prefill_speed: Dict[str, float],
+                                log_path: str = None):
+        self._mh_enabled = bool(enabled)
+        self._mh_speed = dict(prefill_speed or {})
+        self._mh_round = 0
+        self._mh_last_log = 0.0
+        self._mh_zero_streak = 0
+        self._mh_warned_at = 0.0
+        self._mh_deferred_rids = set()
+        self._mh_diag_requests = os.getenv("PRISM_DIAG_ALG2", "0") == "1"
+        self._mh_request_decision = {}
+        self._mh_stats = defaultdict(float)
+        self._mh_log_path = log_path
+        self._mh_fh = None
+        # The scheduling loop runs at ~100 Hz, so an unbounded per-round log can
+        # reach hundreds of MB on a long high-rate run. Cap the FILE; the
+        # counters in _mh_stats keep accumulating either way, so the aggregate
+        # numbers stay exact and only the round-by-round detail is truncated.
+        self._mh_log_lines = 0
+        self._mh_log_cap = 300_000
+        if log_path:
+            try:
+                self._mh_fh = open(log_path, "a", buffering=1)
+            except OSError as e:                       # pragma: no cover
+                logger.warning(f"[PAPER-ALG2] cannot open {log_path}: {e}")
+        logger.info(
+            f"[PAPER-ALG2] enabled={self._mh_enabled} "
+            f"prefill_speed_entries={len(self._mh_speed)} log={log_path}"
+        )
+
+    def _c_i(self, model_name: str) -> float:
+        return float(self._mh_speed.get(model_name, DEFAULT_PREFILL_SPEED))
+
+    def mh_exec_time(self, req) -> float:
+        """Paper execution cost p_i/c_i for runtime outstanding accounting."""
+        return float(req.prompt_len or 0) / self._c_i(req.model)
+
+    # ------------------------------------------------------------ Algorithm 2
+    def admission_control_mh(
+        self,
+        available_resources: float,
+        model_backend_queue_lens: Dict[str, int],
+        model_states: Dict[str, str],
+        allow_sending_when_activating: bool = False,
+        mh_outstanding_work_s: float = 0.0,
+        mh_dispatch_budget: int = 1,
+    ):
+        import heapq
+
+        # Algorithm 2 produces one global order.  Returning a model-keyed
+        # mapping here destroyed that order (A:m1, B:m2, C:m1 -> A,C,B).
+        # The GPU scheduler's runtime gate consumes this ordered list.
+        admitted = []
+        self._mh_round += 1
+        wall_now = time.time()
+        # Already admitted prefills cannot be removed from the schedule.  The
+        # paper's single-machine completion equation therefore places their
+        # remaining p_i/c_i work before the newly selected subset.
+        now = wall_now + max(0.0, float(mh_outstanding_work_s))
+
+        models_to_skip = {
+            m for m, n in model_backend_queue_lens.items()
+            if n > self._skip_model_threshold
+        }
+
+        with self._lock:
+            queue_len = len(self._queue)
+            if queue_len == 0:
+                return admitted
+
+            eligible, blocked = [], []
+            for w in self._queue:
+                state = model_states.get(w.model_name, "deactivated")
+                if w.model_name in models_to_skip:
+                    blocked.append(w); continue
+                if state in ("deactivating", "deactivated"):
+                    blocked.append(w); continue
+                if state == "activating" and not allow_sending_when_activating:
+                    blocked.append(w); continue
+                eligible.append(w)
+
+            if not eligible:
+                self._queue = blocked
+                heapq.heapify(self._queue)
+                return admitted
+
+            jobs = []
+            for w in eligible:
+                req = w.req
+                plen = req.prompt_len or 0
+                jobs.append(
+                    MHJob(
+                        key=req.rid,
+                        deadline=req.arrival_time + req.slo,   # d_i = a_i + s_i
+                        exec_time=plen / self._c_i(w.model_name),  # e_i = p_i / c_i
+                        payload=w,
+                    )
+                )
+
+            selected, deferred = select(jobs, now)             # Algorithm 2
+
+            late = [j for j in deferred if j.deadline <= wall_now]   # B1
+            requeue = [j for j in deferred if j.deadline > wall_now]
+
+            ordered_dispatch = [j.payload for j in selected] + [j.payload for j in late]
+            # Preserve the global list while staging only the bounded runtime
+            # window supplied by the GPU scheduler. Backend engines enforce
+            # this exact order using the per-GPU alg2_seq barrier.
+            dispatch = ordered_dispatch[:max(0, int(mh_dispatch_budget))]
+            for w in dispatch:
+                admitted.append(w.req)
+                s = self._model_requests.get(w.model_name)
+                if s is not None:
+                    s.discard(w)
+                    if not s:
+                        del self._model_requests[w.model_name]
+
+            dispatched_wrappers = set(dispatch)
+            self._queue = [w for w in self._queue if w not in dispatched_wrappers]
+            heapq.heapify(self._queue)
+
+            est_cost = sum(j.exec_time for j in selected)
+            n_sel, n_def, n_late, n_rq = (
+                len(selected), len(deferred),
+                sum(1 for w in dispatch if any(j.payload is w for j in late)),
+                len(requeue),
+            )
+            new_deferrals = sum(1 for j in deferred if j.key not in self._mh_deferred_rids)
+            self._mh_deferred_rids.update(j.key for j in deferred)
+
+            # Diagnostic-only request lifecycle records.  Emit the first
+            # Algorithm-2 decision and every later transition, rather than one
+            # record per 100 Hz scheduler tick, so the audit is complete but
+            # does not perturb the policy with unbounded logging overhead.
+            decision = {j.key: "selected" for j in selected}
+            decision.update({j.key: "late_waiting" for j in late})
+            decision.update({j.key: "requeued" for j in requeue})
+            for w in dispatch:
+                if any(j.payload is w for j in late):
+                    decision[w.req.rid] = "late_dispatched"
+            changed = []
+            if self._mh_diag_requests:
+                for j in jobs:
+                    state = decision[j.key]
+                    if self._mh_request_decision.get(j.key) != state:
+                        w = j.payload
+                        changed.append({
+                            "rid": j.key,
+                            "model": w.model_name,
+                            "prompt_tokens": int(w.req.prompt_len or 0),
+                            "c_i_tokens_per_s": self._c_i(w.model_name),
+                            "predicted_exec_s": j.exec_time,
+                            "arrival_s": w.req.arrival_time,
+                            "deadline_s": j.deadline,
+                            "decision": state,
+                        })
+                        self._mh_request_decision[j.key] = state
+
+        # ------------------------------------------------------- observability
+        st = self._mh_stats
+        st["rounds"] += 1
+        st["eligible"] += len(eligible)
+        st["selected"] += n_sel
+        st["deferred"] += n_def
+        st["late_dispatched"] += n_late
+        st["requeued"] += n_rq
+        st["max_queue_len"] = max(st["max_queue_len"], queue_len)
+        if n_def:
+            st["rounds_with_deferral"] += 1
+            st["selected_in_deferral_rounds"] += n_sel
+
+        pathological = bool(eligible) and n_sel == 0 and n_late == 0
+        if pathological:
+            self._mh_zero_streak += 1
+            st["pathological_rounds"] += 1
+        else:
+            self._mh_zero_streak = 0
+
+        should_log = (
+            self._mh_fh is not None
+            and self._mh_log_lines < self._mh_log_cap
+            and (n_def or pathological or (wall_now - self._mh_last_log) >= 0.5)
+        )
+        if should_log:
+            self._mh_last_log = wall_now
+            self._mh_log_lines += 1
+            if self._mh_log_lines == self._mh_log_cap:
+                logger.warning(f"[PAPER-ALG2] round log capped at "
+                               f"{self._mh_log_cap} lines; counters continue")
+            rec = {
+                "round": self._mh_round, "t": round(wall_now, 4),
+                "queue_length": queue_len, "eligible_requests": len(eligible),
+                "selected_requests": n_sel, "deferred_requests": n_def,
+                "runtime_dispatched_requests": len(dispatch),
+                "requeued": n_rq, "late_dispatched": n_late,
+                "new_deferrals": new_deferrals,
+                "estimated_execution_cost_s": round(est_cost, 6),
+                "outstanding_execution_cost_s": round(
+                    float(mh_outstanding_work_s), 6
+                ),
+                "earliest_deadline": round(
+                    min((j.deadline for j in jobs), default=0) - wall_now, 4
+                ),
+                "available_kv_gb": round(float(available_resources or 0), 4),
+                "backend_queue_lens": model_backend_queue_lens,
+                "blocked": len(blocked),
+                "pathological": pathological,
+                "zero_streak": self._mh_zero_streak,
+            }
+            self._mh_fh.write(json.dumps(rec) + "\n")
+
+        if self._mh_fh is not None and changed:
+            for item in changed:
+                if self._mh_log_lines >= self._mh_log_cap:
+                    break
+                self._mh_log_lines += 1
+                item.update({"type": "request_decision", "round": self._mh_round,
+                             "decision_time_s": wall_now})
+                self._mh_fh.write(json.dumps(item) + "\n")
+
+        # Loud, rate-limited warning for the exact pathology the brief asks about.
+        if self._mh_zero_streak >= 20 and (wall_now - self._mh_warned_at) > 5.0:
+            self._mh_warned_at = wall_now
+            logger.warning(
+                f"[PAPER-ALG2-WARN] under-admission: {self._mh_zero_streak} consecutive "
+                f"rounds with eligible={len(eligible)} selected=0 late=0 "
+                f"queue_len={queue_len} avail_kv={available_resources}"
+            )
+        if self._mh_round % 2000 == 0:
+            logger.info("[PAPER-ALG2] " + json.dumps({k: v for k, v in st.items()}))
+        return admitted
diff --git a/python/sglang/multi_model/scheduling/overlap_migration.py b/python/sglang/multi_model/scheduling/overlap_migration.py
new file mode 100644
index 0000000..e86aa42
--- /dev/null
+++ b/python/sglang/multi_model/scheduling/overlap_migration.py
@@ -0,0 +1,54 @@
+"""Target-first migration action used by paper-faithful-v3."""
+
+import dataclasses
+import logging
+import time
+
+from sglang.multi_model.scheduling.action import ActivateAction, BaseAction, DeactivateAction
+
+logger = logging.getLogger(__name__)
+
+
+@dataclasses.dataclass
+class OverlapMigrateAction(BaseAction):
+    """Load the target fully, then drain/deactivate the source.
+
+    The worker-pool HTTP handler is patched to return only after the engine's
+    activation/deactivation completion event, so action ordering here is a real
+    readiness barrier rather than merely ordering two HTTP submissions.
+    """
+
+    source_gpu_id: int = 0
+    target_gpu_id: int = 0
+    memory_pool_size: int = 0
+
+    def execute(self, url, model_instance_state_dict, request_timeout=None):
+        started = time.monotonic()
+        activate = ActivateAction(
+            model_name=self.model_name,
+            instance_idx=self.target_gpu_id,
+            memory_pool_size=self.memory_pool_size,
+            gpu_id=self.target_gpu_id,
+        )
+        if not activate.execute(url, model_instance_state_dict, request_timeout):
+            return False
+        ready = time.monotonic()
+        deactivate = DeactivateAction(
+            model_name=self.model_name,
+            instance_idx=self.source_gpu_id,
+            preempt=False,
+            preempt_mode="RECOMPUTE",
+            evict_waiting_requests=False,
+            gpu_id=self.source_gpu_id,
+        )
+        success = deactivate.execute(url, model_instance_state_dict, request_timeout)
+        logger.info(
+            "[PAPER-OVERLAP] model=%s source=%s target=%s target_ready_s=%.3f total_s=%.3f success=%s",
+            self.model_name,
+            self.source_gpu_id,
+            self.target_gpu_id,
+            ready - started,
+            time.monotonic() - started,
+            success,
+        )
+        return success
diff --git a/python/sglang/multi_model/scheduling/policy/kvpr_global.py b/python/sglang/multi_model/scheduling/policy/kvpr_global.py
new file mode 100644
index 0000000..4baf1bd
--- /dev/null
+++ b/python/sglang/multi_model/scheduling/policy/kvpr_global.py
@@ -0,0 +1,317 @@
+"""Paper Algorithm 1 -- Model Placement to Maximize Memory Headroom (KVPR).
+
+Transcribed from *Prism: Cost-Efficient Multi-LLM Serving via GPU Memory
+Ballooning*, Algorithm 1:
+
+    1: sort models by t_j * tz_j / s_j descending
+    2: for i = 1..N:  shared_kv_i <- C ; w_token_rate_i <- 0
+    4: for k = 1..M:
+    6:     best_r, best_idx <- (min, argmin)_i  w_token_rate_i / shared_kv_i
+    7:     current_r <- w_token_rate_{g_k} / shared_kv_{g_k}
+    8:     best_gpu  <- best_idx if current_r - best_r > tau else g_k
+    9:     assign m_k to best_gpu
+   10:     w_token_rate_{best_gpu} += ...
+   11:     shared_kv_{best_gpu}    -= w_k
+
+Terms (paper Require clause):
+    t_j   token rate         tz_j  token size (KV bytes/token)
+    w_j   model weight size  s_j   latency SLO         g_j  current GPU
+
+Only `_find_optimal_migrations` is overridden; idle eviction, model
+activation, action emission and every threshold outside Algorithm 1 are
+inherited unchanged from SimpleGlobalPolicy so that the two arms differ in
+placement policy alone.
+
+--------------------------------------------------------------------------
+ASSUMPTION / OUR IMPLEMENTATION DECISION  (the paper does not fix these)
+--------------------------------------------------------------------------
+A1. tau is dimensionless here.  KVPR carries units of
+    (tokens/s * bytes/token / s) / GiB, so the paper's literal absolute test
+    `current_r - best_r > tau` has no scale-free meaning and the paper gives
+    neither a unit nor a value.  We keep Algorithm 1's greedy pass verbatim
+    and gate the resulting placement change on the RELATIVE reduction of the
+    cluster's PEAK KVPR:
+        (peak_now - peak_after) / peak_now > tau
+    "Bounding the maximum KVPR across the cluster" is the paper's own stated
+    objective for this greedy approximation (Analysis paragraph / App. A.2),
+    so this preserves the intent while making tau comparable across setups.
+A2. Line 10 of the printed algorithm reads `+= r_k / s_k`, which contradicts
+    the line-1 sort key `t_j * tz_j / s_j` (it is a leftover from arXiv v1,
+    where the numerator was a request rate).  We use `t_k * tz_k / s_k`
+    consistently for both the sort key and the accumulator.
+A3. s_j is read as the TPOT SLO: KVPR models memory pressure, and it is TPOT
+    that memory headroom governs (paper Sec. 6.2 Analysis).
+A4. t_j = admitted input tokens/s (sliding window) + decode tokens/s reported
+    by the engine -- "the rate at which the KV cache actually grows".
+    Window length is not specified by the paper; we reuse the prototype's own
+    30 s ModelRequestTracker window so window length is not a confound.
+A5. At most one migration is emitted per scheduling cycle, matching the
+    released prototype, because a migration is stop-the-world here.
+A6. A GPU is never emptied: the upstream launcher only starts a GPU scheduler
+    for GPUs present in the initial placement, so an emptied GPU stays dead
+    for the rest of the run.
+A7. A candidate GPU must have free memory >= the model's weights.
+"""
+
+import json
+import logging
+import time
+from typing import Dict, List, Optional, Tuple
+
+from sglang.multi_model.scheduling.model_queue_tracker import ModelQueueTracker
+from sglang.multi_model.scheduling.policy.simple_global import SimpleGlobalPolicy
+from sglang.multi_model.scheduling.state import ModelInstanceState, ModelState
+
+logger = logging.getLogger(__name__)
+
+
+class KVPRGlobalPolicy(SimpleGlobalPolicy):
+    def __init__(
+        self,
+        num_gpus: int,
+        gpu_mem: float,
+        model_weights_info: Dict[str, Dict[str, float]],
+        workers_per_gpu: int,
+        tau: float = 0.35,
+        rate_window: float = 30.0,
+        migration_cooldown: float = 30.0,
+        tpot_slo_s: Optional[Dict[str, float]] = None,
+    ):
+        super().__init__(num_gpus, gpu_mem, model_weights_info, workers_per_gpu)
+        self.tau = tau
+        self.rate_window = rate_window
+        self.migration_cooldown = migration_cooldown
+        # Default TPOT SLO if the slo-base-file carries no entry for a model.
+        self.tpot_slo_s: Dict[str, float] = dict(tpot_slo_s or {})
+        self._default_tpot_slo_s = 0.05
+        self._last_migration_time = float("-inf")
+        self._cycle = 0
+
+    # ------------------------------------------------------------------ terms
+
+    def _tpot_slo(self, model_name: str) -> float:
+        return self.tpot_slo_s.get(model_name, self._default_tpot_slo_s)
+
+    def _weighted_token_rates(
+        self, model_queues: Dict[str, ModelQueueTracker], model_names: List[str]
+    ) -> Dict[str, Dict[str, float]]:
+        """t_j, tz_j, s_j and w_token_rate_j = t_j * tz_j / s_j per model."""
+        now = time.time()
+        out: Dict[str, Dict[str, float]] = {}
+        for name in model_names:
+            queue = model_queues.get(name)
+            input_tokens = 0.0
+            if queue is not None:
+                for req in queue.received_reqs.values():
+                    if getattr(req, "is_warmup", False):
+                        continue
+                    at = getattr(req, "arrival_time", None)
+                    if at is None or now - at > self.rate_window:
+                        continue
+                    input_tokens += req.prompt_len or 0
+            input_rate = input_tokens / self.rate_window
+            decode_rate = float(getattr(queue, "decode_token_tput", 0.0) or 0.0)
+            token_rate = input_rate + decode_rate
+            token_size = float(self.model_weights_info[name]["cell_size"])
+            slo = self._tpot_slo(name)
+            out[name] = {
+                "input_token_rate": input_rate,
+                "decode_token_rate": decode_rate,
+                "token_rate": token_rate,
+                "token_size": token_size,
+                "tpot_slo_s": slo,
+                "weighted_token_rate": token_rate * token_size / slo,
+            }
+        return out
+
+    def _shared_kv(self, gpu_to_model_mapping: Dict[int, List[str]], gpu_id: int) -> float:
+        """shared_kv_i = C - sum of resident model weights on GPU i."""
+        used = sum(
+            self.model_weights_info[m]["model_size"]
+            for m in gpu_to_model_mapping.get(gpu_id, [])
+        )
+        return self.gpu_mem - used
+
+    def _kvpr(
+        self,
+        gpu_to_model_mapping: Dict[int, List[str]],
+        weights: Dict[str, Dict[str, float]],
+    ) -> Dict[int, float]:
+        """KVPR_i = sum_j w_token_rate_j / shared_kv_i."""
+        out = {}
+        for gpu_id in range(self.num_gpus):
+            agg = sum(
+                weights.get(m, {}).get("weighted_token_rate", 0.0)
+                for m in gpu_to_model_mapping.get(gpu_id, [])
+            )
+            kv = self._shared_kv(gpu_to_model_mapping, gpu_id)
+            out[gpu_id] = agg / kv if kv > 0 else float("inf")
+        return out
+
+    @staticmethod
+    def _peak(kvpr: Dict[int, float]) -> float:
+        return max(kvpr.values()) if kvpr else 0.0
+
+    # ------------------------------------------------------- Algorithm 1 pass
+
+    def _greedy_placement(
+        self,
+        weights: Dict[str, Dict[str, float]],
+        current_gpu: Dict[str, int],
+        gpu_available_memory: Dict[int, float],
+    ) -> Dict[str, int]:
+        """Algorithm 1 lines 1-12 verbatim; returns model -> target GPU."""
+        # line 1: descending w_token_rate (name breaks ties deterministically)
+        order = sorted(
+            current_gpu,
+            key=lambda m: (-weights[m]["weighted_token_rate"], m),
+        )
+        # line 2-3
+        shared_kv = {i: self.gpu_mem for i in range(self.num_gpus)}
+        w_rate = {i: 0.0 for i in range(self.num_gpus)}
+
+        target: Dict[str, int] = {}
+        for name in order:
+            g_k = current_gpu[name]
+            w_k = self.model_weights_info[name]["model_size"]
+
+            # A7: only GPUs that can physically hold the weights are candidates.
+            # The current GPU always qualifies -- the weights are already there.
+            cand = [
+                i
+                for i in range(self.num_gpus)
+                if shared_kv[i] > w_k
+                and (i == g_k or gpu_available_memory.get(i, 0.0) >= w_k)
+            ]
+            if not cand:
+                cand = [g_k]
+
+            # line 6
+            ratios = {i: (w_rate[i] / shared_kv[i] if shared_kv[i] > 0 else float("inf"))
+                      for i in cand}
+            best_idx = min(ratios, key=lambda i: (ratios[i], i))
+            best_r = ratios[best_idx]
+            # line 7
+            current_r = (
+                w_rate[g_k] / shared_kv[g_k] if shared_kv.get(g_k, 0) > 0 else float("inf")
+            )
+            # line 8 -- gated later on peak KVPR (assumption A1); the greedy pass
+            # itself takes the argmin, which is the tau -> 0 reading of line 8.
+            best_gpu = best_idx if best_r < current_r else g_k
+
+            # lines 9-11
+            target[name] = best_gpu
+            w_rate[best_gpu] += weights[name]["weighted_token_rate"]
+            shared_kv[best_gpu] -= w_k
+        return target
+
+    # --------------------------------------------------------------- override
+
+    def _find_optimal_migrations(
+        self,
+        model_instance_state_dict: Dict[str, List[ModelInstanceState]],
+        model_queues: Dict[str, ModelQueueTracker],
+        gpu_available_memory: Dict[int, float],
+        model_violation_stats,
+        gpu_to_model_mapping: Dict[int, List[str]],
+    ) -> List[Tuple[str, int, int]]:
+        self._cycle += 1
+        now = time.time()
+
+        current_gpu: Dict[str, int] = {}
+        for gpu_id, names in gpu_to_model_mapping.items():
+            for name in names:
+                if name in self.model_weights_info:
+                    current_gpu[name] = gpu_id
+        if not current_gpu:
+            return []
+
+        weights = self._weighted_token_rates(model_queues, list(current_gpu))
+        kvpr_now = self._kvpr(gpu_to_model_mapping, weights)
+        peak_now = self._peak(kvpr_now)
+
+        # ---- per-cycle observability (Section 7 of the task brief)
+        rows = []
+        for name, gpu_id in sorted(current_gpu.items()):
+            w = weights[name]
+            rows.append(
+                {
+                    "model": name,
+                    "token_rate": round(w["token_rate"], 3),
+                    "input_token_rate": round(w["input_token_rate"], 3),
+                    "decode_token_rate": round(w["decode_token_rate"], 3),
+                    "token_size": w["token_size"],
+                    "tpot_slo_s": w["tpot_slo_s"],
+                    "w_token_rate": w["weighted_token_rate"],
+                    "current_gpu": gpu_id,
+                }
+            )
+        shared_kv_now = {
+            i: self._shared_kv(gpu_to_model_mapping, i) for i in range(self.num_gpus)
+        }
+
+        def emit(decision, reason, cand=None, peak_after=None, improvement=None):
+            logger.info(
+                "[PAPER-ALG1] "
+                + json.dumps(
+                    {
+                        "cycle": self._cycle,
+                        "t": round(now, 3),
+                        "models": rows,
+                        "shared_kv": {str(k): round(v, 3) for k, v in shared_kv_now.items()},
+                        "kvpr": {str(k): v for k, v in kvpr_now.items()},
+                        "peak_kvpr": peak_now,
+                        "candidate": cand,
+                        "peak_kvpr_after": peak_after,
+                        "improvement": improvement,
+                        "tau": self.tau,
+                        "migration_decision": decision,
+                        "migration_reason": reason,
+                    },
+                    default=str,
+                )
+            )
+
+        if peak_now <= 0:
+            emit(None, "no measured load (peak KVPR == 0)")
+            return []
+        if now - self._last_migration_time < self.migration_cooldown:
+            emit(None, "migration cooldown active")
+            return []
+
+        target = self._greedy_placement(weights, current_gpu, gpu_available_memory)
+        diffs = [(m, current_gpu[m], g) for m, g in target.items() if g != current_gpu[m]]
+        if not diffs:
+            emit(None, "greedy placement equals current placement")
+            return []
+
+        # A5: evaluate each single move on its own and keep the best one.
+        best = None
+        for name, src, dst in diffs:
+            if len(gpu_to_model_mapping.get(src, [])) <= 1:
+                continue                                        # A6
+            if gpu_available_memory.get(dst, 0.0) < self.model_weights_info[name]["model_size"]:
+                continue                                        # A7
+            sim = {g: list(ms) for g, ms in gpu_to_model_mapping.items()}
+            sim[src].remove(name)
+            sim.setdefault(dst, []).append(name)
+            peak_after = self._peak(self._kvpr(sim, weights))
+            improvement = (peak_now - peak_after) / peak_now
+            if best is None or improvement > best[3]:
+                best = ((name, src, dst), peak_after, sim, improvement)
+
+        if best is None:
+            emit(None, "every candidate move blocked (would empty a GPU / no room)")
+            return []
+
+        (name, src, dst), peak_after, _sim, improvement = best
+        cand = {"model": name, "from": src, "to": dst}
+        if improvement > self.tau:
+            self._last_migration_time = now
+            emit("MIGRATE", "peak KVPR improvement exceeds tau", cand, peak_after, improvement)
+            logger.info(f"[PAPER-ALG1] MIGRATE {name} gpu{src} -> gpu{dst} "
+                        f"improvement={improvement:.4f} tau={self.tau}")
+            return [(name, src, dst)]
+
+        emit(None, "peak KVPR improvement below tau", cand, peak_after, improvement)
+        return []
diff --git a/python/sglang/multi_model/scheduling/policy/kvpr_global_tp.py b/python/sglang/multi_model/scheduling/policy/kvpr_global_tp.py
new file mode 100644
index 0000000..2421b9d
--- /dev/null
+++ b/python/sglang/multi_model/scheduling/policy/kvpr_global_tp.py
@@ -0,0 +1,408 @@
+"""Algorithm 1 with TP groups kept whole, and the paper's anti-affinity constraint.
+
+Two things change against v4, and only the second is a new mechanism.
+
+1.  **A TP group stops being collapsed to one GPU.**  v4 builds
+    ``current_gpu = {name: gpu_id ...}`` from ``gpu_to_model_mapping``.  That
+    mapping already lists a TP model under *every* GPU it spans
+    (``simple_global.py:401-405`` walks ``instance.gpu_ids``), so the dict
+    comprehension silently keeps whichever GPU it saw last and the group is
+    lost.  Here placement carries an ordered tuple of GPUs per model.
+
+2.  **Anti-affinity.**  The paper constrains the k shards of a tp_size=k model
+    to k distinct GPUs.  Expressing it needs per-shard placement: the shards are
+    placed one at a time by the same argmin v3 uses, and the constraint is a
+    filter on that argmin's candidate set -- drop any GPU already holding a
+    shard of this model.  It is opt-in (``--enable-tp-anti-affinity``), per the
+    project's rule that new paths sit behind a flag.
+
+Why the constraint can actually bind here
+-----------------------------------------
+It would be easy to build this so the constraint never fires and then report
+that it "passed".  It fires only because placement is per-shard: two shards of
+one model are two independent argmin decisions, and the least-loaded GPU for
+shard 0 is very often also the least-loaded for shard 1.  With the filter off,
+that is exactly what the planner picks.
+
+The counterfactual is therefore recorded on every cycle: ``aa_violations`` counts
+the shards whose unconstrained argmin would have doubled up on a GPU already
+holding a shard of the same model, and ``aa_diverted`` counts the ones the
+filter actually moved.  A run where both are zero is reported as "the constraint
+never bound", not as "the constraint works".
+
+Modelling a shard's load
+------------------------
+Under TP both the weights and the KV cache are sharded across the group: each
+GPU holds 1/k of the parameters and 1/k of the KV heads.  So a shard
+contributes ``weighted_token_rate / k`` and occupies ``model_size / k``.
+Charging each GPU the whole model would make a TP model look k times more
+expensive than it is and Algorithm 1 would refuse to place it.
+"""
+
+import json
+import logging
+import time
+from typing import Dict, List, Sequence, Tuple
+
+from sglang.multi_model.scheduling.model_queue_tracker import ModelQueueTracker
+from sglang.multi_model.scheduling.policy.kvpr_global_v4 import KVPRGlobalPolicyV4
+from sglang.multi_model.tp_slots import plan_for_server_args
+from sglang.multi_model.scheduling.state import ModelInstanceState
+
+logger = logging.getLogger(__name__)
+
+
+class KVPRGlobalPolicyTP(KVPRGlobalPolicyV4):
+    """v4's decision rule, with TP groups intact and anti-affinity available."""
+
+    def __init__(self, *args, **kwargs):
+        # The parent takes keyword args only and does not know about
+        # server_args, so it is popped before the super() call rather than
+        # forwarded.  controller_global passes it only for this class.
+        server_args = kwargs.pop("server_args", None)
+        super().__init__(*args, **kwargs)
+        self.anti_affinity = bool(
+            getattr(server_args, "enable_tp_anti_affinity", False)
+        )
+        # Off by default: the paper's rule is the second-lowest-KVPR fallback,
+        # not a hard filter.  See _greedy_placement_tp.
+        self.anti_affinity_strict = bool(
+            getattr(server_args, "enable_tp_anti_affinity_strict", False)
+        )
+        self._model_tp_sizes: Dict[str, int] = {
+            mc.model_name: int(getattr(mc, "tp_size", 1) or 1)
+            for mc in (getattr(server_args, "model_configs", None) or [])
+        }
+        self._plan = plan_for_server_args(server_args) if server_args else None
+        # Seeded from the configured placement; updated when this policy moves a
+        # model.  Nothing else changes a group.
+        self._group_of: Dict[str, Tuple[int, ...]] = {}
+        for mc in (getattr(server_args, "model_configs", None) or []):
+            for ic in mc.get_instance_configs():
+                if ic.on and len(ic.gpu_ids) > 1:
+                    self._group_of[mc.model_name] = tuple(ic.gpu_ids)
+        self._tp_audit = {
+            "cycles": 0,
+            "shards_placed": 0,
+            "aa_violations": 0,   # unconstrained argmin would have doubled up
+            "aa_diverted": 0,     # the filter actually changed the choice
+            "aa_infeasible": 0,   # no candidate left; fell back
+            # The paper's rule permits this; counted, not repaired.
+            "aa_second_also_collides": 0,
+            # The argmin's GPU set was not a real/free engine group and had to
+            # be snapped to one.  Emitting an unrealisable set is what left a TP
+            # model down with 1672 requests queued behind it.
+            "snapped_to_group": 0,
+            "group_unavailable": 0,
+        }
+        logger.info(
+            "[PAPER-ALG1-TP] init "
+            + json.dumps({
+                "anti_affinity": self.anti_affinity,
+                "anti_affinity_strict": self.anti_affinity_strict,
+                "model_tp_sizes": self._model_tp_sizes,
+            })
+        )
+
+    # ------------------------------------------------------------------ helpers
+    def tp_size_of(self, model_name: str) -> int:
+        return max(1, self._model_tp_sizes.get(model_name, 1))
+
+    def _current_groups(
+        self, gpu_to_model_mapping: Dict[int, List[str]]
+    ) -> Dict[str, Tuple[int, ...]]:
+        """model -> the ordered tuple of GPUs it currently occupies.
+
+        The shared instance state cannot carry the group.  ``ModelInstanceState``
+        for a worker-pool model is one candidate per GPU with ``gpu_ids=[gpu]``,
+        and ``simple_global.py:239-240`` asserts that a model's ACTIVE instances
+        touch at most **one** GPU::
+
+            for gpu_id in instance.gpu_ids: model_active_instances[gpu_id] = ...
+            assert len(model_active_instances) <= 1
+
+        Widening an instance to ``gpu_ids=[0, 1]`` therefore crashes the
+        controller's scheduling loop on the first cycle -- measured, not
+        predicted (``aa-on`` went from 4/4 to 0/4 that way).  That file belongs
+        to the migration work, so the shared representation is left exactly as
+        it is and the group is tracked here instead.
+
+        The tracked group is seeded from the configured placement and updated
+        whenever this policy emits a move, which is the only thing that changes
+        it.  If tracking and the reported rank0 GPU ever disagree, the reported
+        GPU wins and the group is re-derived from the slot plan -- the runtime
+        is the authority, not our memory of it.
+        """
+        reported: Dict[str, List[int]] = {}
+        for gpu_id, names in gpu_to_model_mapping.items():
+            for name in names:
+                if name in self.model_weights_info:
+                    reported.setdefault(name, []).append(gpu_id)
+
+        out: Dict[str, Tuple[int, ...]] = {}
+        for name, gpus in reported.items():
+            seen = tuple(sorted(set(gpus)))
+            k = self.tp_size_of(name)
+            if k <= 1 or len(seen) >= k:
+                out[name] = seen
+                continue
+            out[name] = self._group_containing(name, seen[0], k)
+        return out
+
+    def _group_containing(self, name: str, rank0_gpu: int, k: int) -> Tuple[int, ...]:
+        """The k-GPU group this model sits on, given only rank0's GPU."""
+        tracked = self._group_of.get(name)
+        if tracked and len(tracked) == k and tracked[0] == rank0_gpu:
+            return tracked
+        for slot in self._plan.groups(k):
+            if slot.owner_gpu == rank0_gpu:
+                self._group_of[name] = tuple(slot.gpu_ids)
+                return self._group_of[name]
+        # No group owns that GPU: report what is actually known rather than
+        # inventing the missing ranks.
+        logger.warning(
+            f"[PAPER-ALG1-TP] no tp_size={k} group owned by gpu {rank0_gpu} "
+            f"for {name}; reporting rank0 only"
+        )
+        return (rank0_gpu,)
+
+    # -------------------------------------------------------------- placement
+    def _greedy_placement_tp(
+        self,
+        weights: Dict[str, Dict[str, float]],
+        current_groups: Dict[str, Tuple[int, ...]],
+        gpu_available_memory: Dict[int, float],
+    ) -> Dict[str, Tuple[int, ...]]:
+        """v3's line-8 argmin, run once per shard, with the group kept whole."""
+        order = sorted(
+            current_groups, key=lambda m: (-weights[m]["weighted_token_rate"], m)
+        )
+        shared_kv = {i: self.gpu_mem for i in range(self.num_gpus)}
+        w_rate = {i: 0.0 for i in range(self.num_gpus)}
+        target: Dict[str, Tuple[int, ...]] = {}
+        self._last_line8 = []
+
+        for name in order:
+            k = self.tp_size_of(name)
+            current = list(current_groups[name])
+            # Under TP each GPU carries 1/k of the weights and 1/k of the KV.
+            shard_size = self.model_weights_info[name]["model_size"] / k
+            shard_rate = weights[name]["weighted_token_rate"] / k
+            chosen_gpus: List[int] = []
+
+            for rank in range(k):
+                cur = current[rank] if rank < len(current) else current[-1]
+                feasible = [
+                    i
+                    for i in range(self.num_gpus)
+                    if shared_kv[i] > shard_size
+                    and (i in current or gpu_available_memory.get(i, 0.0) >= shard_size)
+                ] or [cur]
+
+                def ratio(i):
+                    return w_rate[i] / shared_kv[i] if shared_kv[i] > 0 else float("inf")
+
+                order_by_kvpr = sorted(feasible, key=lambda i: (ratio(i), i))
+                best_unconstrained = order_by_kvpr[0]
+
+                # The paper's rule, verbatim (Appendix A.2.2):
+                #
+                #   "if assigning a TP part to the GPU with the minimum KVPR
+                #    would result in collocating it with another part of the
+                #    same original model, we instead assign it to the GPU
+                #    exhibiting the second-lowest KVPR."
+                #
+                # Note what that does NOT say: it does not re-check the
+                # second-lowest GPU.  For tp_size=2 the distinction is empty --
+                # only one part is placed, so the second-lowest cannot collide.
+                # From tp_size=3 up it is not empty: the second-lowest GPU may
+                # already hold a part, and the literal rule places there anyway.
+                # So the paper's rule reduces collocation, it does not forbid
+                # it.  ``strict`` implements the forbidding version and is kept
+                # separate rather than folded in, because silently strengthening
+                # the paper's rule would misreport what was reproduced.
+                filtered_out = [i for i in feasible if i in chosen_gpus]
+                best = best_unconstrained
+                if k > 1 and self.anti_affinity and best_unconstrained in chosen_gpus:
+                    if self.anti_affinity_strict:
+                        allowed = [i for i in order_by_kvpr if i not in chosen_gpus]
+                        if allowed:
+                            best = allowed[0]
+                        else:
+                            self._tp_audit["aa_infeasible"] += 1
+                    elif len(order_by_kvpr) > 1:
+                        best = order_by_kvpr[1]          # second-lowest KVPR
+                        if best in chosen_gpus:
+                            # The paper's rule allows this; record it rather
+                            # than quietly repairing it.
+                            self._tp_audit["aa_second_also_collides"] += 1
+                    else:
+                        self._tp_audit["aa_infeasible"] += 1
+
+                if k > 1 and best_unconstrained in chosen_gpus:
+                    # The unconstrained argmin would have stacked two shards of
+                    # this model on one GPU.  This is the counterfactual that
+                    # makes the ON/OFF comparison meaningful.
+                    self._tp_audit["aa_violations"] += 1
+                    if self.anti_affinity and best != best_unconstrained:
+                        self._tp_audit["aa_diverted"] += 1
+
+                cur_r = ratio(cur)
+                delta = cur_r - ratio(best)
+                # tau applies to the move, exactly as in v3: stay put unless the
+                # improvement clears it.  A shard may only stay if staying does
+                # not itself violate anti-affinity.
+                stay_ok = not (k > 1 and self.anti_affinity and cur in chosen_gpus)
+                chosen = best if (delta > self.tau or not stay_ok) else cur
+
+                self._last_line8.append({
+                    "model": name,
+                    "tp_size": k,
+                    "rank": rank,
+                    "current_gpu": cur,
+                    "best_gpu": best,
+                    "best_gpu_unconstrained": best_unconstrained,
+                    "anti_affinity_filtered": sorted(filtered_out) if k > 1 else [],
+                    "current_r": cur_r,
+                    "best_r": ratio(best),
+                    "absolute_delta": delta,
+                    "tau": self.tau,
+                    "chosen_gpu": chosen,
+                })
+
+                chosen_gpus.append(chosen)
+                w_rate[chosen] += shard_rate
+                shared_kv[chosen] -= shard_size
+                self._tp_audit["shards_placed"] += 1
+
+            target[name] = self._snap_to_group(name, k, chosen_gpus, target)
+        # NOTE: _group_of is deliberately NOT updated here.  A plan is not a
+        # placement -- v4 emits at most one migration per cycle and blocks
+        # others, so most of this plan never happens.  Recording it as "where
+        # the model is" made the next cycle read a group that does not exist
+        # (observed: an OFF-arm plan of [0, 0] became current_groups [0, 0],
+        # a placement no engine could hold).  Only an emitted move updates it.
+        return target
+
+    def _greedy_placement(self, weights, current_gpu, gpu_available_memory):
+        """v4 calls this with ``{model: single_gpu}`` and expects the same back.
+
+        The single GPU is rank0's -- v4 built the dict from
+        ``gpu_to_model_mapping`` and kept whichever GPU it saw last.  Wrapping
+        that as a 1-tuple and handing it to the per-shard planner is wrong and
+        was: with ``current = [0]``, shard 1 read ``current[-1] = 0`` as its
+        current GPU, so staying put meant staying on GPU 0 and the plan came out
+        ``[0, 0]``.  The group has to be recovered first.
+
+        The dict returned to v4 keeps its shape (rank0's GPU per model); the
+        full group is exposed separately as ``_last_group_plan``.
+        """
+        groups = {}
+        for name, g in current_gpu.items():
+            rank0 = g[0] if isinstance(g, (tuple, list)) else g
+            k = self.tp_size_of(name)
+            groups[name] = (
+                tuple(g) if isinstance(g, (tuple, list)) and len(g) == k
+                else self._group_containing(name, rank0, k) if k > 1
+                else (rank0,)
+            )
+        plan = self._greedy_placement_tp(weights, groups, gpu_available_memory)
+        self._last_group_plan = plan
+        return {m: g[0] for m, g in plan.items()}
+
+    def _snap_to_group(self, name, k, chosen_gpus, target_so_far):
+        """Map a per-shard argmin result onto a group that actually exists and is free.
+
+        Placement picks GPUs by KVPR alone.  Nothing in that step knows whether
+        a k-GPU engine group spans exactly those GPUs, or whether it is already
+        serving something else.  Emitting such a placement is not a no-op: the
+        controller sends the activation to the chosen rank0 GPU, that GPU's pool
+        has no free tp_size=k slot, the activation is refused, and the model
+        stays down -- its requests then queue forever.  Measured: a 4-GPU run
+        where all 1672 outstanding requests belonged to the single TP model
+        while the five TP=1 models finished normally.
+
+        So the chosen set is snapped to the nearest real, free group: most GPUs
+        in common with what the argmin wanted, then lowest resulting KVPR order,
+        then lowest GPU ids for determinism.  The model's current group is
+        always a legal target -- staying put must never be impossible.
+        """
+        want = tuple(chosen_gpus)
+        if k <= 1 or self._plan is None:
+            return want
+
+        taken = set()
+        for other, grp in self._group_of.items():
+            if other != name and len(grp) > 1:
+                taken.add(tuple(grp))
+        for other, grp in target_so_far.items():
+            if other != name and len(grp) > 1:
+                taken.add(tuple(grp))
+
+        mine = self._group_of.get(name)
+        candidates = [g for g in self._plan.groups(k)
+                      if tuple(g.gpu_ids) not in taken or tuple(g.gpu_ids) == mine]
+        if not candidates:
+            # Every group of this size is serving something else.  Staying put
+            # is the only honest answer; say so rather than emitting a move that
+            # will be refused.
+            self._tp_audit["group_unavailable"] += 1
+            logger.warning(
+                f"[PAPER-ALG1-TP] no free tp_size={k} group for {name}; "
+                f"wanted {list(want)}, staying on {list(mine) if mine else 'unknown'}"
+            )
+            return tuple(mine) if mine else want
+
+        wanted = set(want)
+        best = max(candidates,
+                   key=lambda g: (len(wanted & set(g.gpu_ids)), -g.gpu_ids[0], -g.worker_id))
+        snapped = tuple(best.gpu_ids)
+        if snapped != want:
+            self._tp_audit["snapped_to_group"] += 1
+        return snapped
+
+    # ------------------------------------------------------------ entry point
+    def _find_optimal_migrations(
+        self,
+        model_instance_state_dict: Dict[str, List[ModelInstanceState]],
+        model_queues: Dict[str, ModelQueueTracker],
+        gpu_available_memory: Dict[int, float],
+        model_violation_stats,
+        gpu_to_model_mapping: Dict[int, List[str]],
+    ) -> List[Tuple[str, int, int]]:
+        self._tp_audit["cycles"] += 1
+        current_groups = self._current_groups(gpu_to_model_mapping)
+        has_tp = any(len(g) > 1 for g in current_groups.values())
+
+        before = dict(self._group_of)
+        out = super()._find_optimal_migrations(
+            model_instance_state_dict,
+            model_queues,
+            gpu_available_memory,
+            model_violation_stats,
+            gpu_to_model_mapping,
+        )
+
+        # A move was actually emitted: that, and only that, changes the group.
+        for name, _src, _dst in out:
+            planned = getattr(self, "_last_group_plan", {}).get(name)
+            if planned and self.tp_size_of(name) > 1:
+                self._group_of[name] = tuple(planned)
+        if self._group_of != before:
+            logger.info(f"[PAPER-ALG1-TP] group moved: {before} -> {self._group_of}")
+
+        # One line per cycle carrying the group view and the counterfactual, so
+        # "did the constraint bind" is answered from raw data, not from a claim.
+        logger.info("[PAPER-ALG1-TP] " + json.dumps({
+            "cycle": self._tp_audit["cycles"],
+            "timestamp": time.time(),
+            "anti_affinity": self.anti_affinity,
+            "any_tp_group_active": has_tp,
+            "current_groups": {m: list(g) for m, g in current_groups.items()},
+            "tracked_groups": {m: list(g) for m, g in self._group_of.items()},
+            "group_plan": {
+                m: list(g) for m, g in getattr(self, "_last_group_plan", {}).items()
+            },
+            "tp_audit": dict(self._tp_audit),
+        }, default=str))
+        return out
diff --git a/python/sglang/multi_model/scheduling/policy/kvpr_global_v3.py b/python/sglang/multi_model/scheduling/policy/kvpr_global_v3.py
new file mode 100644
index 0000000..f1baa4a
--- /dev/null
+++ b/python/sglang/multi_model/scheduling/policy/kvpr_global_v3.py
@@ -0,0 +1,163 @@
+"""Prism Algorithm 1 with the paper's literal absolute-tau decision rule."""
+
+import json
+import logging
+import time
+from typing import Dict, List, Tuple
+
+from sglang.multi_model.scheduling.model_queue_tracker import ModelQueueTracker
+from sglang.multi_model.scheduling.policy.kvpr_global import KVPRGlobalPolicy
+from sglang.multi_model.scheduling.state import ModelInstanceState
+
+logger = logging.getLogger(__name__)
+
+
+class KVPRGlobalPolicyV3(KVPRGlobalPolicy):
+    """Use ``current_r - best_r > tau`` exactly as printed in Algorithm 1.
+
+    V2 used a relative reduction in cluster peak KVPR because the paper omits
+    tau's value and units.  V3 deliberately restores the literal, absolute
+    threshold.  Tau is calibrated once on this machine and then held fixed for
+    every workload/rate/system comparison.
+    """
+
+    def _weighted_token_rates(self, model_queues, model_names):
+        """Return Algorithm-1 rates in GiB/s/SLO so they match shared_kv GiB.
+
+        The base v2 policy can leave ``cell_size`` in bytes because its final
+        migration gate is relative and the common 2**30 scale cancels.  V3's
+        literal absolute tau cannot: both numerator and denominator must use
+        the same memory unit.
+        """
+        out = super()._weighted_token_rates(model_queues, model_names)
+        for row in out.values():
+            row["token_size_bytes"] = row["token_size"]
+            row["token_size"] /= 2**30
+            row["weighted_token_rate"] /= 2**30
+        return out
+
+    def _greedy_placement(
+        self,
+        weights: Dict[str, Dict[str, float]],
+        current_gpu: Dict[str, int],
+        gpu_available_memory: Dict[int, float],
+    ) -> Dict[str, int]:
+        order = sorted(
+            current_gpu, key=lambda m: (-weights[m]["weighted_token_rate"], m)
+        )
+        shared_kv = {i: self.gpu_mem for i in range(self.num_gpus)}
+        w_rate = {i: 0.0 for i in range(self.num_gpus)}
+        target: Dict[str, int] = {}
+        self._last_line8 = []
+
+        for name in order:
+            current = current_gpu[name]
+            model_size = self.model_weights_info[name]["model_size"]
+            candidates = [
+                i
+                for i in range(self.num_gpus)
+                if shared_kv[i] > model_size
+                and (i == current or gpu_available_memory.get(i, 0.0) >= model_size)
+            ] or [current]
+            ratios = {
+                i: w_rate[i] / shared_kv[i] if shared_kv[i] > 0 else float("inf")
+                for i in candidates
+            }
+            best = min(ratios, key=lambda i: (ratios[i], i))
+            best_r = ratios[best]
+            current_r = (
+                w_rate[current] / shared_kv[current]
+                if shared_kv.get(current, 0) > 0
+                else float("inf")
+            )
+            delta = current_r - best_r
+            chosen = best if delta > self.tau else current
+            self._last_line8.append(
+                {
+                    "model": name,
+                    "current_gpu": current,
+                    "best_gpu": best,
+                    "current_r": current_r,
+                    "best_r": best_r,
+                    "absolute_delta": delta,
+                    "tau": self.tau,
+                    "chosen_gpu": chosen,
+                }
+            )
+            target[name] = chosen
+            w_rate[chosen] += weights[name]["weighted_token_rate"]
+            shared_kv[chosen] -= model_size
+        return target
+
+    def _find_optimal_migrations(
+        self,
+        model_instance_state_dict: Dict[str, List[ModelInstanceState]],
+        model_queues: Dict[str, ModelQueueTracker],
+        gpu_available_memory: Dict[int, float],
+        model_violation_stats,
+        gpu_to_model_mapping: Dict[int, List[str]],
+    ) -> List[Tuple[str, int, int]]:
+        self._cycle += 1
+        now = time.time()
+        current_gpu = {
+            name: gpu_id
+            for gpu_id, names in gpu_to_model_mapping.items()
+            for name in names
+            if name in self.model_weights_info
+        }
+        if not current_gpu:
+            return []
+
+        weights = self._weighted_token_rates(model_queues, list(current_gpu))
+        kvpr_now = self._kvpr(gpu_to_model_mapping, weights)
+        peak_now = self._peak(kvpr_now)
+
+        def emit(decision, reason, candidate=None, peak_after=None):
+            logger.info(
+                "[PAPER-ALG1-V3] "
+                + json.dumps(
+                    {
+                        "cycle": self._cycle,
+                        "tau_mode": "absolute-line8",
+                        "tau": self.tau,
+                        "kvpr": kvpr_now,
+                        "peak_kvpr": peak_now,
+                        "line8": getattr(self, "_last_line8", []),
+                        "candidate": candidate,
+                        "peak_kvpr_after": peak_after,
+                        "migration_decision": decision,
+                        "migration_reason": reason,
+                    },
+                    default=str,
+                )
+            )
+
+        if peak_now <= 0:
+            emit(None, "no measured load")
+            return []
+        if now - self._last_migration_time < self.migration_cooldown:
+            emit(None, "migration cooldown active")
+            return []
+
+        target = self._greedy_placement(weights, current_gpu, gpu_available_memory)
+        candidates = []
+        for name, dst in target.items():
+            src = current_gpu[name]
+            if dst == src or len(gpu_to_model_mapping.get(src, [])) <= 1:
+                continue
+            if gpu_available_memory.get(dst, 0.0) < self.model_weights_info[name]["model_size"]:
+                continue
+            sim = {gpu: list(models) for gpu, models in gpu_to_model_mapping.items()}
+            sim[src].remove(name)
+            sim.setdefault(dst, []).append(name)
+            peak_after = self._peak(self._kvpr(sim, weights))
+            candidates.append((peak_after, name, src, dst))
+
+        if not candidates:
+            emit(None, "line-8 placement unchanged or candidate blocked")
+            return []
+        peak_after, name, src, dst = min(candidates)
+        self._last_migration_time = now
+        candidate = {"model": name, "from": src, "to": dst}
+        emit("MIGRATE", "literal line-8 absolute delta exceeds tau", candidate, peak_after)
+        return [(name, src, dst)]
diff --git a/python/sglang/multi_model/scheduling/policy/kvpr_global_v4.py b/python/sglang/multi_model/scheduling/policy/kvpr_global_v4.py
new file mode 100644
index 0000000..0dd91b1
--- /dev/null
+++ b/python/sglang/multi_model/scheduling/policy/kvpr_global_v4.py
@@ -0,0 +1,154 @@
+"""Algorithm 1 with a runtime placement audit.
+
+v3 already implements the paper's line-8 rule literally.  What it does not
+record is the gap between what the planner decided and what the cluster ended
+up looking like: ``_greedy_placement`` computes a target GPU for *every* model,
+but a cycle emits at most one migration, so the plan and the runtime placement
+can disagree for a long time -- or forever -- and nothing said so.
+
+v4 keeps v3's decision rule and its one-migration-per-cycle emission unchanged.
+Emitting the whole plan at once is not obviously the paper's intent and is
+demonstrably worse here: each migration is a real transfer, the rate estimate
+underneath is a 30 s sliding window, and moving several models against one
+window's estimate is what produced the thrashing documented in
+``docs/paper_faithful/design_analysis.md`` §5a.  So the behaviour is left
+alone and the *evidence* is added instead:
+
+* the full placement plan for every cycle, model by model;
+* for every model the plan wants to move, why it was not moved this cycle --
+  cooldown, memory infeasibility, last model on its GPU, or simply not the
+  cycle's best candidate;
+* a convergence gap: how many models sit somewhere other than where the
+  planner wants them.
+
+Whether one-at-a-time converges is then a measured question, not an assumption.
+"""
+
+import json
+import logging
+import time
+from typing import Dict, List, Tuple
+
+from sglang.multi_model.scheduling.model_queue_tracker import ModelQueueTracker
+from sglang.multi_model.scheduling.policy.kvpr_global_v3 import KVPRGlobalPolicyV3
+from sglang.multi_model.scheduling.state import ModelInstanceState
+
+logger = logging.getLogger(__name__)
+
+
+class KVPRGlobalPolicyV4(KVPRGlobalPolicyV3):
+    """v3's rule, plus the planner-versus-runtime record v3 never kept."""
+
+    def __init__(self, *args, **kwargs):
+        super().__init__(*args, **kwargs)
+        self._audit = {
+            "cycles": 0,
+            "placement_decisions": 0,
+            "migrations_emitted": 0,
+            "suppressed_by_tau": 0,
+            "rejected_by_memory": 0,
+            "rejected_last_model_on_gpu": 0,
+            "deferred_by_cooldown": 0,
+            "not_best_candidate": 0,
+        }
+
+    def _find_optimal_migrations(
+        self,
+        model_instance_state_dict: Dict[str, List[ModelInstanceState]],
+        model_queues: Dict[str, ModelQueueTracker],
+        gpu_available_memory: Dict[int, float],
+        model_violation_stats,
+        gpu_to_model_mapping: Dict[int, List[str]],
+    ) -> List[Tuple[str, int, int]]:
+        self._cycle += 1
+        self._audit["cycles"] += 1
+        now = time.time()
+        current_gpu = {
+            name: gpu_id
+            for gpu_id, names in gpu_to_model_mapping.items()
+            for name in names
+            if name in self.model_weights_info
+        }
+        if not current_gpu:
+            return []
+
+        weights = self._weighted_token_rates(model_queues, list(current_gpu))
+        kvpr_now = self._kvpr(gpu_to_model_mapping, weights)
+        peak_now = self._peak(kvpr_now)
+
+        cooldown_active = (now - self._last_migration_time) < self.migration_cooldown
+        plan = self._greedy_placement(weights, current_gpu, gpu_available_memory)
+        line8 = list(getattr(self, "_last_line8", []))
+        self._audit["placement_decisions"] += len(line8)
+        for row in line8:
+            if row["chosen_gpu"] == row["current_gpu"] and row["best_gpu"] != row["current_gpu"]:
+                self._audit["suppressed_by_tau"] += 1
+
+        # Everything the plan wants moved, and what stopped each one.
+        wanted, blocked, candidates = [], [], []
+        for name, dst in plan.items():
+            src = current_gpu[name]
+            if dst == src:
+                continue
+            wanted.append({"model": name, "from": src, "to": dst})
+            if len(gpu_to_model_mapping.get(src, [])) <= 1:
+                blocked.append({"model": name, "reason": "last active model on source gpu"})
+                self._audit["rejected_last_model_on_gpu"] += 1
+                continue
+            need = self.model_weights_info[name]["model_size"]
+            if gpu_available_memory.get(dst, 0.0) < need:
+                blocked.append({
+                    "model": name, "reason": "target memory infeasible",
+                    "need_gib": need, "free_gib": gpu_available_memory.get(dst, 0.0),
+                })
+                self._audit["rejected_by_memory"] += 1
+                continue
+            sim = {gpu: list(models) for gpu, models in gpu_to_model_mapping.items()}
+            sim[src].remove(name)
+            sim.setdefault(dst, []).append(name)
+            candidates.append((self._peak(self._kvpr(sim, weights)), name, src, dst))
+
+        # How far the cluster is from the plan right now.
+        misplaced = [m for m, dst in plan.items() if dst != current_gpu[m]]
+
+        chosen = None
+        decision, reason = None, ""
+        if peak_now <= 0:
+            reason = "no measured load"
+        elif cooldown_active:
+            reason = "migration cooldown active"
+            self._audit["deferred_by_cooldown"] += len(candidates)
+        elif not candidates:
+            reason = "line-8 placement unchanged or every candidate blocked"
+        else:
+            peak_after, name, src, dst = min(candidates)
+            chosen = {"model": name, "from": src, "to": dst, "peak_kvpr_after": peak_after}
+            decision, reason = "MIGRATE", "literal line-8 absolute delta exceeds tau"
+            self._last_migration_time = now
+            self._audit["migrations_emitted"] += 1
+            self._audit["not_best_candidate"] += len(candidates) - 1
+
+        logger.info("[PAPER-ALG1-V4] " + json.dumps({
+            "cycle": self._cycle,
+            "timestamp": now,
+            "tau_mode": "absolute-line8",
+            "tau": self.tau,
+            "kvpr": kvpr_now,
+            "peak_kvpr": peak_now,
+            "line8": line8,
+            "placement_plan": plan,
+            "current_placement": current_gpu,
+            "plan_wants_moved": wanted,
+            "blocked": blocked,
+            "convergence_gap": len(misplaced),
+            "misplaced_models": misplaced,
+            "cooldown_active": cooldown_active,
+            "candidate": chosen,
+            "migration_decision": decision,
+            "migration_reason": reason,
+            "audit_totals": dict(self._audit),
+        }, default=str))
+
+        if chosen is None:
+            return []
+        return [(chosen["model"], chosen["from"], chosen["to"])]
diff --git a/python/sglang/multi_model/tp_slots.py b/python/sglang/multi_model/tp_slots.py
new file mode 100644
index 0000000..ecc5b43
--- /dev/null
+++ b/python/sglang/multi_model/tp_slots.py
@@ -0,0 +1,250 @@
+"""TP worker-slot groups for the worker-pool path.
+
+Why this module exists
+----------------------
+In the released prototype ``launch_worker_pool_engines`` builds one engine per
+``(GPU, worker slot)`` and hands each one ``[gpu_id]`` -- a list of length 1.
+The obvious reading is that the fix is to hand it the model's TP group instead.
+It is not, because of an ordering problem:
+
+* a worker-pool engine's ``tp_size`` is fixed when the **server starts**;
+* which model occupies a slot is decided at **runtime**, by the scheduler.
+
+So "pass the model's tp_size" is not expressible -- at launch time nobody knows
+which model will land in the slot.  The slots themselves have to carry the
+type.
+
+What a TP slot group is
+-----------------------
+A TP=k group is a *static* tuple of k distinct GPUs plus one worker-slot id.
+One engine spans it with k scheduler processes, rank r running on ``gpu_ids[r]``.
+Because membership is fixed at launch, the runtime side stays simple:
+
+* the slot is **owned** by rank0's GPU -- that pool assigns it to a model;
+* on every other GPU of the group the same slot id is **reserved** forever and
+  is never handed to anything else.
+
+That is worth stating plainly because the handoff expected otherwise.  The
+handoff assumed the k pools would have to acquire k slots together and roll
+back on partial failure.  With static membership there is no partial state to
+roll back: activation touches exactly one pool (rank0's), so it is already
+atomic.  The cost of that simplicity is stranded capacity -- a reserved slot on
+a non-rank0 GPU is unusable by TP=1 models even while the group is idle.  That
+is a real trade and is recorded rather than hidden.
+
+Why the slot id must be shared across the group
+-----------------------------------------------
+``worker_pool_model_runner.py:121`` derives each rank's accounting segment as
+``ipc_{gpu_id}_{worker_id}_{user}`` from *that rank's own* gpu_id.  So a group
+must carry a single ``worker_id``, and that id must be free on every GPU in the
+group.  Ids are therefore allocated globally, not per GPU, which means a GPU's
+slot ids are a sparse subset of the integers -- ``range(num_workers)`` is no
+longer a valid enumeration of them.  Callers must use :meth:`SlotPlan.worker_ids_on`.
+
+Determinism
+-----------
+``build_slot_plan`` is a pure function of ``(num_gpus, workers_per_gpu,
+tp_sizes, max_groups_per_tp_size)``.  The server launcher and each per-GPU
+scheduler process call it independently and must agree, so nothing here may
+depend on iteration order of a set, wall-clock, or process identity.
+"""
+
+from dataclasses import dataclass, field
+from itertools import combinations
+from typing import Dict, List, Optional, Sequence, Tuple
+
+
+@dataclass(frozen=True)
+class TPSlot:
+    """One worker slot.  ``tp_size == 1`` is the prototype's per-GPU slot."""
+
+    worker_id: int
+    tp_size: int
+    gpu_ids: Tuple[int, ...]
+
+    @property
+    def owner_gpu(self) -> int:
+        """The GPU whose scheduler drives this slot (rank 0)."""
+        return self.gpu_ids[0]
+
+    def role_on(self, gpu_id: int) -> str:
+        """``owner`` where the pool may assign it, ``shadow`` where it is only reserved."""
+        if gpu_id == self.owner_gpu:
+            return "owner"
+        if gpu_id in self.gpu_ids:
+            return "shadow"
+        return "absent"
+
+    def rank_of(self, gpu_id: int) -> int:
+        return self.gpu_ids.index(gpu_id)
+
+    def as_dict(self) -> Dict:
+        return {
+            "worker_id": self.worker_id,
+            "tp_size": self.tp_size,
+            "gpu_ids": list(self.gpu_ids),
+            "owner_gpu": self.owner_gpu,
+        }
+
+
+@dataclass
+class SlotPlan:
+    """Every slot on every GPU, and which of them are TP groups."""
+
+    num_gpus: int
+    workers_per_gpu: int
+    slots: List[TPSlot] = field(default_factory=list)
+
+    # -- lookups ---------------------------------------------------------
+    def slots_on(self, gpu_id: int) -> List[TPSlot]:
+        return [s for s in self.slots if gpu_id in s.gpu_ids]
+
+    def worker_ids_on(self, gpu_id: int) -> List[int]:
+        """The slot ids that actually exist on this GPU.
+
+        Sparse in general.  This is what replaces ``range(num_workers)`` for
+        anything that walks a GPU's slots (the worker pool, and the resource
+        manager's per-slot memory readers).
+        """
+        return sorted(s.worker_id for s in self.slots_on(gpu_id))
+
+    def owned_slots_on(self, gpu_id: int) -> List[TPSlot]:
+        return [s for s in self.slots_on(gpu_id) if s.role_on(gpu_id) == "owner"]
+
+    def groups(self, tp_size: Optional[int] = None) -> List[TPSlot]:
+        """The TP>1 groups, optionally filtered to one tp_size."""
+        out = [s for s in self.slots if s.tp_size > 1]
+        if tp_size is not None:
+            out = [s for s in out if s.tp_size == tp_size]
+        return out
+
+    def group_for_gpus(self, gpu_ids: Sequence[int]) -> Optional[TPSlot]:
+        """The group occupying exactly this ordered GPU tuple, if one exists."""
+        want = tuple(gpu_ids)
+        for s in self.slots:
+            if s.gpu_ids == want:
+                return s
+        return None
+
+    def tp_sizes_available(self) -> List[int]:
+        return sorted({s.tp_size for s in self.slots})
+
+    def as_dict(self) -> Dict:
+        return {
+            "num_gpus": self.num_gpus,
+            "workers_per_gpu": self.workers_per_gpu,
+            "slots": [s.as_dict() for s in self.slots],
+            "slots_per_gpu": {
+                g: self.worker_ids_on(g) for g in range(self.num_gpus)
+            },
+        }
+
+
+def build_slot_plan(
+    num_gpus: int,
+    workers_per_gpu: int,
+    tp_sizes: Sequence[int] = (),
+    max_groups_per_tp_size: Optional[int] = None,
+) -> SlotPlan:
+    """Lay out every worker slot in the cluster.
+
+    Slots ``0 .. workers_per_gpu-1`` on each GPU stay exactly what the released
+    prototype builds: one TP=1 engine each.  Anything with ``tp_size > 1`` is
+    appended afterwards with globally-unique ids, so with no TP models in the
+    config the plan is byte-for-byte the prototype's layout.
+
+    ``tp_sizes`` are the distinct TP sizes the model configs ask for.  For each
+    k > 1 every k-subset of GPUs is enumerated, which is what gives Algorithm 1
+    a real choice of placement -- with only one candidate group per model the
+    anti-affinity constraint would be satisfied by construction and there would
+    be nothing to measure.
+
+    ``max_groups_per_tp_size`` caps that enumeration.  Each group costs k
+    scheduler processes and k CUDA contexts, so the full C(n,k) is not always
+    affordable.  When it bites, the *dropped* groups are recorded on the plan
+    rather than silently omitted.
+    """
+    if num_gpus < 1:
+        raise ValueError(f"num_gpus must be >= 1, got {num_gpus}")
+    if workers_per_gpu < 0:
+        raise ValueError(f"workers_per_gpu must be >= 0, got {workers_per_gpu}")
+
+    slots: List[TPSlot] = []
+
+    # 1. the prototype's per-GPU slots, unchanged.
+    for worker_id in range(workers_per_gpu):
+        for gpu_id in range(num_gpus):
+            slots.append(TPSlot(worker_id=worker_id, tp_size=1, gpu_ids=(gpu_id,)))
+
+    # 2. TP groups.  Ids continue past the per-GPU block and are global, since
+    #    a group's id has to be free on every GPU it spans.
+    next_id = workers_per_gpu
+    dropped: List[Tuple[int, Tuple[int, ...]]] = []
+    for k in sorted({int(t) for t in tp_sizes}):
+        if k <= 1:
+            continue
+        if k > num_gpus:
+            # Not satisfiable here: k distinct GPUs are required and do not exist.
+            dropped.append((k, ()))
+            continue
+        combos = list(combinations(range(num_gpus), k))
+        keep = combos if max_groups_per_tp_size is None else combos[:max_groups_per_tp_size]
+        # Spread rank0 across GPUs instead of always giving it to the lowest id.
+        #
+        # This is not cosmetic.  Reactivation does not go through this module's
+        # placement: simple_global picks the GPU with the lowest KV pressure
+        # ratio (the paper's own rule, 6.1) and addresses the activation there.
+        # A group's control socket is bound only on rank0's GPU, so a GPU that
+        # owns no group can never accept a TP activation -- and with plain
+        # lexicographic ordering the highest GPU owns nothing at all
+        # (n=4, k=2: owners 0,0,0,1,1,2 -- GPU 3 owns none).  Measured
+        # consequence: "activate inactive model model_1 on GPU 3" was refused
+        # every time and the model stayed down with 1826 requests queued.
+        owned = {g: 0 for g in range(num_gpus)}
+        for combo in keep:
+            rank0 = min(combo, key=lambda g: (owned[g], g))
+            rest = [g for g in combo if g != rank0]
+            gpu_ids = (rank0, *rest)
+            owned[rank0] += 1
+            slots.append(TPSlot(worker_id=next_id, tp_size=k, gpu_ids=gpu_ids))
+            next_id += 1
+        for combo in combos[len(keep):]:
+            dropped.append((k, combo))
+
+    plan = SlotPlan(num_gpus=num_gpus, workers_per_gpu=workers_per_gpu, slots=slots)
+    plan.dropped_groups = dropped  # type: ignore[attr-defined]
+    return plan
+
+
+def tp_sizes_from_model_configs(model_configs) -> List[int]:
+    """Distinct ``tp_size`` values the configured models ask for."""
+    sizes = set()
+    for mc in model_configs or []:
+        sizes.add(int(getattr(mc, "tp_size", 1) or 1))
+    return sorted(sizes)
+
+
+def plan_for_server_args(multi_model_server_args) -> SlotPlan:
+    """The slot layout for a server, derived from its args alone.
+
+    Lives here rather than in ``multi_model_server`` so that the per-GPU
+    scheduler can call it too: ``multi_model_server`` already imports
+    ``gpu_scheduler`` (``multi_model_server.py:66``), so importing back the
+    other way is a cycle -- and it would drag the whole FastAPI/uvicorn stack
+    into every scheduler process for the sake of one pure function.
+
+    With ``enable_tp_worker_pool`` off this returns the released prototype's
+    ``(gpu, worker)`` grid, so callers need no second code path.
+    """
+    tp_sizes = [1]
+    if getattr(multi_model_server_args, "enable_tp_worker_pool", False):
+        tp_sizes = tp_sizes_from_model_configs(
+            getattr(multi_model_server_args, "model_configs", None)
+        ) or [1]
+    cap = getattr(multi_model_server_args, "tp_max_groups", 0) or None
+    return build_slot_plan(
+        num_gpus=multi_model_server_args.num_gpus,
+        workers_per_gpu=multi_model_server_args.workers_per_gpu,
+        tp_sizes=tp_sizes,
+        max_groups_per_tp_size=cap,
+    )
